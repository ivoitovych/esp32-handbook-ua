# Помічені помилки

Кожен запис прив'язаний до **ревізії**, надрукованої на звороті
титулу — рядок `Ревізія РРРР-ММ-ДД`. Без неї виправлення не має до чого
чіплятися: наступний друк тихо ввібрав би його, і читач першого накладу
не дізнався б, що саме змінилося.

## Ревізія 2026-08-27 · Видання перше

Перший наклад, **два примірники**, A5. Віддано в друк 2026-08-27.

### До чого прив'язана ця ревізія

Ревізія на звороті титулу — це те, що бачить читач. Але щоб виправлення
можна було звірити з тим, що справді надруковано, потрібне точне
джерело. Воно закріплене гілкою:

    backup/main-2026-08-27-1313Z-printed  →  7715504

Гілка не для розробки — це **позначка**. Комітити в неї не можна;
виправлення йдуть у `main` і потрапляють сюди рядком таблиці.

    відбиток джерел   ecb726f53d76a59c
    обсяг             448 сторінок = 28 зошитів
    кегль тіла        9.6 pt, смуга 118 мм

Маючи гілку, будь-хто зібере той самий PDF і побачить ту саму сторінку
з тим самим рядком — тобто виправлення перевірне, а не на слово.

Подробиці стану на момент друку — у `PRINT-1.md`: розклад фактчеку за
класами й перелік того, чого ми **не** перевірили.

### Виправлення

| Сторінка | Що надруковано | Як має бути | Звідки відомо |
|---|---|---|---|
| розділ 08, таблиця модулів | `ESP32-WROOM-32D`, `-32E` — «пізніші ревізії того самого» | те саме, але **варіанти з суфіксом `R2` мають 2 МБ PSRAM** | даташит `ESP32-WROOM-32E/32UE`, розділ Ordering Information: `ESP32-WROOM-32E-N4R2` / `-N8R2` / `-N16R2` — стовпчик PSRAM `2 MB (Quad SPI)` |
| розділ 23, таблиця тріажу | `ESP32-WROOM-32D`, `-32E` — «пізніші ревізії, той самий код» | те саме, плюс **суфікс `R2` означає 2 МБ PSRAM** | те саме джерело |

**Чому це важить.** Обидві таблиці відрізняють WROOM від WROVER саме
за наявністю PSRAM — рядком нижче стоїть «WROVER … з PSRAM». Але
WROOM-32E-N8R2 PSRAM має, тож ознака не тримає.

Найдорожче це в розділі 23: він для того й існує, щоб людина з платою
в руках упізнала, що тримає. За надрукованою таблицею вона прочитає
`ESP32-WROOM-32E` і вирішить «PSRAM немає». На `-N8R2` вона є — і два
мегабайти лишаться невикористані; або навпаки, код, що на неї
розраховує, впаде на `-N8`.

Розділ 08 при цьому **правильно** пояснює суфікси рядком нижче
таблиці («`N16R8` — 16 МБ флешу і 8 МБ PSRAM»). Хибний лише рядок
таблиці, і саме його читають найчастіше.

**Знайшов** М2, за кешованим даташитом. Супровідник М1 цього джерела
**не бачив**: `espressif.com` із його мережі не відкривається. Тому
виправлення внесено за доказом М2, а не за власною звіркою, і тут це
сказано навмисно.

Виправлено в `main` після друку; у примірниках першого накладу стоїть
стара редакція.

Книга це визнає прямо: вона складена приблизно за 48 годин і **не мала
людського рецензента**. Автоматика зменшила кількість помилок, але не
довела її до нуля, і невідомо, наскільки не довела. Отже цей перелік
має наповнюватися — порожній він означає лише те, що читання ще
попереду.

| Сторінка | Що надруковано | Як має бути | Звідки відомо |
|---|---|---|---|

## Як надіслати виправлення

Через сторінку проєкту: `github.com/ivoitovych/esp32-handbook-ua`,
розділ Issues. Корисно вказати сторінку, надрукований рядок і — якщо є —
джерело, з якого видно, як правильно.

Виправлення без джерела теж потрібні. «Тут не сходиться» — це вже
робота: далі джерело шукає супровідник.

## Що зробить із цим реєстр фактчекінгу

Кожне підтверджене виправлення отримує запис у `factcheck/` нарівні з
рештою: одиниця, клас перевірки, джерело, дослівна цитата. Тобто
виправлення проходить ті самі ворота, що й початкове твердження, і не
стає винятком лише тому, що виправляє.

Якщо помилка пройшла попри перевірку — це показання проти перевірки, а
не лише проти сторінки. Такі випадки описуються окремо: що саме її
пропустило й чому.
