#!/usr/bin/env python3
"""Суцільний прохід: наряди на **всю** решту, згруповані за темою.

## Навіщо окремо від `vybirka.py`

`vybirka` бере випадкову вибірку — щоб **міряти**. Тут завдання інше:
пройти все, що лишилося, по одному разу, щоб жодна одиниця не лишилася
в стані «до неї ніхто не дійшов».

Різниця не косметична. Читач у полі не має ані часу, ані мережі
розбиратися, що означає «фактчек неповний». Незакінчена робота для
нього — сигнал, що книзі можна не вірити. Перетворити «не звірено» на
«подивилися, зовнішнього джерела немає, ось де шукали» — це не менша
чесність, а більша: у другому реченні є адреса, за якою можна
перевірити нас самих.

## Чому пакет збирається за темою — і чому НЕ за «розділом книги»

Одиниці однієї теми питають про одне й те саме, тож один завантажений
документ відповідає на весь пакет. Це і швидше, і чесніше: помічник
читає джерело **цілком**, а не вихоплює рядок під конкретне твердження.

**Але тему не можна називати «розділом книги», і це куплено хвилею на
274 записи, з яких 249 виявилися посиланням на сам довідник.**

Перша редакція цього файлу писала в наряді «пакет — це один розділ
книги» й доручала «вибрати один документ на весь пакет». Найпростіший
документ, що відповідає опису «цей розділ», — **сам файл розділу**.
Помічники його й качали, з репозиторію довідника.

Заборона «книга не є джерелом для себе» стояла в тому ж наряді. Не
втримала — як не втримувала жодного разу з трьох.

Тому тут: тема називається **предметно** («I²C», «esptool», «розділи
флешу»), назва файлу книги в наряд не потрапляє **взагалі**, і наряд
сам називає документ-кандидат, з якого починати.

    tools/prochid.py <файл-переліку> <куди>   наряди по 10 пакетів
"""
from __future__ import annotations

import collections
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT / "tools"))

NA_PAKET = 5
PAKETIV_NA_NARYAD = 10

# Тема й документ-кандидат за префіксом файлу книги.
#
# **Ключ використовується лише тут, у генераторі. У наряд потрапляє
# тільки тема й адреса.** Назва файлу книги не пишеться в наряд ніколи:
# саме вона перетворила попередню хвилю на 249 самопосилань.
#
# **Порожній рядок означає «кандидата немає», і пакет так і скаже.**
# Перша редакція мовчки не друкувала рядок про кандидата, тимчасом як
# шапка обіцяла його «для кожного пакета». Помічник, що дістав такий
# наряд, відмовився працювати — і мав рацію. Це той самий дефект, що
# раніше знайшли в `vorota` й `mira_f`: **документ обіцяє те, чого код
# не робить.** Обіцянка мусить або справджуватися, або бути знята.
TEMY: dict[str, tuple[str, str]] = {
    "05": ("основи електроніки", ""),
    "06": ("живлення", "I:docs/en/api-reference/system/power_management.rst"),
    "07": ("GPIO", "I:components/esp_driver_gpio/include/driver/gpio.h"),
    "08": ("плати розробки", "I:docs/en/hw-reference/index.rst"),
    "09": ("підключення до комп'ютера", "I:docs/en/get-started/establish-serial-connection.rst"),
    "11": ("ESP-IDF: збирання й конфігурація", "I:docs/en/api-guides/build-system.rst"),
    "12": ("Arduino на ESP32", "A:docs/en/guides/core_debug.rst"),
    "13": ("PlatformIO", ""),
    "14": ("швидкі шляхи", ""),
    "16": ("завантаження й boot", "I:docs/en/api-guides/startup.rst"),
    "17": ("esptool", "E:docs/en/esptool/basic-commands.rst"),
    "18": ("розділи флешу", "I:docs/en/api-guides/partition-tables.rst"),
    "19": ("OTA", "I:docs/en/api-reference/system/ota.rst"),
    "20": ("резервні копії", "E:docs/en/esptool/basic-commands.rst"),
    "21": ("серійна прошивка", "E:docs/en/esptool/basic-commands.rst"),
    "25": ("журнал і монітор", "I:docs/en/api-guides/tools/idf-monitor.rst"),
    "30": ("пам'ять", "I:docs/en/api-reference/system/mem_alloc.rst"),
    "33": ("периферія в коді", "I:docs/en/api-reference/peripherals/index.rst"),
    "35": ("I²C", "I:docs/en/api-reference/peripherals/i2c.rst"),
    "36": ("SPI", "I:docs/en/api-reference/peripherals/spi_master.rst"),
    "37": ("1-Wire і DS18B20", "I:docs/en/api-reference/peripherals/rmt.rst"),
    "42": ("ESP-NOW", "I:docs/en/api-reference/network/esp_now.rst"),
    "43": ("LoRa", "R:README.md"),
    "45": ("датчики", ""),
    "46": ("дисплеї", "U:README.md"),
    "47": ("ключі й навантаження", ""),
    "50": ("безпека", "I:docs/en/security/flash-encryption.rst"),
    "58": ("журналювання у виробі", "I:docs/en/api-reference/system/log.rst"),
    "59": ("проєкт: монітор", ""),
    "60": ("проєкт: логер", ""),
    "61": ("проєкт: канал", ""),
    "62": ("проєкт: керування", ""),
    "63": ("проєкт: міст", ""),
}
BAZY = {
    "I": "https://raw.githubusercontent.com/espressif/esp-idf/master/",
    "E": "https://raw.githubusercontent.com/espressif/esptool/master/",
    "A": "https://raw.githubusercontent.com/espressif/arduino-esp32/master/",
    "R": "https://raw.githubusercontent.com/jgromes/RadioLib/master/",
    "U": "https://raw.githubusercontent.com/olikraus/u8g2/master/",
}

BEZ_KANDYDATA = (
    "**Документа-кандидата немає.** Тему не покриває жодне зі сховищ, "
    "названих у шапці, настільки прямо, щоб назвати файл наперед. Обери "
    "документ сам із того переліку й назви в `dzherelo` саме той, який "
    "відкривав. Якщо жоден не підходить — це чесне `nedosyazhne` з назвою "
    "документа, який був би потрібен."
)


def tema_dlya(fayl: str) -> tuple[str, str]:
    """Тема й документ-кандидат. Назва файлу книги назовні не йде."""
    pref = fayl.split("-")[0]
    tema, dok = TEMY.get(pref, ("", ""))
    if not tema:
        tema = "різне"
    if not dok:
        return tema, ""
    baza, shlyah = dok.split(":", 1)
    return tema, BAZY[baza] + shlyah

SHAPKA = """# Наряд {nomer}: суцільний прохід — {skilky} одиниць

**Генерується** `tools/prochid.py`. Це **не** вибірка: це решта, яку
досі ніхто не дивився.

## Спершу — що це за робота і що з нею стається потім

Це друкований довідник, який читатиме людина в полі: без мережі, без
часу перевіряти. Ми звіряємо кожне його твердження із зовнішнім
документом і поруч записуємо, де саме дивилися.

**Твоя відповідь не йде в книгу напряму.** Вона проходить три шари:

1. **механічний.** Адреса, що вказує на сам довідник, відкидається
   вирáзом. Вердикт без `dzherelo` відкидається як «не дивився».
2. **дослівний.** Кожну `cytata` шукають **підрядком** у документі,
   який ти назвав, — його качають ще раз і звіряють. Пробіли й лапки
   до порівняння не рахуються; слова, числа й великі літери — рахуються.
3. **змістовий.** Людина читає витяг і судить, чи він справді
   підтримує твердження.

Числа з попередньої хвилі, щоб було видно, наскільки другий шар
серйозний: із **528** заявлених підтверджень дослівну звірку пережили
**235**. Решта **293** загинули — переказ замість витягу, склеєні через
`…` шматки, правильний факт із адресою не того файлу.

**Що з цього випливає практично.** Переказана цитата не дає нам нічого
й не лишається непоміченою: її просто викидають, а одиниця повертається
в чергу. Тобто вигадати дешевше, ніж прочитати, лише в межах твоєї
відповіді — далі воно коштує дорожче за всіх, і найдорожче тобі: твоя
робота зникає цілком.

Тому:

> **Чесне «шукав, не знайшов» — це повноцінний результат, а не
> поразка.** Воно каже нам, де ми вже шукали, і саме з таких записів
> складається те речення, яке ми маємо право надрукувати.

## Якщо потрібного документа немає в мережі — скажи, якого саме

Мережа тут врізана не тобою і не нами: організація ріже все, крім
`raw.githubusercontent.com`. Даташити мікросхем на GitHub не лежать, і
це не твоя провина.

Коли одиницю можна було б звірити, **якби** ти мав конкретний документ,
— напиши це прямо:

```yaml
- odynycya: T-45-012
  verdykt: nedosyazhne
  dzherelo: https://raw.githubusercontent.com/espressif/esp-idf/master/docs/en/api-reference/peripherals/i2c.rst
  potribno: даташит BME280, розділ Electrical characteristics
  komentar: у прочитаному документі цього немає; потрібен даташит виробника
```

Поле `potribno` читає супровідник. Документи, які ти назвеш, він
дістає іншими шляхами й кладе в кеш, а одиниця йде в наступний наряд
уже з ним. **Це найкорисніше, що можна зробити з недосяжним
твердженням** — і воно варте більше, ніж вгадана цитата.

## Правило, яке важить більше за вердикт

**Кожна відповідь мусить назвати документ, який ти відкривав** — у полі
`dzherelo`, і ті відповіді, де нічого не знайшлося, теж.

Без цього «не знайшов» коштує нуль, і його пишуть, не відкривши нічого.
Такий запис не є ані знахідкою, ані свідченням її відсутності:
`tools/mira_f.py` відкидає його як «не дивився» ще до лічби, тобто це
змарнована робота.

> Наряд не каже, якої відповіді чекає. Він каже, що кожна відповідь
> мусить пред'явити.

## Пакет — це одна тема

Одиниці в пакеті питають про одне. Де для теми відомий **документ-кандидат**,
його названо просто під заголовком пакета: почни з нього, завантаж один
раз і прочитай, а вже потім проходь одиниці. Не шукай новий документ під
кожен рядок.

Кандидат — здогад, не вирок. Якщо він виявився не тим, візьми інший і
назви в `dzherelo` саме той, який відкривав.

**Де кандидата немає, у пакеті так і написано.** Це не пропуск і не
привід зупинитися: обери документ сам зі сховищ, названих нижче. Наряд
не обіцяє кандидата там, де його не має.

**Джерелом ніколи не є довідник, який ти перевіряєш.** Адреса з
`esp32-handbook`, `ivoitovych` чи `voytovych` відкидається механічно
ще до лічби — такий запис не рахується ані підтвердженням, ані
запереченням, тобто це змарнована робота.

## Вердикти й що кожен вимагає

| Вердикт | Обов'язково |
|---|---|
| `pidtverdzheno` | `dzherelo` + `cytata` — дослівний рядок із документа |
| `sperechayetsya` | `dzherelo` + `cytata` — рядок, що каже **інакше** |
| `ne_znayshov` | `dzherelo` — **адреса документа, який відкривав** |
| `nedosyazhne` | `dzherelo` + код відповіді в `komentar` |

`dzherelo` завжди починається з `https://raw.githubusercontent.com/`.

**Адреса, що вказує на сам довідник** (`esp32-handbook`, `ivoitovych`,
`voytovych`, шляхи `manual/`, `kartky/`, `dodatky/`) відкидається
механічно: довідник не є джерелом для себе. Текст книги нижче — це те,
що **перевіряють**, а не те, чим перевіряють.

## Заборони

- **переказ — не цитата.** Поле `cytata` звіряється підрядком у
  завантаженому документі;
- **пам'ять — не документ.** Велика літера замість малої валить запис
  навіть при правильному факті;
- **«стандартна практика» — не джерело**;
- **знати відповідь — не підстава написати цитату.**

## Мережа

Досяжне лише `raw.githubusercontent.com`. Усе інше — 403; **не повторюй
запит, що дав 403**.

`espressif/esp-idf` (`docs/en/…`, `components/…`, `examples/…`),
`espressif/esptool`, `espressif/arduino-esp32`, `torvalds/linux`,
`esphome/esphome`, `micropython/micropython`, `adafruit/*`,
`jgromes/RadioLib`, `olikraus/u8g2`, `Bodmer/TFT_eSPI`, `lvgl/lvgl`.

Даташити мікросхем на GitHub **не лежать**. Якщо твердження про
конкретну мікросхему — це чесне `nedosyazhne` з назвою документа, який
був би потрібен.

## Формат

```yaml
- odynycya: T-35-042
  verdykt: ne_znayshov
  dzherelo: https://raw.githubusercontent.com/espressif/esp-idf/master/docs/en/api-reference/peripherals/i2c.rst
  komentar: документ прочитано, про це в ньому не сказано

- odynycya: T-35-043
  verdykt: pidtverdzheno
  dzherelo: https://raw.githubusercontent.com/espressif/esp-idf/master/docs/en/api-reference/peripherals/i2c.rst
  cytata: |
    дослівний рядок
  komentar: що саме він підтверджує
```

**YAML:** якщо значення містить `: ` або починається з лапки — бери все
значення в одинарні лапки.

"""


def main() -> int:
    import podil
    import vybirka

    if len(sys.argv) < 2:
        print(__doc__)
        return 2
    kudy = Path(sys.argv[1])
    kudy.mkdir(parents=True, exist_ok=True)

    e1, _e2, _s1, _s2 = podil.podil_e()
    moyi = set(e1)
    vsi = vybirka.odynyci("E") + vybirka.odynyci("F")

    # Групуємо за розділом книги — саме це робить пакет дешевим.
    za_rozdilom: dict[str, list[dict]] = collections.defaultdict(list)
    for u in vsi:
        fayl = u["src"].split("/")[-1].split(":")[0]
        if fayl in moyi:
            za_rozdilom[fayl].append(u)

    pakety: list[tuple[str, list[dict]]] = []
    for fayl in sorted(za_rozdilom):
        odyn = za_rozdilom[fayl]
        for i in range(0, len(odyn), NA_PAKET):
            pakety.append((fayl, odyn[i:i + NA_PAKET]))

    naryadiv = 0
    for i in range(0, len(pakety), PAKETIV_NA_NARYAD):
        chastyna = pakety[i:i + PAKETIV_NA_NARYAD]
        naryadiv += 1
        skilky = sum(len(p) for _f, p in chastyna)
        r = [SHAPKA.format(nomer=naryadiv, skilky=skilky).rstrip("\n"), ""]
        for j, (fayl, odyn) in enumerate(chastyna, 1):
            tema, dok = tema_dlya(fayl)
            r.append(f"\n## Пакет {j} · тема: {tema}\n")
            r.append(f"Документ-кандидат: `{dok}`\n" if dok
                     else BEZ_KANDYDATA + "\n")
            for u in odyn:
                r.append(f"**`{u['id']}`**\n")
                r.append(f"> {u['tekst']}\n")
        (kudy / f"naryad-{naryadiv:03d}.md").write_text(
            "\n".join(r) + "\n", encoding="utf-8")

    print(f"prochid: одиниць {sum(len(p) for _f, p in pakety)}, "
          f"пакетів {len(pakety)}, нарядів {naryadiv} → {kudy}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
