#!/usr/bin/env python3
"""Випадкова вибірка одиниць одного класу — щоб міряти, а не збирати.

## Навіщо окремий інструмент

Штурм класу `E` брав одиниці, **відібрані рукою** як найімовірніші
носії джерела. Для здобичі це правильно: шукаєш там, де світло. Для
**міри** це нікуди не годиться — вибірка відібрана саме за тією
ознакою, яку збираєшся виміряти, і будь-який відсоток із неї завищений
за побудовою.

А міра тут потрібна окремо від здобичі. Клас `E` присвоюється
**механічно**: проза, у якій немає ані цифри, ані ідентифікатора в
зворотних лапках, ані назви чипа чи протоколу, ані одиниці виміру
словами. Це правило про **брак сигналу**, а книга друкувала про нього
інше — «поза зовнішньою звіркою: редакційне рішення, порада». Два різні
твердження. Наскільки вони розходяться, з відібраної рукою вибірки
не видно.

## Чому насіння записується в сам наряд

Вибірку можна перекидати доти, доки число не сподобається. Захист від
цього один: насіння й спосіб добору лежать у файлі поряд із
результатом, тож будь-хто повторить добір і отримає ті самі одиниці.
Перекинута вибірка одразу видно як інше насіння.

Тому насіння тут **не** береться з годинника.

## Чому тут є довірчий проміжок, а в штурмі його немає

Бо тут він означає щось. Вибірка випадкова, отже відсоток із неї —
оцінка відсотка в усій популяції, і в цієї оцінки є похибка, яку можна
порахувати. Назвати частку без похибки означало б видати 160 одиниць за
кілька тисяч одиниць.

У штурмі проміжку немає не тому, що ліньки, а тому, що там він був би
брехнею: похибка вибіркового середнього нічого не каже про вибірку,
відібрану за досліджуваною ознакою.

    tools/vybirka.py E 150          наряд на 150 одиниць класу E
    tools/vybirka.py E 150 --nasinnya 7   інше насіння, явно назване
    tools/vybirka.py --zvit <каталог>     звести вивантаження в міру
"""
from __future__ import annotations

import collections
import math
import random
import re
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))

import yaml

import vyvantazh

ROOT = Path(__file__).resolve().parent.parent
GRUPY = ("manual", "kartky", "dodatky", "inserts")
CIL = ROOT / "factcheck" / "BRIEF-SAMPLE.md"

NASINNYA = 20260826

RE_ZAHOLOVOK = re.compile(
    r"<!-- fc id:(?P<id>[\w-]+) sha:(?P<sha>\w+) src:(?P<src>[^\s]+) "
    r"klas:(?P<klas>\w+) -->")
RE_CYTATA = re.compile(r"^> (?P<t>.+)$", re.M)


def odynyci(klas: str) -> list[dict]:
    """Усі одиниці заданого класу, у сталому порядку.

    Сталість тут не косметика: `glob` і так сортується, але без
    `sorted()` порядок залежав би від файлової системи — і те саме
    насіння давало б різні вибірки на різних машинах.
    """
    out: list[dict] = []
    for grupa in GRUPY:
        katalog = ROOT / "factcheck" / grupa
        if not katalog.exists():
            continue
        for f in sorted(katalog.glob("*.md")):
            tekst = f.read_text(encoding="utf-8")
            shmatky = RE_ZAHOLOVOK.split(tekst)
            # `split` із групами віддає [до, id, sha, src, klas, після, ...]
            for i in range(1, len(shmatky), 5):
                ident, sha = shmatky[i], shmatky[i + 1]
                src, k = shmatky[i + 2], shmatky[i + 3]
                if k != klas:
                    continue
                tilo = shmatky[i + 4]
                m = RE_CYTATA.search(tilo)
                out.append({"id": ident, "sha": sha, "src": src,
                            "tekst": m.group("t").strip() if m else ""})
    return out


ZAHOLOVOK = """# Наряд: випадкова вибірка класу `{klas}`

**Генерується** `tools/vybirka.py`. Насіння **{nasinnya}**, з популяції
**{vsyoho}** одиниць відібрано **{skilky}**.

## Що саме міряємо

Не «чи можна знайти джерело, якщо старатися» — це міряв штурм, і його
вибірка була відібрана рукою під відповідь.

Тут питання інше: **яка частка класу `E` справді не має зовнішнього
референта.** Клас присвоюється механічно, за браком цифри чи
ідентифікатора в тексті. Твердження «підтягувальний резистор потрібен
завжди» цифри не має — і потрапляє в `E`, хоча це перевірюване
твердження про світ, і воно може бути хибним.

Вибірка випадкова саме для цього: відсоток із неї можна переносити на
всі {vsyoho} одиниць, а відсоток зі штурму — не можна.

## Три дозволені відповіді, і третя не гірша за першу

| Вердикт | Коли |
|---|---|
| `znayshov` | зовнішнє джерело є: адреса + **дослівна** цитата |
| `ideya` | джерела не дістав, але можу назвати документ, де воно було б |
| `spravdi-e` | зовнішнього референта справді немає: це позиція автора |

`spravdi-e` — **повноцінна відповідь і повноцінний результат.** Ми
міряємо частку, а не збираємо здобич: підтвердити, що клас поставлено
правильно, тут рівно так само цінно, як спростувати. Вигадане джерело
псує міру сильніше, ніж чесне «немає».

Цитату **не переказувати й не набирати з пам'яті**: усе, що в полі
`cytata`, звіряється підрядком у самому документі, і переказ туди не
проходить.

"""


# Клас `F` — «ще не звірено», і питання до нього інше, ніж до `E`.
#
# Для `E` ми випробовуємо **присуд**: чи правда, що джерела не існує.
# Для `F` присуду немає взагалі — є нероблена робота. Тому й вердикти
# інші, і головний із них той, якого в наряді на `E` немає:
# `sperechayetsya`. Саме він знаходить помилки в книзі.
ZAHOLOVOK_F = """# Наряд: випадкова вибірка класу `{klas}` — ще не звірене

**Генерується** `tools/vybirka.py`. Насіння **{nasinnya}**, з популяції
**{vsyoho}** одиниць відібрано **{skilky}**.

## Що це за одиниці

Клас `F` — «ще не звірено». Не «сумнівне»: до цих рядків просто ніхто
не дійшов.

## Правило, яке важить більше за вердикт

**Кожна відповідь мусить назвати документ, який ти дивився.** Кожна —
включно з тими, де нічого не знайшлося.

Причина проста: без цього «не знайшов» коштує нуль, і його пишуть, не
відкривши нічого. Такий запис не є ані знахідкою, ані свідченням її
відсутності, і `tools/measure_f.py` відкидає його як «не дивився» ще до
лічби.

> Наряд не каже, якої відповіді чекає. Він каже, що кожна відповідь
> мусить пред'явити.

## Вердикти й що кожен вимагає

| Вердикт | Обов'язково |
|---|---|
| `pidtverdzheno` | `dzherelo` + `cytata` — дослівний рядок із документа |
| `sperechayetsya` | `dzherelo` + `cytata` — рядок, що каже **інакше** |
| `ne_znayshov` | `dzherelo` — **адреса документа, який дивився** |
| `nedosyazhne` | `dzherelo` + код відповіді в `komentar` (403, 404, заглушка) |

`dzherelo` завжди починається з `https://raw.githubusercontent.com/`.

**Адреса, що вказує на сам довідник** (`esp32-handbook`, `ivoitovych`,
`voytovych`, або шлях виду `manual/…`, `kartky/…`, `dodatky/…`)
відкидається механічно: довідник не є джерелом для себе. Текст книги,
наведений нижче, — це те, що **перевіряють**, а не те, чим перевіряють.

## Як шукати

По кожній одиниці: вибери документ, у якому це мало б стояти, завантаж
його `curl`-ом, подивися. Далі — вердикт за таблицею вище.

Досяжне лише `raw.githubusercontent.com`. Усе інше — 403; **не
повторюй запит, що дав 403**.

Куди дивитися:
`espressif/esp-idf` (`docs/en/…`, `components/…`, `examples/…`),
`espressif/esptool`, `espressif/arduino-esp32`, `torvalds/linux`
(`drivers/…`), `esphome/esphome`, `micropython/micropython`,
`adafruit/*`, `jgromes/RadioLib`, `olikraus/u8g2`, `Bodmer/TFT_eSPI`,
`lvgl/lvgl`.

Окремо: `espressif.com` на деякі адреси віддає **HTML-заглушку
15 495 байтів із кодом 200**. Відповідь «успішна», документа немає. Не
схоже на документ — `nedosyazhne`.

## Заборони

Повністю — `factcheck/POMICHNYKY.md`. Тут найважливіші:

- **переказ — не цитата.** Усе в полі `cytata` звіряється підрядком у
  завантаженому документі;
- **пам'ять — не документ.** Велика літера замість малої валить запис
  навіть при правильному факті;
- **«стандартна практика» — не джерело**;
- **знати відповідь — не підстава написати цитату.** Факт відомий, а
  рядка не видно — це `ne_znayshov` із назвою того, де дивився.

## Про `sperechayetsya`

Найцінніша відповідь: книгу ще можна виправити. Але лише коли **бачиш
інший текст**, а не пам'ятаєш інакше.

## Формат

```yaml
- odynycya: T-12-034
  verdykt: ne_znayshov
  dzherelo: https://raw.githubusercontent.com/espressif/esp-idf/master/docs/en/api-reference/peripherals/gpio.rst
  komentar: документ прочитано, про це в ньому не сказано

- odynycya: T-12-035
  verdykt: pidtverdzheno
  dzherelo: https://raw.githubusercontent.com/espressif/esp-idf/master/...
  cytata: |
    дослівний рядок із завантаженого документа
  komentar: що саме він підтверджує
```

**YAML:** якщо значення містить `: ` або починається з лапки — бери все
значення в одинарні лапки.

"""

ZVIT = ROOT / "factcheck" / "MEASURE-NO-SIGNAL.md"

RE_NE_TVERDZHENNYA = re.compile(
    r"не тверджен|самоопис|заголов|назв[ау] колонк|вступ до перел|підпис",
    re.I)


KANDYDATY = ROOT / "factcheck" / "vybirka-kandydaty.yaml"


def tretiy_shar_vybirky(zap: list[dict]) -> tuple[int, int]:
    """Скільки заявлених `znayshov` справді стоять за названою адресою."""
    kand = [{"title": str(z.get("odynycya", "?")),
             "source": str(z.get("source", "")).strip(),
             "quote": str(z.get("quote", "")),
             "zvidky": z.get("_fayl", "?")}
            for z in zap if str(z.get("verdykt")) == "znayshov"]
    if not kand:
        return 0, 0
    KANDYDATY.write_text(
        "# Кандидати `znayshov` із **випадкової** вибірки. Не реєстр:\n"
        "# їх перевіряє третій шар перед тим, як число піде у звіт.\n"
        + yaml.safe_dump(kand, allow_unicode=True, sort_keys=False),
        encoding="utf-8")
    try:
        import citaty
    except ImportError:
        return 0, len(kand)
    naslidky, _ = citaty.perevirka(True, [KANDYDATY])
    return (sum(1 for x in naslidky if x.get("stan") == "ok"), len(kand))


def zvesty(katalog: Path) -> int:
    """Звести вивантаження випадкової вибірки в міру з похибкою."""
    # Перший прогін цієї міри втратив 40 із 160 одиниць на зламаному
    # YAML — і втратив **не випадково**: обидва помічники, чиї файли
    # впали, давали найвищу частку «має референта». Тобто мовчазна
    # втрата зсувала саме те число, яке міряють, і зсувала вниз.
    #
    # Звідси `tools/vyvantazh.py`: механічне лагодження того, що
    # написано, і поіменний перелік полагодженого.
    zap, polagodzheni, bidy = vyvantazh.chytaty(katalog)
    for z in zap:
        z["_hto"] = str(z.get("_fayl", "?")).split("-")[0]

    n = len(zap)
    if not n:
        print("vybirka: вивантажень не знайдено")
        return 1

    # Четвертий стан, якого в наряді не було окремим вердиктом: одиниця,
    # яка **взагалі не є твердженням про світ** — заголовок стовпця,
    # вступ до переліку, рядок таблиці, у якій книга описує сама себе.
    #
    # Знахідка обопільна: я побачив її в першому ж пакеті (`T-00-022` —
    # рядок таблиці класів), М2 узагальнив краще: це міра **дрібності
    # інструмента**, а не книги. Верифікувати таку одиницю нема чого, і
    # соромитися теж нема чого — просто її не мало бути.
    #
    # Рахується взірцем по полю `chomu`, і це **здогад, а не вердикт**:
    # помічник писав його вільним текстом. Числу можна вірити як
    # порядку величини, не як межі.
    ne_tverdzhennya = sum(
        1 for z in zap
        if str(z.get("verdykt")) == "spravdi-e"
        and RE_NE_TVERDZHENNYA.search(str(z.get("chomu", ""))))

    c = collections.Counter(str(z.get("verdykt", "?")) for z in zap)
    maye_referenta = c["znayshov"] + c["ideya"]
    pozyciya = c["spravdi-e"] - ne_tverdzhennya

    # `znayshov` із вибірки теж проходить третій шар. Спершу цього не
    # робилося — перевіряли лише штурм, — і це була діра: саме ці
    # записи дають найгучнішу частину відповіді, і саме на них тисне
    # наряд.
    #
    # Знахідка М2: наряд «випробуй присуд» читається як «спростуй
    # присуд», і тиск виробляє вигадані спростування так само справно,
    # як тиск «знайди джерело» виробляв джерела.
    #
    # **Але на підсумок це не впливає, і не випадково.** Провалена
    # цитата не доводить, що референта немає, — вона доводить, що його
    # не здобули. Тобто одиниця падає з `znayshov` у `ideya`, а обидва
    # стани рахуються як «має референт». Міра стійка до цієї вади за
    # побудовою: тиск переставляє одиниці між кошиками, не додаючи їх.
    vystoyalo, zayavleno = tretiy_shar_vybirky(zap)

    def promizhok(k: int) -> tuple[float, float]:
        """Вілсонів проміжок 95%. Для часток біля 0 нормальний бреше."""
        p, z = k / n, 1.96
        seredyna = (p + z * z / (2 * n)) / (1 + z * z / n)
        pivshyryna = (z / (1 + z * z / n)) * math.sqrt(
            p * (1 - p) / n + z * z / (4 * n * n))
        return (max(0.0, seredyna - pivshyryna),
                min(1.0, seredyna + pivshyryna))

    nyz, verh = promizhok(maye_referenta)

    # Популяція береться **живою**, а не числом із дня добору. Клас `E`
    # тане: щойно злиття роботи М2 перевело частину його одиниць у
    # `A`/`B`/`D`, і 3892 стало 3350. Вписане число тихо старіє й
    # починає брехати рівно там, де його читають найуважніше — у рядку
    # «стільки-то тверджень закрито надто щедро».
    #
    # Частка при цьому лишається чинною: вибірку брали з `E`, і
    # відповідає вона на питання про `E`, хоч би скільки їх було зараз.
    populyaciya = len(odynyci("E"))

    # Розкид між помічниками. Це не косметика: якщо різні судді на тих
    # самих даних дають різні частки, справжня похибка більша за
    # вибіркову, і Вілсонів проміжок нижче — оптимістичний.
    po_hto: dict[str, list[int]] = collections.defaultdict(lambda: [0, 0])
    for z in zap:
        hto = str(z.get("_hto"))
        po_hto[hto][1] += 1
        if str(z.get("verdykt")) in ("znayshov", "ideya"):
            po_hto[hto][0] += 1
    chastky = [k / v for k, v in po_hto.values() if v]

    r = [f"""# Міра класу `E`

**Генерується** `tools/vybirka.py --zvit`. Наряд —
`factcheck/BRIEF-SAMPLE.md`, там же насіння добору.

Питання: **яка частка класу `E` має зовнішній референт**, тобто
поставлена надто щедро. Вибірка **випадкова**, тому відсоток звідси
можна переносити на весь клас — на відміну від `factcheck/SWEEP-NO-SIGNAL.md`,
де вибірку відібрано рукою під відповідь.

## Результат

Одиниць у вибірці: **{n}**.

| Вердикт | Скільки | Частка |
|---|---|---|
| `znayshov` — джерело здобуто | {c['znayshov']} | {c['znayshov'] / n:.0%} |
| `ideya` — джерело назване, не здобуте | {c['ideya']} | {c['ideya'] / n:.0%} |
| `spravdi-e` — позиція автора, референта немає | {pozyciya} | {pozyciya / n:.0%} |
| **не твердження взагалі** — заголовок, самоопис книги | {ne_tverdzhennya} | {ne_tverdzhennya / n:.0%} |

Із {zayavleno} заявлених `znayshov` третій шар витримали **{vystoyalo}**.
Решту зараховано як `ideya`: провалена цитата доводить, що джерела **не
здобули**, а не що його немає. На підсумок це не впливає — обидва стани
рахуються як «має референт», і саме тому міра стійка до тиску наряду.

**Має зовнішній референт: {maye_referenta} з {n} = {maye_referenta / n:.0%}**
(95% Вілсон: {nyz:.0%}–{verh:.0%}).

## Клас `E` — це не одна річ, а три

Розкладання вибірки показує, що присуд `E` накриває три стани, і
плутати їх означає соромитися не за те:

| Стан | Частка | У класі | Що з цим робити |
|---|---|---|---|
| має зовнішній референт | {maye_referenta / n:.0%} | ~{round(maye_referenta / n * populyaciya):d} | **перевіряти** — присуд помилковий |
| позиція автора | {pozyciya / n:.0%} | ~{round(pozyciya / n * populyaciya):d} | лишити; це чесний `E` |
| не твердження взагалі | {ne_tverdzhennya / n:.0%} | ~{round(ne_tverdzhennya / n * populyaciya):d} | **не одиниця**; міра дрібності витягача |

Третій рядок — знахідка М2, узагальнена з їхнього кошика «решта», і
підтверджена тут на **випадковій** вибірці, тобто вже з правом
переносити частку на весь клас. Заголовок стовпця, вступ до переліку,
рядок таблиці, у якій книга описує власний реєстр, — усе це витягач
називає твердженням, бо ділить текст механічно.

Цю частку не треба ані верифікувати, ані ставити собі в докір: вона
міряє **інструмент**, а не книгу. Але й ховати її в «неперевірене»
не можна — саме звідси береться завищене відчуття боргу.

Тобто приблизно **{round(maye_referenta / n * populyaciya):d}** одиниць
класу `E` — з {populyaciya} на цей момент — насправді перевірювані, і
присуд «поза зовнішньою звіркою» на них не мав би стояти.

Популяція рахується при кожному прогоні, а не береться з дня добору:
клас `E` тане, коли записи доказів переводять його одиниці в інші
класи. Частка від цього не змінюється — вибірку брали з `E`, і
відповідає вона про `E`.

## Чому цей проміжок оптимістичний

Вілсонів проміжок рахує лише похибку добору — він припускає, що в
кожної одиниці є одна правильна відповідь, яку будь-який суддя дав би
однаково. Тут це не так.

Частки «має референта» по помічниках: {', '.join(f'{x:.0%}' for x in sorted(chastky))}.

Розкид між суддями **більший за вибіркову похибку**. Отже головне
джерело невизначеності — не те, що одиниць {n} замість {populyaciya}, а те, що
межа між «порада автора» і «твердження про світ» проводиться
по-різному. Звужувати проміжок довшою вибіркою марно, доки межа не
описана точніше.

Це не привід відкинути міру. Порядок величини вона встановлює твердо:
йдеться про **сотні** хибно віднесених одиниць, не про десяток.
"""]
    if polagodzheni:
        r.append("\n## Полагоджені вивантаження\n")
        r.append("Механічно виправлено (значення взято в лапки), вміст "
                 "не змінено: "
                 + ", ".join(f"`{b}`" for b in polagodzheni) + ".\n")
        r.append("Причина в брифінгу супровідника, не в помічниках: "
                 "формат вимагав писати двокрапку всередині значення. "
                 "Див. `tools/vyvantazh.py`.\n")
    if bidy:
        r.append("\nНе розібралися й пропущені: "
                 + ", ".join(f"`{b}`" for b in bidy) + ".\n")

    r.append("\n## Одиниці, у яких референт є\n")
    r.append("| Одиниця | Вердикт | Де шукати або що знайдено |")
    r.append("|---|---|---|")
    for z in zap:
        v = str(z.get("verdykt"))
        if v not in ("znayshov", "ideya"):
            continue
        shcho = str(z.get("propozyciya") or z.get("komentar") or "").strip()
        r.append(f"| `{z.get('odynycya','?')}` | {v} | {shcho[:140]} |")

    ZVIT.write_text("\n".join(r) + "\n", encoding="utf-8")
    print(f"vybirka: вибірка {n}, має референта {maye_referenta} "
          f"({maye_referenta / n:.0%}, 95% {nyz:.0%}–{verh:.0%}) "
          f"→ {ZVIT.relative_to(ROOT)}")
    return 0


def main() -> int:
    if "--zvit" in sys.argv:
        i = sys.argv.index("--zvit")
        if i + 1 >= len(sys.argv):
            print("vybirka: --zvit потребує каталогу вивантажень")
            return 2
        return zvesty(Path(sys.argv[i + 1]))
    if len(sys.argv) < 3:
        print(__doc__)
        return 2
    klas = sys.argv[1].upper()
    skilky = int(sys.argv[2])
    nasinnya = NASINNYA
    if "--nasinnya" in sys.argv:
        nasinnya = int(sys.argv[sys.argv.index("--nasinnya") + 1])

    vsi = odynyci(klas)
    if not vsi:
        print(f"vybirka: одиниць класу {klas} не знайдено")
        return 1
    skilky = min(skilky, len(vsi))
    vybir = random.Random(nasinnya).sample(vsi, skilky)
    vybir.sort(key=lambda z: z["id"])

    # Розмір пакета — не косметика. Пакет визначає, скільки одиниць
    # помічник тримає в голові за один захід: довгий пакет дає спокусу
    # відповісти «оптом», коротший тримає увагу на кожній одиниці
    # окремо.
    na_paket = 8
    if "--na-paket" in sys.argv:
        na_paket = int(sys.argv[sys.argv.index("--na-paket") + 1])

    shapka = ZAHOLOVOK_F if klas == "F" else ZAHOLOVOK
    r = [shapka.format(klas=klas, nasinnya=nasinnya,
                       vsyoho=len(vsi), skilky=skilky).rstrip("\n"), ""]
    for i, z in enumerate(vybir):
        if i % na_paket == 0:
            r.append(f"\n## Пакет {i // na_paket + 1}\n")
        r.append(f"**`{z['id']}`** · `{z['src']}`\n")
        r.append(f"> {z['tekst']}\n")
    CIL.write_text("\n".join(r) + "\n", encoding="utf-8")
    print(f"vybirka: клас {klas}, популяція {len(vsi)}, вибірка {skilky}, "
          f"насіння {nasinnya} → {CIL.relative_to(ROOT)}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
