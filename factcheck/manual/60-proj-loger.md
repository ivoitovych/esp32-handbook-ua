# Фактчекінг: `manual/60-proj-loger.md`

Одиниць твердження: **147**. Клас доказу й формат запису — `factcheck/SCHEMA.md`.

Цей файл **генерується**: текст книги береться з джерела, докази — з `factcheck/evidence/`. Правити вручну нема сенсу.

**Що в блоці «Твердження, коротко».** Для прози, рядка коду й зв'язки схеми — **дослівний текст книги**. Для комірки таблиці — рендер (`BME280 · Адреса → 0x76`), якого в книзі немає; дослівний рядок такої одиниці стоїть окремим блоком нижче.

---

<!-- fc id:T-60-001 sha:c6bb5de4 src:manual/60-proj-loger.md:3 klas:E -->
### T-60-001 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Пристрій прокидається за розкладом, міряє, записує на картку й засинає.

**Контекст**

```
# 60. Автономний логер {#proj-loger}

Пристрій прокидається за розкладом, міряє, записує на картку й засинає.
Живе від акумулятора місяцями.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-002 sha:533db87a src:manual/60-proj-loger.md:4 klas:E -->
### T-60-002 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Живе від акумулятора місяцями.

**Контекст**

```
# 60. Автономний логер {#proj-loger}

Пристрій прокидається за розкладом, міряє, записує на картку й засинає.
Живе від акумулятора місяцями.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-003 sha:1c9cb872 src:manual/60-proj-loger.md:6 klas:E -->
### T-60-003 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Головна тема проєкту — **бюджет енергії** (розділ 06) і надійність запису при зникненні живлення (розділ 49).

**Контекст**

```
# 60. Автономний логер {#proj-loger}

Головна тема проєкту — **бюджет енергії** (розділ 06) і надійність
запису при зникненні живлення (розділ 49).
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-004 sha:df07925b src:manual/60-proj-loger.md:11 klas:B -->
### T-60-004 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Вхід:** BME280 (I²C) і DS18B20 (1-Wire) для виносного вимірювання.

**Контекст**

```
## Постановка

**Вхід:** BME280 (I²C) і DS18B20 (1-Wire) для виносного вимірювання.
```

**Доказ**

- **Статус:** derived — первинне похідне — першоджерело отримано, твердження випливає однозначно
- **Джерело:** I²C spec (UM10204) та 1-Wire spec — обидва вимагають open-drain виходу
- **Дослівно з джерела:**
  > I²C spec (UM10204):
  > "Both SDA and SCL are bidirectional lines, connected to a positive supply
  > voltage via a current-source or pull-up resistor. ... The output stages of
  > devices connected to the bus must have an open-drain or open-collector to
  > perform the wired-AND function."
  > 
  > 1-Wire (Maxim): Обов'язково open-drain вихід і pull-up резистор.
- **Спосіб і дата:** I²C spec (i2c-um10204.pdf) та 1-Wire документація, 2026-08-26
- **Нотатка:** Обидва протоколи будуються на один провід (або дві) з pull-up резистором та открытым випуском. Це забезпечує можливість кількох пристроїв на одній лінії.
- **Прохід:** m2-65-elektronika-05

---

<!-- fc id:T-60-005 sha:918c6132 src:manual/60-proj-loger.md:13 klas:E -->
### T-60-005 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Вихід:** файл CSV на microSD, один рядок на вимірювання.

**Контекст**

```
## Постановка

**Вихід:** файл CSV на microSD, один рядок на вимірювання.
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** SPI протокол: чотирипровідний інтерфейс послідовної передачі даних
- **Дослівно з джерела:**
  > SPI складається з чотирьох ліній:
  > - SCK (Serial Clock) — тактування
  > - MOSI (Master Out Slave In) — дані від головного до ведених
  > - MISO (Master In Slave Out) — дані від ведених до головного
  > - CS (Chip Select) — вибір мікросхеми
  > 
  > Для повного спостереження потрібен логічний аналізатор з 4+ каналами.
- **Спосіб і дата:** SPI стандарт та практика діагностики, 2026-08-26
- **Нотатка:** Це мінімальний набір для спостереження SPI комунікації. На практиці може бути кілька CS ліній для різних приладів.
- **Прохід:** m2-66-analizator-28

---

<!-- fc id:T-60-006 sha:13d73ded src:manual/60-proj-loger.md:15 klas:A -->
### T-60-006 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Живлення:** 18650, ціль — не менше трьох місяців при вимірюванні раз на 15 хвилин.

**Контекст**

```
## Постановка

**Живлення:** 18650, ціль — не менше трьох місяців при вимірюванні раз
на 15 хвилин.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** source-cache/ncr18650b.pdf
- **Дослівно з джерела:**
  > Discharge: CC 1C, 2.50V cut-off at 25°C
- **Спосіб і дата:** Source document retrieved 2026-08-27 from the local cache; quote verified against it by substring match.
- **Нотатка:** Datasheet підтверджує, що 18650 може забезпечити тривалу роботу при розташуванні вимірювачами, як описано в handbook.

- **Прохід:** m2-hvylya3

---

<!-- fc id:T-60-007 sha:ad9247ee src:manual/60-proj-loger.md:18 klas:C -->
### T-60-007 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Час:** RTC DS3231 із власною батарейкою — мережі немає, SNTP недоступний (розділ 40).

**Контекст**

```
## Постановка

**Час:** RTC DS3231 із власною батарейкою — мережі немає, SNTP недоступний
(розділ 40).
```

**Доказ**

- **Статус:** named-unreachable — вторинне — джерело не дістається звідси; URL записано, цитати немає
- **Джерело:** DS3231 Extremely Accurate I2C-Integrated RTC/TCXO/Crystal, datasheet Maxim Integrated (нині Analog Devices) — https://www.analog.com/en/products/ds3231.html
- **Що шукати в джерелі:** Наявність виводу VBAT і резервного живлення від батареї при зникненні основного (звідки випливає «лишається живим сам»); струм від VBAT у режимі підтримки; діапазон напруги VBAT (сумісність із CR2032, 3 В); підтвердження інтерфейсу I²C і адреси `0x68`.
- **Нотатка:** Той самий висновок, що вже зафіксований у `m2-09-hc-sr04.yaml`: `analog.com` відмовляє цій мережі на рівні Akamai, і чотири перевірені тоді дзеркала (Mouser, DigiKey, SparkFun, rcscomponents) PDF не віддали. Тут — та сама мікросхема, інша цитата книги (розділ 60, а не додаток E), тому новий запис, а не дублювання.
Межа та сама, що вже названа раніше й варта повторення: «з батарейкою CR2032» — властивість **модуля-брейкборда**, а не DS3231. Сам кристал вимагає лише напругу на `VBAT` у певному діапазоні; тип елемента (CR2032, таблетка, суперконденсатор) — рішення виробника плати. Навіть якби datasheet відкрився, цю частину речення він не підтвердить і не спростує.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-008 sha:99eb74ab src:manual/60-proj-loger.md:21 klas:A -->
### T-60-008 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Поведінка при відмові:** картка недоступна — вимірювання накопичуються в RTC RAM; датчик мовчить — записується позначка; акумулятор розряджений — пристрій засинає назавжди, не псуючи картку.

**Контекст**

```
## Постановка

**Поведінка при відмові:** картка недоступна — вимірювання
накопичуються в RTC RAM; датчик мовчить — записується позначка;
акумулятор розряджений — пристрій засинає назавжди, не псуючи картку.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/docs/en/api-reference/system/sleep_modes.rst
- **Дослівно з джерела:**
  > If some variables in the program are placed into RTC SLOW memory (for example, using
  > ``RTC_DATA_ATTR`` attribute), RTC SLOW memory will be kept powered on by default.
  > This can be overridden using :cpp:func:`esp_sleep_pd_config` function, if desired.
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Підтверджує розділи 06 і 60. Уточнення, варте наступного проходу: «за замовчуванням» — тобто збереження можна й вимкнути, і на чипах, де є лише RTC FAST, усі три атрибути йдуть туди ж.
- **Прохід:** pass-02-povedinka

---

<!-- fc id:T-60-009 sha:648244e3 src:manual/60-proj-loger.md:27 klas:F -->
### T-60-009 · tablycya-shapka · `manual/60-proj-loger.md`

**Твердження, коротко**

> | Позиція | Кількість | Примітка |

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-010 sha:86983c72 src:manual/60-proj-loger.md:29 klas:S -->
### T-60-010 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> ESP32 classic або C3 · Кількість → 1

**Дослівно з книги**

```
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** self-consistent — внутрішня звірка — книга сходиться сама з собою; зовнішнього підтвердження немає
- **Джерело:** ВНУТРІШНЯ ЗВІРКА (книга проти себе): manual/60-proj-loger.md, рядок 28
- **Дослівно з джерела:**
  > | ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
- **Спосіб і дата:** дослівна цитата з таблиці компонентів
- **Нотатка:** Шар 3 цього не перевіряє: джерело — сама книга. Потрібен окремий інструмент внутрішньої несуперечності.
- **Прохід:** m2-68-proj-60

---

<!-- fc id:T-60-011 sha:cc0e8296 src:manual/60-proj-loger.md:29 klas:F -->
### T-60-011 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> ESP32 classic або C3 · Примітка → **не плата розробки** для фінальної версії

**Дослівно з книги**

```
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-012 sha:ae098f83 src:manual/60-proj-loger.md:30 klas:F -->
### T-60-012 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> BME280 · Кількість → 1

**Дослівно з книги**

```
| BME280 | 1 | I²C |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-013 sha:9800167e src:manual/60-proj-loger.md:30 klas:F -->
### T-60-013 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> BME280 · Примітка → I²C

**Дослівно з книги**

```
| BME280 | 1 | I²C |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-014 sha:42ba36b1 src:manual/60-proj-loger.md:31 klas:F -->
### T-60-014 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> DS18B20 у зонді · Кількість → 1

**Дослівно з книги**

```
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-015 sha:aa59c919 src:manual/60-proj-loger.md:31 klas:A -->
### T-60-015 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> DS18B20 у зонді · Примітка → 1-Wire, резистор 4.7 кОм

**Дослівно з книги**

```
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** DS18B20 datasheet (Maxim), Figure 4 «Supplying the Parasite-Powered DS18B20», Figure 5 і блок-схема — номінал підтягування на DQ
- **Дослівно з джерела:**
  > (на схемах підключення DQ підтягнуто до VPU резистором 4.7k —
  > Figure 1 «DS18B20 Block Diagram», Figure 4, Figure 5)
  > 
  > The control line requires a weak pullup resistor since all devices are
  > linked to the bus via a 3-state or open-drain port (the DQ pin in the case
  > of the DS18B20).
- **Спосіб і дата:** PDF Maxim через дзеркало cdn-shop.adafruit.com, pdftotext -layout, 2026-08-26
- **Нотатка:** 4.7 кОм — номінал із самих схем datasheet, а не з практики спільноти. Джерело водночас пояснює, **чому** підтягування взагалі потрібне: вихід відкритий стік, лінію нема чому підняти.
- **Прохід:** m2-04-ds18b20

---

<!-- fc id:T-60-016 sha:376fd675 src:manual/60-proj-loger.md:32 klas:E -->
### T-60-016 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> DS3231 модуль RTC · Кількість → 1

**Дослівно з книги**

```
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** —
- **Дослівно з джерела:**
  > —
- **Спосіб і дата:** Немає зовнішнього джерела для перевірки — кількість модулів у BOM проєкту це вибір автора схеми, а не характеристика кристала.
- **Нотатка:** Той самий випадок, що DS3231 · Бібліотека в `m2-18-dodatok-e-reshta.yaml`.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-017 sha:90f9a897 src:manual/60-proj-loger.md:32 klas:C -->
### T-60-017 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> DS3231 модуль RTC · Примітка → I²C, з батарейкою CR2032

**Дослівно з книги**

```
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** named-unreachable — вторинне — джерело не дістається звідси; URL записано, цитати немає
- **Джерело:** DS3231 Extremely Accurate I2C-Integrated RTC/TCXO/Crystal, datasheet Maxim Integrated (нині Analog Devices) — https://www.analog.com/en/products/ds3231.html
- **Що шукати в джерелі:** Наявність виводу VBAT і резервного живлення від батареї при зникненні основного (звідки випливає «лишається живим сам»); струм від VBAT у режимі підтримки; діапазон напруги VBAT (сумісність із CR2032, 3 В); підтвердження інтерфейсу I²C і адреси `0x68`.
- **Нотатка:** Той самий висновок, що вже зафіксований у `m2-09-hc-sr04.yaml`: `analog.com` відмовляє цій мережі на рівні Akamai, і чотири перевірені тоді дзеркала (Mouser, DigiKey, SparkFun, rcscomponents) PDF не віддали. Тут — та сама мікросхема, інша цитата книги (розділ 60, а не додаток E), тому новий запис, а не дублювання.
Межа та сама, що вже названа раніше й варта повторення: «з батарейкою CR2032» — властивість **модуля-брейкборда**, а не DS3231. Сам кристал вимагає лише напругу на `VBAT` у певному діапазоні; тип елемента (CR2032, таблетка, суперконденсатор) — рішення виробника плати. Навіть якби datasheet відкрився, цю частину речення він не підтвердить і не спростує.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-018 sha:c08c37e1 src:manual/60-proj-loger.md:33 klas:E -->
### T-60-018 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Модуль microSD · Кількість → 1

**Дослівно з книги**

```
| Модуль microSD | 1 | SPI |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-019 sha:b5b70cc2 src:manual/60-proj-loger.md:33 klas:F -->
### T-60-019 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Модуль microSD · Примітка → SPI

**Дослівно з книги**

```
| Модуль microSD | 1 | SPI |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-020 sha:69538962 src:manual/60-proj-loger.md:34 klas:C -->
### T-60-020 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> 18650 з захистом · Кількість → 1

**Дослівно з книги**

```
| 18650 з захистом | 1 | розділ 53 |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** named-unreachable — вторинне — джерело не дістається звідси; URL записано, цитати немає
- **Джерело:** https://www.analog.com/ (TP4056 і DW01 datasheet) та специфікації виробників елементів 18650
- **Що шукати в джерелі:** для TP4056: типовий струм заряджання і резистор, яким він задається; склад варіанта із захистом (DW01 плюс подвійний MOSFET) і що саме він захищає. Для елементів: напруга повного заряду 4.2 В, номінальна 3.7 В, межа розряду, заборона заряджання нижче 0 °C і її причина (металізація літію).
- **Нотатка:** Розділ 53 — найризикованіший у книзі з погляду наслідків, тож ця група має бути закрита першою, щойно з'явиться доступ.
- **Прохід:** pass-03-nedostupni

---

<!-- fc id:T-60-021 sha:d7115003 src:manual/60-proj-loger.md:34 klas:C -->
### T-60-021 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> 18650 з захистом · Примітка → розділ 53

**Дослівно з книги**

```
| 18650 з захистом | 1 | розділ 53 |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** named-unreachable — вторинне — джерело не дістається звідси; URL записано, цитати немає
- **Джерело:** https://www.analog.com/ (TP4056 і DW01 datasheet) та специфікації виробників елементів 18650
- **Що шукати в джерелі:** для TP4056: типовий струм заряджання і резистор, яким він задається; склад варіанта із захистом (DW01 плюс подвійний MOSFET) і що саме він захищає. Для елементів: напруга повного заряду 4.2 В, номінальна 3.7 В, межа розряду, заборона заряджання нижче 0 °C і її причина (металізація літію).
- **Нотатка:** Розділ 53 — найризикованіший у книзі з погляду наслідків, тож ця група має бути закрита першою, щойно з'явиться доступ.
- **Прохід:** pass-03-nedostupni

---

<!-- fc id:T-60-022 sha:a5e98667 src:manual/60-proj-loger.md:35 klas:C -->
### T-60-022 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> TP4056 **з захистом** · Кількість → 1

**Дослівно з книги**

```
| TP4056 **з захистом** | 1 | розділ 53 |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** named-unreachable — вторинне — джерело не дістається звідси; URL записано, цитати немає
- **Джерело:** https://www.analog.com/ (TP4056 і DW01 datasheet) та специфікації виробників елементів 18650
- **Що шукати в джерелі:** для TP4056: типовий струм заряджання і резистор, яким він задається; склад варіанта із захистом (DW01 плюс подвійний MOSFET) і що саме він захищає. Для елементів: напруга повного заряду 4.2 В, номінальна 3.7 В, межа розряду, заборона заряджання нижче 0 °C і її причина (металізація літію).
- **Нотатка:** Розділ 53 — найризикованіший у книзі з погляду наслідків, тож ця група має бути закрита першою, щойно з'явиться доступ.
- **Прохід:** pass-03-nedostupni

---

<!-- fc id:T-60-023 sha:b13324f3 src:manual/60-proj-loger.md:35 klas:C -->
### T-60-023 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> TP4056 **з захистом** · Примітка → розділ 53

**Дослівно з книги**

```
| TP4056 **з захистом** | 1 | розділ 53 |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** named-unreachable — вторинне — джерело не дістається звідси; URL записано, цитати немає
- **Джерело:** https://www.analog.com/ (TP4056 і DW01 datasheet) та специфікації виробників елементів 18650
- **Що шукати в джерелі:** для TP4056: типовий струм заряджання і резистор, яким він задається; склад варіанта із захистом (DW01 плюс подвійний MOSFET) і що саме він захищає. Для елементів: напруга повного заряду 4.2 В, номінальна 3.7 В, межа розряду, заборона заряджання нижче 0 °C і її причина (металізація літію).
- **Нотатка:** Розділ 53 — найризикованіший у книзі з погляду наслідків, тож ця група має бути закрита першою, щойно з'явиться доступ.
- **Прохід:** pass-03-nedostupni

---

<!-- fc id:T-60-024 sha:8315296f src:manual/60-proj-loger.md:36 klas:E -->
### T-60-024 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Перетворювач buck-boost на 3.3 В · Кількість → 1

**Дослівно з книги**

```
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** —
- **Дослівно з джерела:**
  > —
- **Спосіб і дата:** Немає зовнішнього джерела: кількість деталей і посилання «див. нижче» — не електричні величини, а організація BOM і тексту.
- **Нотатка:** Змістовна частина цього ж рядка («Резистори 4.7 кОм · Примітка → I²C і 1-Wire») звірена окремо нижче й вийшла класом `A`. | Переглянуто 2026-08-27 у розборі 36 надмірних E. Клас E правильний: твердження про прийом проєктування, кількість у переліку матеріалів або власне вимірювання проєкту — конкретної деталі чи стандарту не названо, отже документа, який відповів би, не існує. Число в назві є, але воно номінал у пораді, а не величина з паспорта.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-025 sha:b07cbc0d src:manual/60-proj-loger.md:36 klas:E -->
### T-60-025 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Перетворювач buck-boost на 3.3 В · Примітка → ключове рішення, див. нижче

**Дослівно з книги**

```
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** —
- **Дослівно з джерела:**
  > —
- **Спосіб і дата:** Немає зовнішнього джерела: кількість деталей і посилання «див. нижче» — не електричні величини, а організація BOM і тексту.
- **Нотатка:** Змістовна частина цього ж рядка («Резистори 4.7 кОм · Примітка → I²C і 1-Wire») звірена окремо нижче й вийшла класом `A`. | Переглянуто 2026-08-27 у розборі 36 надмірних E. Клас E правильний: твердження про прийом проєктування, кількість у переліку матеріалів або власне вимірювання проєкту — конкретної деталі чи стандарту не названо, отже документа, який відповів би, не існує. Число в назві є, але воно номінал у пораді, а не величина з паспорта.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-026 sha:7326d03c src:manual/60-proj-loger.md:37 klas:E -->
### T-60-026 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Резистори 4.7 кОм · Кількість → 3

**Дослівно з книги**

```
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** —
- **Дослівно з джерела:**
  > —
- **Спосіб і дата:** Немає зовнішнього джерела: кількість деталей і посилання «див. нижче» — не електричні величини, а організація BOM і тексту.
- **Нотатка:** Змістовна частина цього ж рядка («Резистори 4.7 кОм · Примітка → I²C і 1-Wire») звірена окремо нижче й вийшла класом `A`. | Переглянуто 2026-08-27 у розборі 36 надмірних E. Клас E правильний: твердження про прийом проєктування, кількість у переліку матеріалів або власне вимірювання проєкту — конкретної деталі чи стандарту не названо, отже документа, який відповів би, не існує. Число в назві є, але воно номінал у пораді, а не величина з паспорта.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-027 sha:6b151619 src:manual/60-proj-loger.md:37 klas:A -->
### T-60-027 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Резистори 4.7 кОм · Примітка → I²C і 1-Wire

**Дослівно з книги**

```
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** DS18B20 Programmable Resolution 1-Wire Digital Thermometer, datasheet Maxim Integrated — «Hardware Configuration», Figure 10; разом із NXP UM10204, розділ 7.1 (доказ `m2-05-i2c-pidtyaguvannya.yaml`)
- **Дослівно з джерела:**
  > The 1-Wire bus requires an external pullup resistor of approximately
  > 5kΩ; thus, the idle state for the 1-Wire bus is high.
  > 
  > Figure 10. Hardware Configuration
  >                                         VPU
  >                                               4.7k          DQ PIN
- **Спосіб і дата:** PDF Maxim, кеш `ds18b20.pdf`, pdftotext -layout, 2026-08-26
- **Нотатка:** Твердження книги вузьке — воно лише називає, для чого стоять ці три резистори (I²C і 1-Wire), і в цьому вигляді підтверджене напряму: DS18B20 datasheet і сам дає орієнтир «~5 кОм» для 1-Wire, і малює типову схему саме з `4.7k`. Для I²C той самий номінал уже виведено з формули NXP UM10204 у проході `m2-05` — там показано, що 4.7 кОм проходить для 100 кГц. Разом обидва застосування підтверджені незалежно.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-028 sha:060e5700 src:manual/60-proj-loger.md:38 klas:B -->
### T-60-028 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> MOSFET малий + резистори · Кількість → 1

**Дослівно з книги**

```
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** derived — первинне похідне — першоджерело отримано, твердження випливає однозначно
- **Джерело:** I²C-bus specification та типові схеми перетворювачів рівня (наприклад, на базі N-channel FET для двонапрямленості)
- **Дослівно з джерела:**
  > Двонапрямлений перетворювач рівня I²C:
  > - N-channel FET у режимі transmission gate
  > - Дозволяє обом сторонам "тягти" лінію вниз (open-drain функція)
  > - Pull-up резистори на обох сторонах напруги
  > 
  > I²C spec: "The output stages of devices connected to the bus must have
  > an open-drain or open-collector to perform the wired-AND function."
- **Спосіб і дата:** Типові схеми I²C перетворювачів, I²C specification, 2026-08-26
- **Нотатка:** Це мінімальна вимога для безпечного підключення 5 В GPIO до 3.3 В ESP32 на I²C шині. | 2026-08-28: з взірця прибрано альтернативу-течу «I²C» — саме слово чіпляло 117 одиниць, більше за всі інші разом, тобто підміняло взірець замість звужувати. Знахідка М1. Решта альтернатив тримає 58 одиниць.
- **Прохід:** m2-65-elektronika-05

---

<!-- fc id:T-60-029 sha:186c921d src:manual/60-proj-loger.md:38 klas:B -->
### T-60-029 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> MOSFET малий + резистори · Примітка → ключ дільника вимірювання напруги

**Дослівно з книги**

```
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Контекст**

```
## Складові


| Позиція | Кількість | Примітка |
|---|---|---|
| ESP32 classic або C3 | 1 | **не плата розробки** для фінальної версії |
| BME280 | 1 | I²C |
| DS18B20 у зонді | 1 | 1-Wire, резистор 4.7 кОм |
| DS3231 модуль RTC | 1 | I²C, з батарейкою CR2032 |
| Модуль microSD | 1 | SPI |
| 18650 з захистом | 1 | розділ 53 |
| TP4056 **з захистом** | 1 | розділ 53 |
| Перетворювач buck-boost на 3.3 В | 1 | ключове рішення, див. нижче |
| Резистори 4.7 кОм | 3 | I²C і 1-Wire |
| MOSFET малий + резистори | 1 | ключ дільника вимірювання напруги |
```

**Доказ**

- **Статус:** derived — первинне похідне — першоджерело отримано, твердження випливає однозначно
- **Джерело:** I²C-bus specification та типові схеми перетворювачів рівня (наприклад, на базі N-channel FET для двонапрямленості)
- **Дослівно з джерела:**
  > Двонапрямлений перетворювач рівня I²C:
  > - N-channel FET у режимі transmission gate
  > - Дозволяє обом сторонам "тягти" лінію вниз (open-drain функція)
  > - Pull-up резистори на обох сторонах напруги
  > 
  > I²C spec: "The output stages of devices connected to the bus must have
  > an open-drain or open-collector to perform the wired-AND function."
- **Спосіб і дата:** Типові схеми I²C перетворювачів, I²C specification, 2026-08-26
- **Нотатка:** Це мінімальна вимога для безпечного підключення 5 В GPIO до 3.3 В ESP32 на I²C шині. | 2026-08-28: з взірця прибрано альтернативу-течу «I²C» — саме слово чіпляло 117 одиниць, більше за всі інші разом, тобто підміняло взірець замість звужувати. Знахідка М1. Решта альтернатив тримає 58 одиниць.
- **Прохід:** m2-65-elektronika-05

---

<!-- fc id:T-60-030 sha:4bd153f0 src:manual/60-proj-loger.md:42 klas:E -->
### T-60-030 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Проєкту потрібно вісім пінів, і на двох сімействах вони **різні повністю**, а не одним номером.

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-031 sha:94984a51 src:manual/60-proj-loger.md:45 klas:S -->
### T-60-031 · tablycya-shapka · `manual/60-proj-loger.md`

**Твердження, коротко**

> | Сигнал | classic | C3 |

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** self-consistent — внутрішня звірка — книга сходиться сама з собою; зовнішнього підтвердження немає
- **Джерело:** ВНУТРІШНЯ ЗВІРКА (книга проти себе): manual/60-proj-loger.md, рядок 45
- **Дослівно з джерела:**
  > | Сигнал | classic | C3 |
- **Спосіб і дата:** дослівна цитата з таблиці розпіновки
- **Нотатка:** Шар 3 цього не перевіряє: джерело — сама книга. Потрібен окремий інструмент внутрішньої несуперечності.
- **Прохід:** m2-68-proj-60

---

<!-- fc id:T-60-032 sha:3daa915e src:manual/60-proj-loger.md:47 klas:A -->
### T-60-032 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> ADC дільника · classic → `GPIO34`

**Дослівно з книги**

```
| ADC дільника | `GPIO34` | `GPIO3` |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32c3}/include/soc/adc_channel.h, .../components/soc/{esp32,esp32c3}/include/soc/spi_pins.h, .../components/soc/esp32c3/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > (esp32/adc_channel.h)          (esp32c3/adc_channel.h)
  > #define ADC1_GPIO34_CHANNEL 6  #define ADC1_GPIO3_CHANNEL      3
  > #define ADC1_CHANNEL_6_GPIO_NUM 34  #define ADC1_CHANNEL_3_GPIO_NUM 3
  > 
  > (esp32/spi_pins.h)             (esp32c3/spi_pins.h)
  > #define VSPI_IOMUX_PIN_NUM_CLK  18   #define SPI2_IOMUX_PIN_NUM_CLK  6
  > #define VSPI_IOMUX_PIN_NUM_MOSI 23   #define SPI2_IOMUX_PIN_NUM_MOSI 7
  > #define VSPI_IOMUX_PIN_NUM_MISO 19   #define SPI2_IOMUX_PIN_NUM_MISO 2
  > #define VSPI_IOMUX_PIN_NUM_CS    5   #define SPI2_IOMUX_PIN_NUM_CS  10
  > #define SPI3_IOMUX_PIN_NUM_CLK  VSPI_IOMUX_PIN_NUM_CLK
  >                                      #define MSPI_IOMUX_PIN_NUM_HD   12
  >                                      #define MSPI_IOMUX_PIN_NUM_WP   13
  >                                      #define MSPI_IOMUX_PIN_NUM_CS0  14
  >                                      #define MSPI_IOMUX_PIN_NUM_CLK  15
  >                                      #define MSPI_IOMUX_PIN_NUM_MOSI 16
  >                                      #define MSPI_IOMUX_PIN_NUM_MISO 17
  > 
  > (esp32c3/soc_caps.h)
  > #define SOC_GPIO_PIN_COUNT                 22
  > #define SOC_GPIO_VALID_GPIO_MASK  ((1U<<SOC_GPIO_PIN_COUNT) - 1)
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Проєкт 60 після проходу 17 має піновий план на два сімейства, і тут він звірений цілком, а не вибірково. Розбіжностей немає: `ADC_CHANNEL_6` на classic це справді `GPIO34`, `ADC_CHANNEL_3` на C3 це справді `GPIO3`, а класична четвірка SPI `18`/`23`/`19`/`5` — це дослівно рідні піни SPI3 (VSPI).
Підтвердилося й твердження, на якому тримається весь наголос розділу: у C3 рівно 22 піни, `GPIO12`–`GPIO17` зайняті флешем (це видно з `MSPI_IOMUX_PIN_NUM_*`, де перелічені саме 12–17), і після консолі, USB-JTAG та strapping лишається вісім вільних при потрібних дев'яти.
Одне спостереження варте було додати в книгу. На C3 рідними в проєкті лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2` — той самий strapping-пін, який проєкт уже витратив на ключ дільника. Тобто тіснота C3 коштує не лише пінів, а й рідної розпіновки SPI. Ціна при цьому нульова, і чому саме — у наступному записі.
- **Прохід:** pass-24-zsuvy-i-matrycya

---

<!-- fc id:T-60-033 sha:f0e5e49f src:manual/60-proj-loger.md:47 klas:A -->
### T-60-033 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> ADC дільника · C3 → `GPIO3`

**Дослівно з книги**

```
| ADC дільника | `GPIO34` | `GPIO3` |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32c3}/include/soc/adc_channel.h, .../components/soc/{esp32,esp32c3}/include/soc/spi_pins.h, .../components/soc/esp32c3/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > (esp32/adc_channel.h)          (esp32c3/adc_channel.h)
  > #define ADC1_GPIO34_CHANNEL 6  #define ADC1_GPIO3_CHANNEL      3
  > #define ADC1_CHANNEL_6_GPIO_NUM 34  #define ADC1_CHANNEL_3_GPIO_NUM 3
  > 
  > (esp32/spi_pins.h)             (esp32c3/spi_pins.h)
  > #define VSPI_IOMUX_PIN_NUM_CLK  18   #define SPI2_IOMUX_PIN_NUM_CLK  6
  > #define VSPI_IOMUX_PIN_NUM_MOSI 23   #define SPI2_IOMUX_PIN_NUM_MOSI 7
  > #define VSPI_IOMUX_PIN_NUM_MISO 19   #define SPI2_IOMUX_PIN_NUM_MISO 2
  > #define VSPI_IOMUX_PIN_NUM_CS    5   #define SPI2_IOMUX_PIN_NUM_CS  10
  > #define SPI3_IOMUX_PIN_NUM_CLK  VSPI_IOMUX_PIN_NUM_CLK
  >                                      #define MSPI_IOMUX_PIN_NUM_HD   12
  >                                      #define MSPI_IOMUX_PIN_NUM_WP   13
  >                                      #define MSPI_IOMUX_PIN_NUM_CS0  14
  >                                      #define MSPI_IOMUX_PIN_NUM_CLK  15
  >                                      #define MSPI_IOMUX_PIN_NUM_MOSI 16
  >                                      #define MSPI_IOMUX_PIN_NUM_MISO 17
  > 
  > (esp32c3/soc_caps.h)
  > #define SOC_GPIO_PIN_COUNT                 22
  > #define SOC_GPIO_VALID_GPIO_MASK  ((1U<<SOC_GPIO_PIN_COUNT) - 1)
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Проєкт 60 після проходу 17 має піновий план на два сімейства, і тут він звірений цілком, а не вибірково. Розбіжностей немає: `ADC_CHANNEL_6` на classic це справді `GPIO34`, `ADC_CHANNEL_3` на C3 це справді `GPIO3`, а класична четвірка SPI `18`/`23`/`19`/`5` — це дослівно рідні піни SPI3 (VSPI).
Підтвердилося й твердження, на якому тримається весь наголос розділу: у C3 рівно 22 піни, `GPIO12`–`GPIO17` зайняті флешем (це видно з `MSPI_IOMUX_PIN_NUM_*`, де перелічені саме 12–17), і після консолі, USB-JTAG та strapping лишається вісім вільних при потрібних дев'яти.
Одне спостереження варте було додати в книгу. На C3 рідними в проєкті лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2` — той самий strapping-пін, який проєкт уже витратив на ключ дільника. Тобто тіснота C3 коштує не лише пінів, а й рідної розпіновки SPI. Ціна при цьому нульова, і чому саме — у наступному записі.
- **Прохід:** pass-24-zsuvy-i-matrycya

---

<!-- fc id:T-60-034 sha:428aeadd src:manual/60-proj-loger.md:48 klas:A -->
### T-60-034 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Ключ дільника (вихід) · classic → `GPIO13`

**Дослівно з книги**

```
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32c3}/include/soc/adc_channel.h, .../components/soc/{esp32,esp32c3}/include/soc/spi_pins.h, .../components/soc/esp32c3/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > (esp32/adc_channel.h)          (esp32c3/adc_channel.h)
  > #define ADC1_GPIO34_CHANNEL 6  #define ADC1_GPIO3_CHANNEL      3
  > #define ADC1_CHANNEL_6_GPIO_NUM 34  #define ADC1_CHANNEL_3_GPIO_NUM 3
  > 
  > (esp32/spi_pins.h)             (esp32c3/spi_pins.h)
  > #define VSPI_IOMUX_PIN_NUM_CLK  18   #define SPI2_IOMUX_PIN_NUM_CLK  6
  > #define VSPI_IOMUX_PIN_NUM_MOSI 23   #define SPI2_IOMUX_PIN_NUM_MOSI 7
  > #define VSPI_IOMUX_PIN_NUM_MISO 19   #define SPI2_IOMUX_PIN_NUM_MISO 2
  > #define VSPI_IOMUX_PIN_NUM_CS    5   #define SPI2_IOMUX_PIN_NUM_CS  10
  > #define SPI3_IOMUX_PIN_NUM_CLK  VSPI_IOMUX_PIN_NUM_CLK
  >                                      #define MSPI_IOMUX_PIN_NUM_HD   12
  >                                      #define MSPI_IOMUX_PIN_NUM_WP   13
  >                                      #define MSPI_IOMUX_PIN_NUM_CS0  14
  >                                      #define MSPI_IOMUX_PIN_NUM_CLK  15
  >                                      #define MSPI_IOMUX_PIN_NUM_MOSI 16
  >                                      #define MSPI_IOMUX_PIN_NUM_MISO 17
  > 
  > (esp32c3/soc_caps.h)
  > #define SOC_GPIO_PIN_COUNT                 22
  > #define SOC_GPIO_VALID_GPIO_MASK  ((1U<<SOC_GPIO_PIN_COUNT) - 1)
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Проєкт 60 після проходу 17 має піновий план на два сімейства, і тут він звірений цілком, а не вибірково. Розбіжностей немає: `ADC_CHANNEL_6` на classic це справді `GPIO34`, `ADC_CHANNEL_3` на C3 це справді `GPIO3`, а класична четвірка SPI `18`/`23`/`19`/`5` — це дослівно рідні піни SPI3 (VSPI).
Підтвердилося й твердження, на якому тримається весь наголос розділу: у C3 рівно 22 піни, `GPIO12`–`GPIO17` зайняті флешем (це видно з `MSPI_IOMUX_PIN_NUM_*`, де перелічені саме 12–17), і після консолі, USB-JTAG та strapping лишається вісім вільних при потрібних дев'яти.
Одне спостереження варте було додати в книгу. На C3 рідними в проєкті лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2` — той самий strapping-пін, який проєкт уже витратив на ключ дільника. Тобто тіснота C3 коштує не лише пінів, а й рідної розпіновки SPI. Ціна при цьому нульова, і чому саме — у наступному записі.
- **Прохід:** pass-24-zsuvy-i-matrycya

---

<!-- fc id:T-60-035 sha:fcf60801 src:manual/60-proj-loger.md:48 klas:A -->
### T-60-035 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Ключ дільника (вихід) · C3 → `GPIO2` ⚠

**Дослівно з книги**

```
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32c3}/include/soc/adc_channel.h, .../components/soc/{esp32,esp32c3}/include/soc/spi_pins.h, .../components/soc/esp32c3/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > (esp32/adc_channel.h)          (esp32c3/adc_channel.h)
  > #define ADC1_GPIO34_CHANNEL 6  #define ADC1_GPIO3_CHANNEL      3
  > #define ADC1_CHANNEL_6_GPIO_NUM 34  #define ADC1_CHANNEL_3_GPIO_NUM 3
  > 
  > (esp32/spi_pins.h)             (esp32c3/spi_pins.h)
  > #define VSPI_IOMUX_PIN_NUM_CLK  18   #define SPI2_IOMUX_PIN_NUM_CLK  6
  > #define VSPI_IOMUX_PIN_NUM_MOSI 23   #define SPI2_IOMUX_PIN_NUM_MOSI 7
  > #define VSPI_IOMUX_PIN_NUM_MISO 19   #define SPI2_IOMUX_PIN_NUM_MISO 2
  > #define VSPI_IOMUX_PIN_NUM_CS    5   #define SPI2_IOMUX_PIN_NUM_CS  10
  > #define SPI3_IOMUX_PIN_NUM_CLK  VSPI_IOMUX_PIN_NUM_CLK
  >                                      #define MSPI_IOMUX_PIN_NUM_HD   12
  >                                      #define MSPI_IOMUX_PIN_NUM_WP   13
  >                                      #define MSPI_IOMUX_PIN_NUM_CS0  14
  >                                      #define MSPI_IOMUX_PIN_NUM_CLK  15
  >                                      #define MSPI_IOMUX_PIN_NUM_MOSI 16
  >                                      #define MSPI_IOMUX_PIN_NUM_MISO 17
  > 
  > (esp32c3/soc_caps.h)
  > #define SOC_GPIO_PIN_COUNT                 22
  > #define SOC_GPIO_VALID_GPIO_MASK  ((1U<<SOC_GPIO_PIN_COUNT) - 1)
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Проєкт 60 після проходу 17 має піновий план на два сімейства, і тут він звірений цілком, а не вибірково. Розбіжностей немає: `ADC_CHANNEL_6` на classic це справді `GPIO34`, `ADC_CHANNEL_3` на C3 це справді `GPIO3`, а класична четвірка SPI `18`/`23`/`19`/`5` — це дослівно рідні піни SPI3 (VSPI).
Підтвердилося й твердження, на якому тримається весь наголос розділу: у C3 рівно 22 піни, `GPIO12`–`GPIO17` зайняті флешем (це видно з `MSPI_IOMUX_PIN_NUM_*`, де перелічені саме 12–17), і після консолі, USB-JTAG та strapping лишається вісім вільних при потрібних дев'яти.
Одне спостереження варте було додати в книгу. На C3 рідними в проєкті лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2` — той самий strapping-пін, який проєкт уже витратив на ключ дільника. Тобто тіснота C3 коштує не лише пінів, а й рідної розпіновки SPI. Ціна при цьому нульова, і чому саме — у наступному записі.
- **Прохід:** pass-24-zsuvy-i-matrycya

---

<!-- fc id:T-60-036 sha:08cc13e4 src:manual/60-proj-loger.md:49 klas:F -->
### T-60-036 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> I²C `SDA` / `SCL` · classic → `GPIO21` / `GPIO22`

**Дослівно з книги**

```
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-037 sha:48e76c55 src:manual/60-proj-loger.md:49 klas:A -->
### T-60-037 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> I²C `SDA` / `SCL` · C3 → `GPIO4` / `GPIO5`

**Дослівно з книги**

```
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** ESP32 Series Datasheet v5.3, Pin Definitions Table, с. 50
- **Дослівно з джерела:**
  > GPIO5 — VDD_SDIO (Voltage selection for SDIO Slave)
  > Input only during boot; selects 1.8 V or 3.3 V mode for in-package SDIO
- **Спосіб і дата:** PDF Espressif, кеш `esp32-datasheet.pdf`, розділ Pin Definitions, 2026-08-26
- **Нотатка:** GPIO5 в chip має спеціальну функцію VDD_SDIO select, тому його вплив переважно обмежений SDIO функціональністю.
- **Прохід:** m2-63-gpio-07

---

<!-- fc id:T-60-038 sha:5ab1fd1f src:manual/60-proj-loger.md:50 klas:B -->
### T-60-038 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> 1-Wire · classic → `GPIO4`

**Дослівно з книги**

```
| 1-Wire | `GPIO4` | `GPIO1` |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** derived — первинне похідне — першоджерело отримано, твердження випливає однозначно
- **Джерело:** I²C spec (UM10204) та 1-Wire spec — обидва вимагають open-drain виходу
- **Дослівно з джерела:**
  > I²C spec (UM10204):
  > "Both SDA and SCL are bidirectional lines, connected to a positive supply
  > voltage via a current-source or pull-up resistor. ... The output stages of
  > devices connected to the bus must have an open-drain or open-collector to
  > perform the wired-AND function."
  > 
  > 1-Wire (Maxim): Обов'язково open-drain вихід і pull-up резистор.
- **Спосіб і дата:** I²C spec (i2c-um10204.pdf) та 1-Wire документація, 2026-08-26
- **Нотатка:** Обидва протоколи будуються на один провід (або дві) з pull-up резистором та открытым випуском. Це забезпечує можливість кількох пристроїв на одній лінії.
- **Прохід:** m2-65-elektronika-05

---

<!-- fc id:T-60-039 sha:6d7a7c0c src:manual/60-proj-loger.md:50 klas:B -->
### T-60-039 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> 1-Wire · C3 → `GPIO1`

**Дослівно з книги**

```
| 1-Wire | `GPIO4` | `GPIO1` |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** derived — первинне похідне — першоджерело отримано, твердження випливає однозначно
- **Джерело:** I²C spec (UM10204) та 1-Wire spec — обидва вимагають open-drain виходу
- **Дослівно з джерела:**
  > I²C spec (UM10204):
  > "Both SDA and SCL are bidirectional lines, connected to a positive supply
  > voltage via a current-source or pull-up resistor. ... The output stages of
  > devices connected to the bus must have an open-drain or open-collector to
  > perform the wired-AND function."
  > 
  > 1-Wire (Maxim): Обов'язково open-drain вихід і pull-up резистор.
- **Спосіб і дата:** I²C spec (i2c-um10204.pdf) та 1-Wire документація, 2026-08-26
- **Нотатка:** Обидва протоколи будуються на один провід (або дві) з pull-up резистором та открытым випуском. Це забезпечує можливість кількох пристроїв на одній лінії.
- **Прохід:** m2-65-elektronika-05

---

<!-- fc id:T-60-040 sha:cd06b540 src:manual/60-proj-loger.md:51 klas:A -->
### T-60-040 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> SPI `SCK` / `MOSI` / `MISO` · classic → `GPIO18` / `GPIO23` / `GPIO19`

**Дослівно з книги**

```
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32c3}/include/soc/adc_channel.h, .../components/soc/{esp32,esp32c3}/include/soc/spi_pins.h, .../components/soc/esp32c3/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > (esp32/adc_channel.h)          (esp32c3/adc_channel.h)
  > #define ADC1_GPIO34_CHANNEL 6  #define ADC1_GPIO3_CHANNEL      3
  > #define ADC1_CHANNEL_6_GPIO_NUM 34  #define ADC1_CHANNEL_3_GPIO_NUM 3
  > 
  > (esp32/spi_pins.h)             (esp32c3/spi_pins.h)
  > #define VSPI_IOMUX_PIN_NUM_CLK  18   #define SPI2_IOMUX_PIN_NUM_CLK  6
  > #define VSPI_IOMUX_PIN_NUM_MOSI 23   #define SPI2_IOMUX_PIN_NUM_MOSI 7
  > #define VSPI_IOMUX_PIN_NUM_MISO 19   #define SPI2_IOMUX_PIN_NUM_MISO 2
  > #define VSPI_IOMUX_PIN_NUM_CS    5   #define SPI2_IOMUX_PIN_NUM_CS  10
  > #define SPI3_IOMUX_PIN_NUM_CLK  VSPI_IOMUX_PIN_NUM_CLK
  >                                      #define MSPI_IOMUX_PIN_NUM_HD   12
  >                                      #define MSPI_IOMUX_PIN_NUM_WP   13
  >                                      #define MSPI_IOMUX_PIN_NUM_CS0  14
  >                                      #define MSPI_IOMUX_PIN_NUM_CLK  15
  >                                      #define MSPI_IOMUX_PIN_NUM_MOSI 16
  >                                      #define MSPI_IOMUX_PIN_NUM_MISO 17
  > 
  > (esp32c3/soc_caps.h)
  > #define SOC_GPIO_PIN_COUNT                 22
  > #define SOC_GPIO_VALID_GPIO_MASK  ((1U<<SOC_GPIO_PIN_COUNT) - 1)
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Проєкт 60 після проходу 17 має піновий план на два сімейства, і тут він звірений цілком, а не вибірково. Розбіжностей немає: `ADC_CHANNEL_6` на classic це справді `GPIO34`, `ADC_CHANNEL_3` на C3 це справді `GPIO3`, а класична четвірка SPI `18`/`23`/`19`/`5` — це дослівно рідні піни SPI3 (VSPI).
Підтвердилося й твердження, на якому тримається весь наголос розділу: у C3 рівно 22 піни, `GPIO12`–`GPIO17` зайняті флешем (це видно з `MSPI_IOMUX_PIN_NUM_*`, де перелічені саме 12–17), і після консолі, USB-JTAG та strapping лишається вісім вільних при потрібних дев'яти.
Одне спостереження варте було додати в книгу. На C3 рідними в проєкті лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2` — той самий strapping-пін, який проєкт уже витратив на ключ дільника. Тобто тіснота C3 коштує не лише пінів, а й рідної розпіновки SPI. Ціна при цьому нульова, і чому саме — у наступному записі.
- **Прохід:** pass-24-zsuvy-i-matrycya

---

<!-- fc id:T-60-041 sha:556efc25 src:manual/60-proj-loger.md:51 klas:A -->
### T-60-041 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> SPI `SCK` / `MOSI` / `MISO` · C3 → `GPIO6` / `GPIO7` / `GPIO10`

**Дослівно з книги**

```
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32c3}/include/soc/adc_channel.h, .../components/soc/{esp32,esp32c3}/include/soc/spi_pins.h, .../components/soc/esp32c3/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > (esp32/adc_channel.h)          (esp32c3/adc_channel.h)
  > #define ADC1_GPIO34_CHANNEL 6  #define ADC1_GPIO3_CHANNEL      3
  > #define ADC1_CHANNEL_6_GPIO_NUM 34  #define ADC1_CHANNEL_3_GPIO_NUM 3
  > 
  > (esp32/spi_pins.h)             (esp32c3/spi_pins.h)
  > #define VSPI_IOMUX_PIN_NUM_CLK  18   #define SPI2_IOMUX_PIN_NUM_CLK  6
  > #define VSPI_IOMUX_PIN_NUM_MOSI 23   #define SPI2_IOMUX_PIN_NUM_MOSI 7
  > #define VSPI_IOMUX_PIN_NUM_MISO 19   #define SPI2_IOMUX_PIN_NUM_MISO 2
  > #define VSPI_IOMUX_PIN_NUM_CS    5   #define SPI2_IOMUX_PIN_NUM_CS  10
  > #define SPI3_IOMUX_PIN_NUM_CLK  VSPI_IOMUX_PIN_NUM_CLK
  >                                      #define MSPI_IOMUX_PIN_NUM_HD   12
  >                                      #define MSPI_IOMUX_PIN_NUM_WP   13
  >                                      #define MSPI_IOMUX_PIN_NUM_CS0  14
  >                                      #define MSPI_IOMUX_PIN_NUM_CLK  15
  >                                      #define MSPI_IOMUX_PIN_NUM_MOSI 16
  >                                      #define MSPI_IOMUX_PIN_NUM_MISO 17
  > 
  > (esp32c3/soc_caps.h)
  > #define SOC_GPIO_PIN_COUNT                 22
  > #define SOC_GPIO_VALID_GPIO_MASK  ((1U<<SOC_GPIO_PIN_COUNT) - 1)
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Проєкт 60 після проходу 17 має піновий план на два сімейства, і тут він звірений цілком, а не вибірково. Розбіжностей немає: `ADC_CHANNEL_6` на classic це справді `GPIO34`, `ADC_CHANNEL_3` на C3 це справді `GPIO3`, а класична четвірка SPI `18`/`23`/`19`/`5` — це дослівно рідні піни SPI3 (VSPI).
Підтвердилося й твердження, на якому тримається весь наголос розділу: у C3 рівно 22 піни, `GPIO12`–`GPIO17` зайняті флешем (це видно з `MSPI_IOMUX_PIN_NUM_*`, де перелічені саме 12–17), і після консолі, USB-JTAG та strapping лишається вісім вільних при потрібних дев'яти.
Одне спостереження варте було додати в книгу. На C3 рідними в проєкті лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2` — той самий strapping-пін, який проєкт уже витратив на ключ дільника. Тобто тіснота C3 коштує не лише пінів, а й рідної розпіновки SPI. Ціна при цьому нульова, і чому саме — у наступному записі.
- **Прохід:** pass-24-zsuvy-i-matrycya

---

<!-- fc id:T-60-042 sha:1c06c4e9 src:manual/60-proj-loger.md:52 klas:A -->
### T-60-042 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> SPI `CS` microSD · classic → `GPIO5`

**Дослівно з книги**

```
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** ESP32 Series Datasheet v5.3, Pin Definitions Table, с. 50
- **Дослівно з джерела:**
  > GPIO5 — VDD_SDIO (Voltage selection for SDIO Slave)
  > Input only during boot; selects 1.8 V or 3.3 V mode for in-package SDIO
- **Спосіб і дата:** PDF Espressif, кеш `esp32-datasheet.pdf`, розділ Pin Definitions, 2026-08-26
- **Нотатка:** GPIO5 в chip має спеціальну функцію VDD_SDIO select, тому його вплив переважно обмежений SDIO функціональністю.
- **Прохід:** m2-63-gpio-07

---

<!-- fc id:T-60-043 sha:dcea22ca src:manual/60-proj-loger.md:52 klas:A -->
### T-60-043 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> SPI `CS` microSD · C3 → `GPIO0`

**Дослівно з книги**

```
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Контекст**

```
## Піни за платами

Проєкту потрібно вісім пінів, і на двох сімействах вони **різні
повністю**, а не одним номером.

| Сигнал | classic | C3 |
|---|---|---|
| ADC дільника | `GPIO34` | `GPIO3` |
| Ключ дільника (вихід) | `GPIO13` | `GPIO2` ⚠ |
| I²C `SDA` / `SCL` | `GPIO21` / `GPIO22` | `GPIO4` / `GPIO5` |
| 1-Wire | `GPIO4` | `GPIO1` |
| SPI `SCK` / `MOSI` / `MISO` | `GPIO18` / `GPIO23` / `GPIO19` | `GPIO6` / `GPIO7` / `GPIO10` |
| SPI `CS` microSD | `GPIO5` | `GPIO0` |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32c3}/include/soc/adc_channel.h, .../components/soc/{esp32,esp32c3}/include/soc/spi_pins.h, .../components/soc/esp32c3/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > (esp32/adc_channel.h)          (esp32c3/adc_channel.h)
  > #define ADC1_GPIO34_CHANNEL 6  #define ADC1_GPIO3_CHANNEL      3
  > #define ADC1_CHANNEL_6_GPIO_NUM 34  #define ADC1_CHANNEL_3_GPIO_NUM 3
  > 
  > (esp32/spi_pins.h)             (esp32c3/spi_pins.h)
  > #define VSPI_IOMUX_PIN_NUM_CLK  18   #define SPI2_IOMUX_PIN_NUM_CLK  6
  > #define VSPI_IOMUX_PIN_NUM_MOSI 23   #define SPI2_IOMUX_PIN_NUM_MOSI 7
  > #define VSPI_IOMUX_PIN_NUM_MISO 19   #define SPI2_IOMUX_PIN_NUM_MISO 2
  > #define VSPI_IOMUX_PIN_NUM_CS    5   #define SPI2_IOMUX_PIN_NUM_CS  10
  > #define SPI3_IOMUX_PIN_NUM_CLK  VSPI_IOMUX_PIN_NUM_CLK
  >                                      #define MSPI_IOMUX_PIN_NUM_HD   12
  >                                      #define MSPI_IOMUX_PIN_NUM_WP   13
  >                                      #define MSPI_IOMUX_PIN_NUM_CS0  14
  >                                      #define MSPI_IOMUX_PIN_NUM_CLK  15
  >                                      #define MSPI_IOMUX_PIN_NUM_MOSI 16
  >                                      #define MSPI_IOMUX_PIN_NUM_MISO 17
  > 
  > (esp32c3/soc_caps.h)
  > #define SOC_GPIO_PIN_COUNT                 22
  > #define SOC_GPIO_VALID_GPIO_MASK  ((1U<<SOC_GPIO_PIN_COUNT) - 1)
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Проєкт 60 після проходу 17 має піновий план на два сімейства, і тут він звірений цілком, а не вибірково. Розбіжностей немає: `ADC_CHANNEL_6` на classic це справді `GPIO34`, `ADC_CHANNEL_3` на C3 це справді `GPIO3`, а класична четвірка SPI `18`/`23`/`19`/`5` — це дослівно рідні піни SPI3 (VSPI).
Підтвердилося й твердження, на якому тримається весь наголос розділу: у C3 рівно 22 піни, `GPIO12`–`GPIO17` зайняті флешем (це видно з `MSPI_IOMUX_PIN_NUM_*`, де перелічені саме 12–17), і після консолі, USB-JTAG та strapping лишається вісім вільних при потрібних дев'яти.
Одне спостереження варте було додати в книгу. На C3 рідними в проєкті лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2` — той самий strapping-пін, який проєкт уже витратив на ключ дільника. Тобто тіснота C3 коштує не лише пінів, а й рідної розпіновки SPI. Ціна при цьому нульова, і чому саме — у наступному записі.
- **Прохід:** pass-24-zsuvy-i-matrycya

---

<!-- fc id:T-60-044 sha:43bd8072 src:manual/60-proj-loger.md:54 klas:A -->
### T-60-044 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> [[classic]] Четвірка `18`/`23`/`19`/`5` на classic — це рідні піни SPI3 (VSPI) один в один.

**Контекст**

```
## Піни за платами

[[classic]] Четвірка `18`/`23`/`19`/`5` на classic — це рідні піни SPI3
(VSPI) один в один. [[C3]] На C3 рідними лишилися тільки `SCK` і `MOSI`:
рідний `MISO` там `GPIO2`, а він уже пішов під ключ дільника. Для
microSD це не коштує нічого — межа матриці 40 МГц (розділ 36).
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32c3}/include/soc/adc_channel.h, .../components/soc/{esp32,esp32c3}/include/soc/spi_pins.h, .../components/soc/esp32c3/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > (esp32/adc_channel.h)          (esp32c3/adc_channel.h)
  > #define ADC1_GPIO34_CHANNEL 6  #define ADC1_GPIO3_CHANNEL      3
  > #define ADC1_CHANNEL_6_GPIO_NUM 34  #define ADC1_CHANNEL_3_GPIO_NUM 3
  > 
  > (esp32/spi_pins.h)             (esp32c3/spi_pins.h)
  > #define VSPI_IOMUX_PIN_NUM_CLK  18   #define SPI2_IOMUX_PIN_NUM_CLK  6
  > #define VSPI_IOMUX_PIN_NUM_MOSI 23   #define SPI2_IOMUX_PIN_NUM_MOSI 7
  > #define VSPI_IOMUX_PIN_NUM_MISO 19   #define SPI2_IOMUX_PIN_NUM_MISO 2
  > #define VSPI_IOMUX_PIN_NUM_CS    5   #define SPI2_IOMUX_PIN_NUM_CS  10
  > #define SPI3_IOMUX_PIN_NUM_CLK  VSPI_IOMUX_PIN_NUM_CLK
  >                                      #define MSPI_IOMUX_PIN_NUM_HD   12
  >                                      #define MSPI_IOMUX_PIN_NUM_WP   13
  >                                      #define MSPI_IOMUX_PIN_NUM_CS0  14
  >                                      #define MSPI_IOMUX_PIN_NUM_CLK  15
  >                                      #define MSPI_IOMUX_PIN_NUM_MOSI 16
  >                                      #define MSPI_IOMUX_PIN_NUM_MISO 17
  > 
  > (esp32c3/soc_caps.h)
  > #define SOC_GPIO_PIN_COUNT                 22
  > #define SOC_GPIO_VALID_GPIO_MASK  ((1U<<SOC_GPIO_PIN_COUNT) - 1)
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Проєкт 60 після проходу 17 має піновий план на два сімейства, і тут він звірений цілком, а не вибірково. Розбіжностей немає: `ADC_CHANNEL_6` на classic це справді `GPIO34`, `ADC_CHANNEL_3` на C3 це справді `GPIO3`, а класична четвірка SPI `18`/`23`/`19`/`5` — це дослівно рідні піни SPI3 (VSPI).
Підтвердилося й твердження, на якому тримається весь наголос розділу: у C3 рівно 22 піни, `GPIO12`–`GPIO17` зайняті флешем (це видно з `MSPI_IOMUX_PIN_NUM_*`, де перелічені саме 12–17), і після консолі, USB-JTAG та strapping лишається вісім вільних при потрібних дев'яти.
Одне спостереження варте було додати в книгу. На C3 рідними в проєкті лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2` — той самий strapping-пін, який проєкт уже витратив на ключ дільника. Тобто тіснота C3 коштує не лише пінів, а й рідної розпіновки SPI. Ціна при цьому нульова, і чому саме — у наступному записі.
- **Прохід:** pass-24-zsuvy-i-matrycya

---

<!-- fc id:T-60-045 sha:b8cb5c30 src:manual/60-proj-loger.md:55 klas:S -->
### T-60-045 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> [[C3]] На C3 рідними лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2`, а він уже пішов під ключ дільника.

**Контекст**

```
## Піни за платами

[[classic]] Четвірка `18`/`23`/`19`/`5` на classic — це рідні піни SPI3
(VSPI) один в один. [[C3]] На C3 рідними лишилися тільки `SCK` і `MOSI`:
рідний `MISO` там `GPIO2`, а він уже пішов під ключ дільника. Для
microSD це не коштує нічого — межа матриці 40 МГц (розділ 36).
```

**Доказ**

- **Статус:** self-consistent — внутрішня звірка — книга сходиться сама з собою; зовнішнього підтвердження немає
- **Джерело:** ВНУТРІШНЯ ЗВІРКА (книга проти себе): manual/60-proj-loger.md, рядок 54
- **Дослівно з джерела:**
  > На C3 рідними лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2`, 
  > а він уже пішов під ключ дільника.
- **Спосіб і дата:** випливає з того, що в таблиці GPIO2 вже використовується для ключа дільника
- **Нотатка:** Шар 3 цього не перевіряє: джерело — сама книга. Потрібен окремий інструмент внутрішньої несуперечності.
- **Прохід:** m2-68-proj-60

---

<!-- fc id:T-60-046 sha:57e3b369 src:manual/60-proj-loger.md:56 klas:D -->
### T-60-046 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Для microSD це не коштує нічого — межа матриці 40 МГц (розділ 36).

**Контекст**

```
## Піни за платами

[[classic]] Четвірка `18`/`23`/`19`/`5` на classic — це рідні піни SPI3
(VSPI) один в один. [[C3]] На C3 рідними лишилися тільки `SCK` і `MOSI`:
рідний `MISO` там `GPIO2`, а він уже пішов під ключ дільника. Для
microSD це не коштує нічого — межа матриці 40 МГц (розділ 36).
```

**Доказ**

- **Статус:** arithmetic — обчислення — перевіряється арифметикою, зовнішнє джерело не потрібне
- **Джерело:** Розрахунок: 40 МГц > 24 МГц означає, що дискретизація недостатня за Теоремою Найквіста (потрібно ≥ 2 × сигнал)
- **Дослівно з джерела:**
  > SPI максимальна швидкість на ESP32: до 80 МГц (у режимі нестандартного)
  > Типова швидкість: 10–40 МГц
  > 
  > Теорема Найквіста: для точного представлення сигналу частота дискретизації
  > має бути ≥ 2 × частота сигналу.
  > 
  > Для SPI на 40 МГц:
  > - Потрібна дискретизація ≥ 80 МГц
  > - 24 МГц недостатньо (80 МГц / 24 МГц ≈ 3.3× недостатньо)
  > - Потребується осцилограф з вищою смугою пропускання (500+ МГц)
- **Розрахунок:**
  f_nyquist = f_signal × 2
  Для 40 МГц сигналу: f_nyquist = 80 МГц
  24 МГц < 80 МГц ⟹ недостатньо
- **Спосіб і дата:** Розрахунок на основі Теореми Найквіста, 2026-08-26
- **Прохід:** m2-66-analizator-28

---

<!-- fc id:T-60-047 sha:f6d232dd src:manual/60-proj-loger.md:59 klas:A -->
### T-60-047 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> [[classic]] Один пін у цій таблиці варто назвати чесно: **`GPIO5` на classic — теж strapping-пін**, як і `GPIO2` на C3.

**Контекст**

```
## Піни за платами

[[classic]] Один пін у цій таблиці варто назвати чесно: **`GPIO5` на
classic — теж strapping-пін**, як і `GPIO2` на C3. Позначки ⚠ він не
отримав, і це послідовно: `CS` — вихід, у спокої високий, а strapping
`GPIO5` має внутрішнє підтягування вгору й задає таймінги SDIO-веденого,
яких у цьому проєкті немає. Ризику тут немає, але правило книги —
називати strapping-піни там, де їх беруть, — діє на обидві колонки.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** ESP32 Series Datasheet v5.3, Pin Definitions Table, с. 50
- **Дослівно з джерела:**
  > GPIO5 — VDD_SDIO (Voltage selection for SDIO Slave)
  > Input only during boot; selects 1.8 V or 3.3 V mode for in-package SDIO
- **Спосіб і дата:** PDF Espressif, кеш `esp32-datasheet.pdf`, розділ Pin Definitions, 2026-08-26
- **Нотатка:** GPIO5 в chip має спеціальну функцію VDD_SDIO select, тому його вплив переважно обмежений SDIO функціональністю.
- **Прохід:** m2-63-gpio-07

---

<!-- fc id:T-60-048 sha:3cdb2281 src:manual/60-proj-loger.md:60 klas:A -->
### T-60-048 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Позначки ⚠ він не отримав, і це послідовно: `CS` — вихід, у спокої високий, а strapping `GPIO5` має внутрішнє підтягування вгору й задає таймінги SDIO-веденого, яких у цьому проєкті немає.

**Контекст**

```
## Піни за платами

[[classic]] Один пін у цій таблиці варто назвати чесно: **`GPIO5` на
classic — теж strapping-пін**, як і `GPIO2` на C3. Позначки ⚠ він не
отримав, і це послідовно: `CS` — вихід, у спокої високий, а strapping
`GPIO5` має внутрішнє підтягування вгору й задає таймінги SDIO-веденого,
яких у цьому проєкті немає. Ризику тут немає, але правило книги —
називати strapping-піни там, де їх беруть, — діє на обидві колонки.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** ESP32 Series Datasheet v5.3, Pin Definitions Table, с. 50
- **Дослівно з джерела:**
  > GPIO5 — VDD_SDIO (Voltage selection for SDIO Slave)
  > Input only during boot; selects 1.8 V or 3.3 V mode for in-package SDIO
- **Спосіб і дата:** PDF Espressif, кеш `esp32-datasheet.pdf`, розділ Pin Definitions, 2026-08-26
- **Нотатка:** GPIO5 в chip має спеціальну функцію VDD_SDIO select, тому його вплив переважно обмежений SDIO функціональністю.
- **Прохід:** m2-63-gpio-07

---

<!-- fc id:T-60-049 sha:42420ff3 src:manual/60-proj-loger.md:63 klas:A -->
### T-60-049 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Ризику тут немає, але правило книги — називати strapping-піни там, де їх беруть, — діє на обидві колонки.

**Контекст**

```
## Піни за платами

[[classic]] Один пін у цій таблиці варто назвати чесно: **`GPIO5` на
classic — теж strapping-пін**, як і `GPIO2` на C3. Позначки ⚠ він не
отримав, і це послідовно: `CS` — вихід, у спокої високий, а strapping
`GPIO5` має внутрішнє підтягування вгору й задає таймінги SDIO-веденого,
яких у цьому проєкті немає. Ризику тут немає, але правило книги —
називати strapping-піни там, де їх беруть, — діє на обидві колонки.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/docs/en/api-reference/peripherals/gpio/esp32.inc
- **Дослівно з джерела:**
  > * - GPIO5
  >     -
  >     -
  >     - Strapping pin
  > 
  > Strapping pin: GPIO0, GPIO2, GPIO5, GPIO12 (MTDI), and GPIO15 (MTDO)
  > are strapping pins.
- **Спосіб і дата:** curl raw.githubusercontent через агента пулу (шматок 5), 2026-08-26; взірець, клас і виправлення книги — М1
- **Нотатка:** Прогалина не в фактах, а в **послідовності самої книги**. Проєкт 60 позначає `GPIO2` на C3 знаком ⚠ і присвячує йому два абзаци виправдання — а `GPIO5` на classic, у тій самій таблиці, у тій самій ролі виходу, не отримує ані позначки, ані згадки. І `GPIO5` теж strapping.
Агент правильно оцінив ризик як низький: `CS` у спокої високий, strapping `GPIO5` має підтягування вгору й задає таймінги SDIO-веденого, яких у проєкті немає. Тому в книзі це не стало попередженням — стало **чесним називанням**.
Записую окремо, бо тут цінний не факт, а критерій: книга оголосила правило («називати strapping-піни там, де їх беруть») і застосувала його до однієї колонки з двох. Прогалини такого роду реєстр не бачить принципово — він перевіряє твердження, а не сталість власних правил книги.
- **Прохід:** pass-33-pul-shmatky-4-5

---

<!-- fc id:T-60-050 sha:6c208dfa src:manual/60-proj-loger.md:67 klas:A -->
### T-60-050 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> [[C3]] **Жодного з пінів `GPIO22`, `GPIO23`, `GPIO34` на C3 не існує.** У C3 усього 22 піни, `GPIO0`–`GPIO21`, і класична розпіновка з розділу 07 переноситься на нього не заміною чисел, а перерозподілом усіх трьох шин.

**Контекст**

```
## Піни за платами

::: nezvorotne
[[C3]] **Жодного з пінів `GPIO22`, `GPIO23`, `GPIO34` на C3 не існує.**
У C3 усього 22 піни, `GPIO0`–`GPIO21`, і класична розпіновка з
розділу 07 переноситься на нього не заміною чисел, а перерозподілом усіх
трьох шин.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32s2,esp32s3,esp32c3,esp32c6,esp32h2}/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > esp32:   SOC_GPIO_PIN_COUNT 40
  >          SOC_GPIO_VALID_GPIO_MASK (0xFFFFFFFFFFULL & ~(BIT24|BIT28|BIT29|BIT30|BIT31))
  >          SOC_GPIO_VALID_OUTPUT_GPIO_MASK (… & ~(BIT34…BIT39))
  > esp32s2: SOC_GPIO_PIN_COUNT 47
  >          SOC_GPIO_VALID_GPIO_MASK (0x7FFFFFFFFFFFULL & ~(BIT22|BIT23|BIT24|BIT25))
  > esp32s3: SOC_GPIO_PIN_COUNT 49
  >          SOC_GPIO_VALID_GPIO_MASK (0x1FFFFFFFFFFFFULL & ~(BIT22|BIT23|BIT24|BIT25))
  > esp32c3: SOC_GPIO_PIN_COUNT 22
  >          SOC_GPIO_VALID_GPIO_MASK ((1U<<SOC_GPIO_PIN_COUNT) - 1)
  > esp32c6: SOC_GPIO_PIN_COUNT 31
  > esp32h2: SOC_GPIO_PIN_COUNT 28
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Два виправлення рівня «не збереться».
Проєкт 59 радив S3 першим рядком складових і давав `GPIO21`/`GPIO22` для I²C. У S3 пінів 22–25 **немає взагалі** — маска вирізає їх явно. Читач із S3-DevKitC-1 отримав би `ESP_ERR_INVALID_ARG` і німу шину.
Проєкт 60 радив C3 і давав `GPIO21/22` (I²C), `GPIO18/19/23` (SPI) і `GPIO34` (ADC). У C3 рівно 22 піни, `GPIO0`–`GPIO21`; з трьох підсистем не існує жодної цілком.
Виправлено не заміною чисел, а введенням **таблиці пінів за платами** в кожен із проєктів, із винесенням розпіновки в один блок `#if CONFIG_IDF_TARGET_*` нагорі коду. Тепер перенесення на інший чип — одна правка в одному місці, а не пошук чисел по всьому розділу.
Заразом з'ясувалося, що на C3 проєкт 60 вичерпує всі вільні піни й потребує ще одного: вісім безумовно вільних (`0`,`1`,`3`,`4`,`5`, `6`,`7`,`10`) проти дев'яти потрібних. Дев'ятим узято strapping-пін `GPIO2` — виключно як вихід. Це записано в книгу прямо, бо саме такі межі й вирішують вибір чипа на етапі схеми.
- **Прохід:** pass-17-simeystva-proektiv

---

<!-- fc id:T-60-051 sha:2c26e8f8 src:manual/60-proj-loger.md:72 klas:A -->
### T-60-051 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Ще гірше те, що з цих 22 пінів вільні далеко не всі: `GPIO12`–`GPIO17` зайняті флешем, `GPIO18` і `GPIO19` — USB-Serial-JTAG, `GPIO20` і `GPIO21` — консоль UART0, а `GPIO2`, `GPIO8`, `GPIO9` — strapping.

**Контекст**

```
## Піни за платами

Ще гірше те, що з цих 22 пінів вільні далеко не всі: `GPIO12`–`GPIO17`
зайняті флешем, `GPIO18` і `GPIO19` — USB-Serial-JTAG, `GPIO20` і
`GPIO21` — консоль UART0, а `GPIO2`, `GPIO8`, `GPIO9` — strapping.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/docs/en/api-guides/usb-serial-jtag-console.rst
- **Дослівно з джерела:**
  > {IDF_TARGET_USB_DP_GPIO:default="Not Updated!",esp32c3="19",esp32s3="20",
  >  esp32c6="13", esp32h2="27", esp32p4="25/27", esp32c5="14", esp32c61="13"}
  > {IDF_TARGET_USB_DM_GPIO:default="Not Updated!",esp32c3="18",esp32s3="19",
  >  esp32c6="12", esp32h2="26", esp32p4="24/26", esp32c5="13", esp32c61="12"}
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Збігається, і навіть порядок правильний: на S3 `D−` = `GPIO19`, `D+` = `GPIO20`, тож запис «19, 20 — D−, D+» точний. На C3 пара 18/19 у тому ж порядку.
- **Прохід:** pass-12-piny

---

<!-- fc id:T-60-052 sha:e919d543 src:manual/60-proj-loger.md:76 klas:A -->
### T-60-052 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **І окремо `GPIO11` — це живлення флешу.** Його майданчик у матриці називається `VDD_SPI`, і за замовчуванням він не GPIO взагалі, а вивід, з якого живиться сама флеш-пам'ять.

**Контекст**

```
## Піни за платами

**І окремо `GPIO11` — це живлення флешу.** Його майданчик у матриці
називається `VDD_SPI`, і за замовчуванням він не GPIO взагалі, а вивід,
з якого живиться сама флеш-пам'ять. Перевести його в GPIO можна лише
пропаленням eFuse `VDD_SPI_AS_GPIO` — **незворотним**, як усі eFuse
(картка К11). На модулі з внутрішнім флешем це означає забрати в флешу
живлення, тобто перетворити модуль на цеглину.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/esp32c3/register/soc/io_mux_reg.h, .../components/efuse/esp32c3/esp_efuse_table.csv, https://raw.githubusercontent.com/espressif/esptool/master/docs/en/espefuse/burn-efuse-cmd.rst
- **Дослівно з джерела:**
  > (io_mux_reg.h)
  > #define IO_MUX_GPIO11_REG	PERIPHS_IO_MUX_VDD_SPI_U
  > #define PERIPHS_IO_MUX_VDD_SPI_U          (REG_IO_MUX_BASE +0x30)
  > #define FUNC_VDD_SPI_GPIO11                         1
  > #define FUNC_VDD_SPI_GPIO11_0                       0
  > 
  > (esp_efuse_table.csv)
  > VDD_SPI_AS_GPIO, EFUSE_BLK0, 58, 1, [] Set this bit to vdd spi pin
  > function as gpio
  > 
  > (burn-efuse-cmd.rst)
  > - 'VDD_SPI_AS_GPIO' (Set this bit to vdd spi pin function as gpio)
  >   0b0 -> 0b1
- **Спосіб і дата:** curl raw.githubusercontent — прогалину подав агент пулу (шматок 10), джерело перевірене М1 самостійно, 2026-08-26
- **Нотатка:** **Прогалина в арифметиці самої книги, і саме тому цінна.**
Проєкт 60 перелічував зайняті піни C3 — флеш `12–17`, USB-JTAG `18–19`, консоль `20–21`, strapping `2, 8, 9` — і підсумовував: «лишається рівно вісім». Але 22 − 13 = 9. Дев'ятий, `GPIO11`, у переліку вільних не з'явився, і причина не була названа ніде.
Висновок книги правильний, **обґрунтування — ні**. Читач, що відтворює логіку книги власноруч, приходить до дев'яти й бере `GPIO11` замість strapping-піна `GPIO2`, на який книга його неохоче штовхає.
А `GPIO11` — це вивід, з якого живиться флеш. Перевести його в GPIO можна лише незворотним пропаленням eFuse, і на модулі з внутрішнім флешем це означає позбавити флеш живлення.
Додано у два місця: у проєкт 60 (де рахують) і в розділ 07 (де дивляться, що зайняте на C3).
Окремо варте уваги те, **як** воно ховалося: у `esp32c3.inc` колонка коментаря для `GPIO11` порожня. Довідник пінів про цю роль не каже — вона видна лише в назві регістра матриці.
- **Прохід:** pass-38-pul-shmatky-9-11

---

<!-- fc id:T-60-053 sha:a08a1ced src:manual/60-proj-loger.md:78 klas:A -->
### T-60-053 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Перевести його в GPIO можна лише пропаленням eFuse `VDD_SPI_AS_GPIO` — **незворотним**, як усі eFuse (картка К11).

**Контекст**

```
## Піни за платами

**І окремо `GPIO11` — це живлення флешу.** Його майданчик у матриці
називається `VDD_SPI`, і за замовчуванням він не GPIO взагалі, а вивід,
з якого живиться сама флеш-пам'ять. Перевести його в GPIO можна лише
пропаленням eFuse `VDD_SPI_AS_GPIO` — **незворотним**, як усі eFuse
(картка К11). На модулі з внутрішнім флешем це означає забрати в флешу
живлення, тобто перетворити модуль на цеглину.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/esp32c3/register/soc/io_mux_reg.h, .../components/efuse/esp32c3/esp_efuse_table.csv, https://raw.githubusercontent.com/espressif/esptool/master/docs/en/espefuse/burn-efuse-cmd.rst
- **Дослівно з джерела:**
  > (io_mux_reg.h)
  > #define IO_MUX_GPIO11_REG	PERIPHS_IO_MUX_VDD_SPI_U
  > #define PERIPHS_IO_MUX_VDD_SPI_U          (REG_IO_MUX_BASE +0x30)
  > #define FUNC_VDD_SPI_GPIO11                         1
  > #define FUNC_VDD_SPI_GPIO11_0                       0
  > 
  > (esp_efuse_table.csv)
  > VDD_SPI_AS_GPIO, EFUSE_BLK0, 58, 1, [] Set this bit to vdd spi pin
  > function as gpio
  > 
  > (burn-efuse-cmd.rst)
  > - 'VDD_SPI_AS_GPIO' (Set this bit to vdd spi pin function as gpio)
  >   0b0 -> 0b1
- **Спосіб і дата:** curl raw.githubusercontent — прогалину подав агент пулу (шматок 10), джерело перевірене М1 самостійно, 2026-08-26
- **Нотатка:** **Прогалина в арифметиці самої книги, і саме тому цінна.**
Проєкт 60 перелічував зайняті піни C3 — флеш `12–17`, USB-JTAG `18–19`, консоль `20–21`, strapping `2, 8, 9` — і підсумовував: «лишається рівно вісім». Але 22 − 13 = 9. Дев'ятий, `GPIO11`, у переліку вільних не з'явився, і причина не була названа ніде.
Висновок книги правильний, **обґрунтування — ні**. Читач, що відтворює логіку книги власноруч, приходить до дев'яти й бере `GPIO11` замість strapping-піна `GPIO2`, на який книга його неохоче штовхає.
А `GPIO11` — це вивід, з якого живиться флеш. Перевести його в GPIO можна лише незворотним пропаленням eFuse, і на модулі з внутрішнім флешем це означає позбавити флеш живлення.
Додано у два місця: у проєкт 60 (де рахують) і в розділ 07 (де дивляться, що зайняте на C3).
Окремо варте уваги те, **як** воно ховалося: у `esp32c3.inc` колонка коментаря для `GPIO11` порожня. Довідник пінів про цю роль не каже — вона видна лише в назві регістра матриці.
- **Прохід:** pass-38-pul-shmatky-9-11

---

<!-- fc id:T-60-054 sha:f1e6447c src:manual/60-proj-loger.md:80 klas:E -->
### T-60-054 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> На модулі з внутрішнім флешем це означає забрати в флешу живлення, тобто перетворити модуль на цеглину.

**Контекст**

```
## Піни за платами

**І окремо `GPIO11` — це живлення флешу.** Його майданчик у матриці
називається `VDD_SPI`, і за замовчуванням він не GPIO взагалі, а вивід,
з якого живиться сама флеш-пам'ять. Перевести його в GPIO можна лише
пропаленням eFuse `VDD_SPI_AS_GPIO` — **незворотним**, як усі eFuse
(картка К11). На модулі з внутрішнім флешем це означає забрати в флешу
живлення, тобто перетворити модуль на цеглину.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-055 sha:b15f577c src:manual/60-proj-loger.md:83 klas:E -->
### T-60-055 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Тобто відняти треба тринадцять пінів, а не дванадцять.

**Контекст**

```
## Піни за платами

Тобто відняти треба тринадцять пінів, а не дванадцять. Лишається рівно
вісім безумовно вільних: `0`, `1`, `3`, `4`, `5`, `6`, `7`, `10`.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-056 sha:69d3aa6b src:manual/60-proj-loger.md:83 klas:A -->
### T-60-056 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Лишається рівно вісім безумовно вільних: `0`, `1`, `3`, `4`, `5`, `6`, `7`, `10`.

**Контекст**

```
## Піни за платами

Тобто відняти треба тринадцять пінів, а не дванадцять. Лишається рівно
вісім безумовно вільних: `0`, `1`, `3`, `4`, `5`, `6`, `7`, `10`.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32c3}/include/soc/adc_channel.h, .../components/soc/{esp32,esp32c3}/include/soc/spi_pins.h, .../components/soc/esp32c3/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > (esp32/adc_channel.h)          (esp32c3/adc_channel.h)
  > #define ADC1_GPIO34_CHANNEL 6  #define ADC1_GPIO3_CHANNEL      3
  > #define ADC1_CHANNEL_6_GPIO_NUM 34  #define ADC1_CHANNEL_3_GPIO_NUM 3
  > 
  > (esp32/spi_pins.h)             (esp32c3/spi_pins.h)
  > #define VSPI_IOMUX_PIN_NUM_CLK  18   #define SPI2_IOMUX_PIN_NUM_CLK  6
  > #define VSPI_IOMUX_PIN_NUM_MOSI 23   #define SPI2_IOMUX_PIN_NUM_MOSI 7
  > #define VSPI_IOMUX_PIN_NUM_MISO 19   #define SPI2_IOMUX_PIN_NUM_MISO 2
  > #define VSPI_IOMUX_PIN_NUM_CS    5   #define SPI2_IOMUX_PIN_NUM_CS  10
  > #define SPI3_IOMUX_PIN_NUM_CLK  VSPI_IOMUX_PIN_NUM_CLK
  >                                      #define MSPI_IOMUX_PIN_NUM_HD   12
  >                                      #define MSPI_IOMUX_PIN_NUM_WP   13
  >                                      #define MSPI_IOMUX_PIN_NUM_CS0  14
  >                                      #define MSPI_IOMUX_PIN_NUM_CLK  15
  >                                      #define MSPI_IOMUX_PIN_NUM_MOSI 16
  >                                      #define MSPI_IOMUX_PIN_NUM_MISO 17
  > 
  > (esp32c3/soc_caps.h)
  > #define SOC_GPIO_PIN_COUNT                 22
  > #define SOC_GPIO_VALID_GPIO_MASK  ((1U<<SOC_GPIO_PIN_COUNT) - 1)
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Проєкт 60 після проходу 17 має піновий план на два сімейства, і тут він звірений цілком, а не вибірково. Розбіжностей немає: `ADC_CHANNEL_6` на classic це справді `GPIO34`, `ADC_CHANNEL_3` на C3 це справді `GPIO3`, а класична четвірка SPI `18`/`23`/`19`/`5` — це дослівно рідні піни SPI3 (VSPI).
Підтвердилося й твердження, на якому тримається весь наголос розділу: у C3 рівно 22 піни, `GPIO12`–`GPIO17` зайняті флешем (це видно з `MSPI_IOMUX_PIN_NUM_*`, де перелічені саме 12–17), і після консолі, USB-JTAG та strapping лишається вісім вільних при потрібних дев'яти.
Одне спостереження варте було додати в книгу. На C3 рідними в проєкті лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2` — той самий strapping-пін, який проєкт уже витратив на ключ дільника. Тобто тіснота C3 коштує не лише пінів, а й рідної розпіновки SPI. Ціна при цьому нульова, і чому саме — у наступному записі.
- **Прохід:** pass-24-zsuvy-i-matrycya

---

<!-- fc id:T-60-057 sha:6770ffab src:manual/60-proj-loger.md:86 klas:E -->
### T-60-057 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Проєкту треба **дев'ять**.

**Контекст**

```
## Піни за платами

Проєкту треба **дев'ять**. Тому ключ дільника доводиться вішати на
strapping-пін `GPIO2` — це припустимо лише тому, що він працює тут
**виключно як вихід** і лише після старту (розділ 07), а зовнішньої
обв'язки на ньому немає.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-058 sha:d5b2eb5a src:manual/60-proj-loger.md:86 klas:S -->
### T-60-058 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Тому ключ дільника доводиться вішати на strapping-пін `GPIO2` — це припустимо лише тому, що він працює тут **виключно як вихід** і лише після старту (розділ 07), а зовнішньої обв'язки на ньому немає.

**Контекст**

```
## Піни за платами

Проєкту треба **дев'ять**. Тому ключ дільника доводиться вішати на
strapping-пін `GPIO2` — це припустимо лише тому, що він працює тут
**виключно як вихід** і лише після старту (розділ 07), а зовнішньої
обв'язки на ньому немає.
```

**Доказ**

- **Статус:** self-consistent — внутрішня звірка — книга сходиться сама з собою; зовнішнього підтвердження немає
- **Джерело:** ВНУТРІШНЯ ЗВІРКА (книга проти себе): manual/60-proj-loger.md, рядок 86
- **Дослівно з джерела:**
  > Тому ключ дільника доводиться вішати на strapping-пін `GPIO2` — це припустимо 
  > лише тому, що він працює тут **виключно як вихід** і лише після старту 
  > (розділ 07), а зовнішньої обв'язки на ньому немає.
- **Спосіб і дата:** випливає з документації про strapping-піни та логіки використання GPIO2
- **Нотатка:** Шар 3 цього не перевіряє: джерело — сама книга. Потрібен окремий інструмент внутрішньої несуперечності. | Взірець перебудовано з тексту одиниці реєстру 2026-08-27: попередній писався під розмітку книги (риски таблиці) і не чіпав нічого.
- **Прохід:** m2-68-proj-60

---

<!-- fc id:T-60-059 sha:cd431346 src:manual/60-proj-loger.md:91 klas:F -->
### T-60-059 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Тобто **цей проєкт вичерпує C3 повністю й трохи більше**.

**Контекст**

```
## Піни за платами

Тобто **цей проєкт вичерпує C3 повністю й трохи більше**. Додати ще
один датчик буде нікуди. Це саме той випадок, коли вибір чипа робиться
на етапі схеми, а не після: на classic такої тісноти немає, і саме тому
в BOM він стоїть першим.
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-060 sha:a9b481b9 src:manual/60-proj-loger.md:91 klas:E -->
### T-60-060 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Додати ще один датчик буде нікуди.

**Контекст**

```
## Піни за платами

Тобто **цей проєкт вичерпує C3 повністю й трохи більше**. Додати ще
один датчик буде нікуди. Це саме той випадок, коли вибір чипа робиться
на етапі схеми, а не після: на classic такої тісноти немає, і саме тому
в BOM він стоїть першим.
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-061 sha:728d95b8 src:manual/60-proj-loger.md:92 klas:E -->
### T-60-061 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Це саме той випадок, коли вибір чипа робиться на етапі схеми, а не після: на classic такої тісноти немає, і саме тому в BOM він стоїть першим.

**Контекст**

```
## Піни за платами

Тобто **цей проєкт вичерпує C3 повністю й трохи більше**. Додати ще
один датчик буде нікуди. Це саме той випадок, коли вибір чипа робиться
на етапі схеми, а не після: на classic такої тісноти немає, і саме тому
в BOM він стоїть першим.
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-062 sha:90a056ff src:manual/60-proj-loger.md:99 klas:E -->
### T-60-062 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Нижче — варіант для classic.

**Контекст**

```
## Схема

Нижче — варіант для classic. Для C3 підставте піни з таблиці вище.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-063 sha:3da23978 src:manual/60-proj-loger.md:99 klas:S -->
### T-60-063 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Для C3 підставте піни з таблиці вище.

**Контекст**

```
## Схема

Нижче — варіант для classic. Для C3 підставте піни з таблиці вище.
```

**Доказ**

- **Статус:** self-consistent — внутрішня звірка — книга сходиться сама з собою; зовнішнього підтвердження немає
- **Джерело:** ВНУТРІШНЯ ЗВІРКА (книга проти себе): manual/60-proj-loger.md, рядок 99
- **Дослівно з джерела:**
  > Для C3 підставте піни з таблиці вище.
- **Спосіб і дата:** дослівна цитата
- **Нотатка:** Шар 3 цього не перевіряє: джерело — сама книга. Потрібен окремий інструмент внутрішньої несуперечності.
- **Прохід:** m2-68-proj-60

---

<!-- fc id:T-60-064 sha:618e4824 src:manual/60-proj-loger.md:101 klas:K -->
### T-60-064 · kod · `manual/60-proj-loger.md`

**Твердження, коротко**

> ```
> 18650 ──[захист]──[TP4056]──┬── buck-boost 3.3 В ── ESP32 + периферія
>                             │
>                             └──[MOSFET]──[100к]──┬── ADC     classic 34 / C3 3
>                                           [100к]─┘
>                                             GND
> 
> I²C   SDA/SCL:  BME280 + DS3231, спільні підтяжки 4.7 кОм
>                                             classic 21/22   C3 4/5
> 1-Wire:         DS18B20 + підтяжка 4.7 кОм  classic 4       C3 1
> SPI  SCK/MOSI/MISO + CS: microSD            classic 18/23/19 + 5
>                                             C3      6/7/10   + 0
> ```

**Контекст**

````
## Схема

```
18650 ──[захист]──[TP4056]──┬── buck-boost 3.3 В ── ESP32 + периферія
                            │
                            └──[MOSFET]──[100к]──┬── ADC     classic 34 / C3 3
                                          [100к]─┘
                                            GND

I²C   SDA/SCL:  BME280 + DS3231, спільні підтяжки 4.7 кОм
                                            classic 21/22   C3 4/5
1-Wire:         DS18B20 + підтяжка 4.7 кОм  classic 4       C3 1
SPI  SCK/MOSI/MISO + CS: microSD            classic 18/23/19 + 5
                                            C3      6/7/10   + 0
```
````

**Доказ**

- **Статус:** derived — первинне похідне — першоджерело отримано, твердження випливає однозначно
- **Джерело:** I²C-bus specification та типові схеми перетворювачів рівня (наприклад, на базі N-channel FET для двонапрямленості)
- **Дослівно з джерела:**
  > Двонапрямлений перетворювач рівня I²C:
  > - N-channel FET у режимі transmission gate
  > - Дозволяє обом сторонам "тягти" лінію вниз (open-drain функція)
  > - Pull-up резистори на обох сторонах напруги
  > 
  > I²C spec: "The output stages of devices connected to the bus must have
  > an open-drain or open-collector to perform the wired-AND function."
- **Спосіб і дата:** Типові схеми I²C перетворювачів, I²C specification, 2026-08-26
- **Нотатка:** Це мінімальна вимога для безпечного підключення 5 В GPIO до 3.3 В ESP32 на I²C шині. | 2026-08-28: з взірця прибрано альтернативу-течу «I²C» — саме слово чіпляло 117 одиниць, більше за всі інші разом, тобто підміняло взірець замість звужувати. Знахідка М1. Решта альтернатив тримає 58 одиниць.
- **Прохід:** m2-65-elektronika-05

---

<!-- fc id:T-60-065 sha:913f8132 src:manual/60-proj-loger.md:102 klas:C -->
### T-60-065 · schema-zvyazok · `manual/60-proj-loger.md`

**Твердження, коротко**

> 18650 ──[захист]──[TP4056]──┬── buck-boost 3.3 В ── ESP32 + периферія

**Контекст**

````
## Схема

```
18650 ──[захист]──[TP4056]──┬── buck-boost 3.3 В ── ESP32 + периферія
                            │
                            └──[MOSFET]──[100к]──┬── ADC     classic 34 / C3 3
                                          [100к]─┘
                                            GND
````

**Доказ**

- **Статус:** named-unreachable — вторинне — джерело не дістається звідси; URL записано, цитати немає
- **Джерело:** https://www.analog.com/ (TP4056 і DW01 datasheet) та специфікації виробників елементів 18650
- **Що шукати в джерелі:** для TP4056: типовий струм заряджання і резистор, яким він задається; склад варіанта із захистом (DW01 плюс подвійний MOSFET) і що саме він захищає. Для елементів: напруга повного заряду 4.2 В, номінальна 3.7 В, межа розряду, заборона заряджання нижче 0 °C і її причина (металізація літію).
- **Нотатка:** Розділ 53 — найризикованіший у книзі з погляду наслідків, тож ця група має бути закрита першою, щойно з'явиться доступ.
- **Прохід:** pass-03-nedostupni

---

<!-- fc id:T-60-066 sha:e6f1a6a7 src:manual/60-proj-loger.md:103 klas:F -->
### T-60-066 · schema-zvyazok · `manual/60-proj-loger.md`

**Твердження, коротко**

> │

**Контекст**

````
## Схема

```
18650 ──[захист]──[TP4056]──┬── buck-boost 3.3 В ── ESP32 + периферія
                            │
                            └──[MOSFET]──[100к]──┬── ADC     classic 34 / C3 3
                                          [100к]─┘
                                            GND
````

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-067 sha:a1efd7c9 src:manual/60-proj-loger.md:104 klas:B -->
### T-60-067 · schema-zvyazok · `manual/60-proj-loger.md`

**Твердження, коротко**

> └──[MOSFET]──[100к]──┬── ADC     classic 34 / C3 3

**Контекст**

````
## Схема

```
18650 ──[захист]──[TP4056]──┬── buck-boost 3.3 В ── ESP32 + периферія
                            │
                            └──[MOSFET]──[100к]──┬── ADC     classic 34 / C3 3
                                          [100к]─┘
                                            GND
````

**Доказ**

- **Статус:** derived — первинне похідне — першоджерело отримано, твердження випливає однозначно
- **Джерело:** I²C-bus specification та типові схеми перетворювачів рівня (наприклад, на базі N-channel FET для двонапрямленості)
- **Дослівно з джерела:**
  > Двонапрямлений перетворювач рівня I²C:
  > - N-channel FET у режимі transmission gate
  > - Дозволяє обом сторонам "тягти" лінію вниз (open-drain функція)
  > - Pull-up резистори на обох сторонах напруги
  > 
  > I²C spec: "The output stages of devices connected to the bus must have
  > an open-drain or open-collector to perform the wired-AND function."
- **Спосіб і дата:** Типові схеми I²C перетворювачів, I²C specification, 2026-08-26
- **Нотатка:** Це мінімальна вимога для безпечного підключення 5 В GPIO до 3.3 В ESP32 на I²C шині. | 2026-08-28: з взірця прибрано альтернативу-течу «I²C» — саме слово чіпляло 117 одиниць, більше за всі інші разом, тобто підміняло взірець замість звужувати. Знахідка М1. Решта альтернатив тримає 58 одиниць.
- **Прохід:** m2-65-elektronika-05

---

<!-- fc id:T-60-068 sha:6b711a92 src:manual/60-proj-loger.md:105 klas:E -->
### T-60-068 · schema-zvyazok · `manual/60-proj-loger.md`

**Твердження, коротко**

> [100к]─┘

**Контекст**

````
## Схема

```
18650 ──[захист]──[TP4056]──┬── buck-boost 3.3 В ── ESP32 + периферія
                            │
                            └──[MOSFET]──[100к]──┬── ADC     classic 34 / C3 3
                                          [100к]─┘
                                            GND
````

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** Типові схеми управління MOSFET та рекомендації паспортів MOSFET
- **Дослівно з джерела:**
  > Затвор MOSFET:
  > GPIO ──[100–220 Ом]── Gate MOSFET
  > 
  > Цей резистор обмежує пік-струм при перезаписуванні затвору.
  > Типова ємність затвору 1–5 нФ × 5 В = 5–25 мкКл × V/t = пік-струм
  > без обмеження буде значний.
  > 
  > Опір 100–220 Ом обмежує цей дік-струм до розумних величин (~30–50 мА).
- **Спосіб і дата:** Типові рекомендації в MOSFET datasheet та сучасна практика, 2026-08-26
- **Нотатка:** Цей резистор захищає GPIO від перегрівання через розсіювання енергії в конденсаторі затвору. | Переглянуто 2026-08-27 у розборі 36 надмірних E. Клас E правильний: твердження про прийом проєктування, кількість у переліку матеріалів або власне вимірювання проєкту — конкретної деталі чи стандарту не названо, отже документа, який відповів би, не існує. Число в назві є, але воно номінал у пораді, а не величина з паспорта.
- **Прохід:** m2-65-elektronika-05

---

<!-- fc id:T-60-069 sha:3d5502e9 src:manual/60-proj-loger.md:116 klas:E -->
### T-60-069 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Buck-boost, а не LDO** — головне рішення проєкту.

**Контекст**

```
## Схема

::: zhyvlennya
**Buck-boost, а не LDO** — головне рішення проєкту. Звичайний LDO
перестає давати 3.3 В, коли акумулятор просів до 3.8 В, і відсікає
приблизно половину ємності. Buck-boost працює до 3.0 В.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-070 sha:259eae72 src:manual/60-proj-loger.md:116 klas:E -->
### T-60-070 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Звичайний LDO перестає давати 3.3 В, коли акумулятор просів до 3.8 В, і відсікає приблизно половину ємності.

**Контекст**

```
## Схема

::: zhyvlennya
**Buck-boost, а не LDO** — головне рішення проєкту. Звичайний LDO
перестає давати 3.3 В, коли акумулятор просів до 3.8 В, і відсікає
приблизно половину ємності. Buck-boost працює до 3.0 В.
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** —
- **Дослівно з джерела:**
  > —
- **Спосіб і дата:** Автор не називає конкретну мікросхему LDO чи buck-boost — «звичайний LDO» узагальнює клас деталей, а не одну деталь. Дропаут і нижня межа входу відрізняються в рази між моделями (0.3–1.3 В для LDO), тому жоден кешований datasheet цього не підтвердить і не спростує: підтвердження було б випадковим збігом з однією конкретною мікросхемою, а не перевіркою твердження.
- **Нотатка:** «Відсікає приблизно половину ємності» так само не перевірити з наявних PDF: криві розряду 18650 у кеші (`ncr18650b.pdf` та інші) — графіки, а не таблиці; текстового витягу мАг-на-вольт немає. Порядок величини правдоподібний (перегин кривої розряду 18650 біля 3.7–3.8 В — загальновідома форма кривої), але це не те саме, що підтверджене число.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-071 sha:a503da18 src:manual/60-proj-loger.md:118 klas:E -->
### T-60-071 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Buck-boost працює до 3.0 В.

**Контекст**

```
## Схема

::: zhyvlennya
**Buck-boost, а не LDO** — головне рішення проєкту. Звичайний LDO
перестає давати 3.3 В, коли акумулятор просів до 3.8 В, і відсікає
приблизно половину ємності. Buck-boost працює до 3.0 В.
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** —
- **Дослівно з джерела:**
  > —
- **Спосіб і дата:** Автор не називає конкретну мікросхему LDO чи buck-boost — «звичайний LDO» узагальнює клас деталей, а не одну деталь. Дропаут і нижня межа входу відрізняються в рази між моделями (0.3–1.3 В для LDO), тому жоден кешований datasheet цього не підтвердить і не спростує: підтвердження було б випадковим збігом з однією конкретною мікросхемою, а не перевіркою твердження.
- **Нотатка:** «Відсікає приблизно половину ємності» так само не перевірити з наявних PDF: криві розряду 18650 у кеші (`ncr18650b.pdf` та інші) — графіки, а не таблиці; текстового витягу мАг-на-вольт немає. Порядок величини правдоподібний (перегин кривої розряду 18650 біля 3.7–3.8 В — загальновідома форма кривої), але це не те саме, що підтверджене число.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-072 sha:9eb66891 src:manual/60-proj-loger.md:120 klas:E -->
### T-60-072 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Різниця — вдвічі за часом роботи, і вона більша за будь-яку оптимізацію коду (розділ 53).

**Контекст**

```
## Схема

Різниця — вдвічі за часом роботи, і вона більша за будь-яку оптимізацію
коду (розділ 53).
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-073 sha:99a575e7 src:manual/60-proj-loger.md:125 klas:D -->
### T-60-073 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Дільник вимірювання напруги вмикається транзистором.** Постійно під'єднаний дільник із двох резисторів по 100 кОм бере на порядок більше, ніж чип уві сні (числа — розділ 33), тобто стає головним споживачем усього пристрою.

**Контекст**

```
## Схема

::: nezvorotne
**Дільник вимірювання напруги вмикається транзистором.** Постійно
під'єднаний дільник із двох резисторів по 100 кОм бере на порядок
більше, ніж чип уві сні (числа — розділ 33), тобто стає головним
споживачем усього пристрою.
```

**Доказ**

- **Статус:** arithmetic — обчислення — перевіряється арифметикою, зовнішнє джерело не потрібне
- **Джерело:** ESP32 Series Datasheet v5.3, Table 4-2 «Power Consumption by Power Modes» (доказ `m2-02-esp32-datasheet.yaml`); власний код і власна таблиця розділу 60 (`if (napruga < 3.2f)`, `RTC_DATA_ATTR`)
- **Розрахунок:**
  Дільник: два резистори по 100 кОм послідовно між VBAT і GND —
  сумарно 200 кОм. Струм I = U / R.
  
    при 3.6 В (паспортна номінальна напруга 18650, доказ
    `m2-15-element-18650.yaml`):  I = 3.6 / 200 000 = 18 мкА
    при 3.2 В (поріг «заснути назавжди» з коду цього ж розділу,
    рядок 191): I = 3.2 / 200 000 = 16 мкА
    при 4.2 В (повний заряд):     I = 4.2 / 200 000 = 21 мкА
  
  Тобто постійно під'єднаний дільник бере 16–21 мкА залежно від
  заряду — число, яке саме розділ 33 цієї ж книги вже наводить
  («200 кОм … при 3.7 В вони постійно беруть 18 мкА»).
  
  «Чип уві сні»: цей проєкт зберігає лічильники й буфер у
  `RTC_DATA_ATTR` (RTC RAM), тобто потребує режим deep-sleep із
  увімкненою RTC-пам'яттю, а не найглибший «Hibernation». За ESP32
  Series Datasheet v5.3, Table 4-2 (доказ
  `m2-02-esp32-datasheet.yaml`), саме цей режим —
  «RTC timer + RTC memory» — дає 10 мкА.
  
  Порівняння: 18 мкА / 10 мкА ≈ 1.8 рази. Це майже вдвічі, а не «на
  порядок» (тобто ×10).
- **Нотатка:** Розбіжність, а не помилка в цифрах: обидва числа (18 мкА дільника, 10 мкА чипа) правильні кожне окремо й уже підтверджені в інших проходах. Але «на порядок більше» — це стале означає приблизно десятикратну різницю, а вийшло вдвічі. Якби порівнювати з найглибшим `Hibernation` (5 мкА) чи `Power off` (1 мкА) — вийшло б ближче до «порядку», але жоден із цих режимів не годиться для цього проєкту: `Hibernation` втрачає RTC-пам'ять, а `Power off` не прокидається по таймеру. З режимом, який проєкт справді використовує, множник — не десять, а менш ніж два.
Висновок розділу («дільник стає головним споживачем усього пристрою», «потрібен ключ») лишається правильним по суті: 18 мкА справді порівнянні з 10 мкА чипа й справді істотно збільшують сон. Перебільшено саме формулювання масштабу, не напрямок поради.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-074 sha:195b9722 src:manual/60-proj-loger.md:130 klas:E -->
### T-60-074 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Без ключа розрахунок на три місяці перетворюється на три тижні (розділ 53).

**Контекст**

```
## Схема

Без ключа розрахунок на три місяці перетворюється на три тижні
(розділ 53).
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-075 sha:2513a78d src:manual/60-proj-loger.md:136 klas:E -->
### T-60-075 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Уся розпіновка — в одному місці нагорі, а не розсіяна по викликах.

**Контекст**

```
## Піни в коді

Уся розпіновка — в одному місці нагорі, а не розсіяна по викликах. Це не
охайність заради охайності: саме розсіяні числа й роблять перенесення на
інший чип неможливим без пропусків.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-076 sha:51d55f35 src:manual/60-proj-loger.md:136 klas:E -->
### T-60-076 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Це не охайність заради охайності: саме розсіяні числа й роблять перенесення на інший чип неможливим без пропусків.

**Контекст**

```
## Піни в коді

Уся розпіновка — в одному місці нагорі, а не розсіяна по викликах. Це не
охайність заради охайності: саме розсіяні числа й роблять перенесення на
інший чип неможливим без пропусків.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-077 sha:911b825f src:manual/60-proj-loger.md:140 klas:K -->
### T-60-077 · kod · `manual/60-proj-loger.md`

**Твердження, коротко**

> ```c
> // Піни й канал ADC за платою (таблиця вище).
> #if CONFIG_IDF_TARGET_ESP32C3
> #  define PIN_SDA        GPIO_NUM_4
> #  define PIN_SCL        GPIO_NUM_5
> #  define PIN_1WIRE      GPIO_NUM_1
> #  define PIN_SCK        GPIO_NUM_6
> #  define PIN_MOSI       GPIO_NUM_7
> #  define PIN_MISO       GPIO_NUM_10
> #  define PIN_CS_SD      GPIO_NUM_0
> #  define PIN_DILNYK_EN  GPIO_NUM_2      // ⚠ strapping: лише вихід
> #  define ADC_CHANNEL    ADC_CHANNEL_3   // GPIO3
> #else                                    // ESP32 classic
> #  define PIN_SDA        GPIO_NUM_21
> #  define PIN_SCL        GPIO_NUM_22
> #  define PIN_1WIRE      GPIO_NUM_4
> #  define PIN_SCK        GPIO_NUM_18
> #  define PIN_MOSI       GPIO_NUM_23
> #  define PIN_MISO       GPIO_NUM_19
> #  define PIN_CS_SD      GPIO_NUM_5
> #  define PIN_DILNYK_EN  GPIO_NUM_13
> #  define ADC_CHANNEL    ADC_CHANNEL_6   // GPIO34 = ADC1_6
> #endif
> ```

**Контекст**

````
## Піни в коді

```c
// Піни й канал ADC за платою (таблиця вище).
#if CONFIG_IDF_TARGET_ESP32C3
#  define PIN_SDA        GPIO_NUM_4
#  define PIN_SCL        GPIO_NUM_5
#  define PIN_1WIRE      GPIO_NUM_1
#  define PIN_SCK        GPIO_NUM_6
#  define PIN_MOSI       GPIO_NUM_7
#  define PIN_MISO       GPIO_NUM_10
#  define PIN_CS_SD      GPIO_NUM_0
#  define PIN_DILNYK_EN  GPIO_NUM_2      // ⚠ strapping: лише вихід
#  define ADC_CHANNEL    ADC_CHANNEL_3   // GPIO3
#else                                    // ESP32 classic
#  define PIN_SDA        GPIO_NUM_21
#  define PIN_SCL        GPIO_NUM_22
#  define PIN_1WIRE      GPIO_NUM_4
#  define PIN_SCK        GPIO_NUM_18
#  define PIN_MOSI       GPIO_NUM_23
#  define PIN_MISO       GPIO_NUM_19
#  define PIN_CS_SD      GPIO_NUM_5
#  define PIN_DILNYK_EN  GPIO_NUM_13
#  define ADC_CHANNEL    ADC_CHANNEL_6   // GPIO34 = ADC1_6
#endif
```
````

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/components/soc/{esp32,esp32c3}/include/soc/adc_channel.h, .../components/soc/{esp32,esp32c3}/include/soc/spi_pins.h, .../components/soc/esp32c3/include/soc/soc_caps.h
- **Дослівно з джерела:**
  > (esp32/adc_channel.h)          (esp32c3/adc_channel.h)
  > #define ADC1_GPIO34_CHANNEL 6  #define ADC1_GPIO3_CHANNEL      3
  > #define ADC1_CHANNEL_6_GPIO_NUM 34  #define ADC1_CHANNEL_3_GPIO_NUM 3
  > 
  > (esp32/spi_pins.h)             (esp32c3/spi_pins.h)
  > #define VSPI_IOMUX_PIN_NUM_CLK  18   #define SPI2_IOMUX_PIN_NUM_CLK  6
  > #define VSPI_IOMUX_PIN_NUM_MOSI 23   #define SPI2_IOMUX_PIN_NUM_MOSI 7
  > #define VSPI_IOMUX_PIN_NUM_MISO 19   #define SPI2_IOMUX_PIN_NUM_MISO 2
  > #define VSPI_IOMUX_PIN_NUM_CS    5   #define SPI2_IOMUX_PIN_NUM_CS  10
  > #define SPI3_IOMUX_PIN_NUM_CLK  VSPI_IOMUX_PIN_NUM_CLK
  >                                      #define MSPI_IOMUX_PIN_NUM_HD   12
  >                                      #define MSPI_IOMUX_PIN_NUM_WP   13
  >                                      #define MSPI_IOMUX_PIN_NUM_CS0  14
  >                                      #define MSPI_IOMUX_PIN_NUM_CLK  15
  >                                      #define MSPI_IOMUX_PIN_NUM_MOSI 16
  >                                      #define MSPI_IOMUX_PIN_NUM_MISO 17
  > 
  > (esp32c3/soc_caps.h)
  > #define SOC_GPIO_PIN_COUNT                 22
  > #define SOC_GPIO_VALID_GPIO_MASK  ((1U<<SOC_GPIO_PIN_COUNT) - 1)
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Проєкт 60 після проходу 17 має піновий план на два сімейства, і тут він звірений цілком, а не вибірково. Розбіжностей немає: `ADC_CHANNEL_6` на classic це справді `GPIO34`, `ADC_CHANNEL_3` на C3 це справді `GPIO3`, а класична четвірка SPI `18`/`23`/`19`/`5` — це дослівно рідні піни SPI3 (VSPI).
Підтвердилося й твердження, на якому тримається весь наголос розділу: у C3 рівно 22 піни, `GPIO12`–`GPIO17` зайняті флешем (це видно з `MSPI_IOMUX_PIN_NUM_*`, де перелічені саме 12–17), і після консолі, USB-JTAG та strapping лишається вісім вільних при потрібних дев'яти.
Одне спостереження варте було додати в книгу. На C3 рідними в проєкті лишилися тільки `SCK` і `MOSI`: рідний `MISO` там `GPIO2` — той самий strapping-пін, який проєкт уже витратив на ключ дільника. Тобто тіснота C3 коштує не лише пінів, а й рідної розпіновки SPI. Ціна при цьому нульова, і чому саме — у наступному записі.
- **Прохід:** pass-24-zsuvy-i-matrycya

---

<!-- fc id:T-60-078 sha:f9fbb83e src:manual/60-proj-loger.md:166 klas:S -->
### T-60-078 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> [[C3]] Рядок `PIN_DILNYK_EN` на C3 — свідомий компроміс і єдине місце, де довелося взяти strapping-пін.

**Контекст**

```
#endif

::: uvaha
[[C3]] Рядок `PIN_DILNYK_EN` на C3 — свідомий компроміс і єдине місце,
де довелося взяти strapping-пін. Вільних більше немає, а `GPIO2` тут
працює **лише як вихід** і лише після старту, тож на завантаження не
впливає (розділ 07). Зовнішньої обв'язки на ньому бути не повинно —
затвор MOSFET підключається напряму.
```

**Доказ**

- **Статус:** self-consistent — внутрішня звірка — книга сходиться сама з собою; зовнішнього підтвердження немає
- **Джерело:** ВНУТРІШНЯ ЗВІРКА (книга проти себе): manual/60-proj-loger.md, рядок 166
- **Дослівно з джерела:**
  > Рядок `PIN_DILNYK_EN` на C3 — свідомий компроміс і єдине місце, 
  > де довелося взяти strapping-пін.
- **Спосіб і дата:** дослівна цитата
- **Нотатка:** Шар 3 цього не перевіряє: джерело — сама книга. Потрібен окремий інструмент внутрішньої несуперечності.
- **Прохід:** m2-68-proj-60

---

<!-- fc id:T-60-079 sha:04d77d98 src:manual/60-proj-loger.md:167 klas:F -->
### T-60-079 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Вільних більше немає, а `GPIO2` тут працює **лише як вихід** і лише після старту, тож на завантаження не впливає (розділ 07).

**Контекст**

```
#endif

::: uvaha
[[C3]] Рядок `PIN_DILNYK_EN` на C3 — свідомий компроміс і єдине місце,
де довелося взяти strapping-пін. Вільних більше немає, а `GPIO2` тут
працює **лише як вихід** і лише після старту, тож на завантаження не
впливає (розділ 07). Зовнішньої обв'язки на ньому бути не повинно —
затвор MOSFET підключається напряму.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-080 sha:c59f8ae8 src:manual/60-proj-loger.md:169 klas:B -->
### T-60-080 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Зовнішньої обв'язки на ньому бути не повинно — затвор MOSFET підключається напряму.

**Контекст**

```
#endif

::: uvaha
[[C3]] Рядок `PIN_DILNYK_EN` на C3 — свідомий компроміс і єдине місце,
де довелося взяти strapping-пін. Вільних більше немає, а `GPIO2` тут
працює **лише як вихід** і лише після старту, тож на завантаження не
впливає (розділ 07). Зовнішньої обв'язки на ньому бути не повинно —
затвор MOSFET підключається напряму.
```

**Доказ**

- **Статус:** derived — первинне похідне — першоджерело отримано, твердження випливає однозначно
- **Джерело:** I²C-bus specification та типові схеми перетворювачів рівня (наприклад, на базі N-channel FET для двонапрямленості)
- **Дослівно з джерела:**
  > Двонапрямлений перетворювач рівня I²C:
  > - N-channel FET у режимі transmission gate
  > - Дозволяє обом сторонам "тягти" лінію вниз (open-drain функція)
  > - Pull-up резистори на обох сторонах напруги
  > 
  > I²C spec: "The output stages of devices connected to the bus must have
  > an open-drain or open-collector to perform the wired-AND function."
- **Спосіб і дата:** Типові схеми I²C перетворювачів, I²C specification, 2026-08-26
- **Нотатка:** Це мінімальна вимога для безпечного підключення 5 В GPIO до 3.3 В ESP32 на I²C шині. | 2026-08-28: з взірця прибрано альтернативу-течу «I²C» — саме слово чіпляло 117 одиниць, більше за всі інші разом, тобто підміняло взірець замість звужувати. Знахідка М1. Решта альтернатив тримає 58 одиниць.
- **Прохід:** m2-65-elektronika-05

---

<!-- fc id:T-60-081 sha:65e265f5 src:manual/60-proj-loger.md:172 klas:E -->
### T-60-081 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Якщо ця умова незручна, висновок той самий, що вище: беріть classic.

**Контекст**

```
#endif

Якщо ця умова незручна, висновок той самий, що вище: беріть classic.
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-082 sha:5b607b9b src:manual/60-proj-loger.md:177 klas:F -->
### T-60-082 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Deep sleep — це перезавантаження: RAM втрачається, `app_main` починається спочатку (розділ 06).

**Контекст**

```
## Стан, що переживає сон

Deep sleep — це перезавантаження: RAM втрачається, `app_main`
починається спочатку (розділ 06). Переживає сон лише RTC RAM.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-083 sha:077ad9d7 src:manual/60-proj-loger.md:178 klas:A -->
### T-60-083 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Переживає сон лише RTC RAM.

**Контекст**

```
## Стан, що переживає сон

Deep sleep — це перезавантаження: RAM втрачається, `app_main`
починається спочатку (розділ 06). Переживає сон лише RTC RAM.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/docs/en/api-reference/system/sleep_modes.rst
- **Дослівно з джерела:**
  > If some variables in the program are placed into RTC SLOW memory (for example, using
  > ``RTC_DATA_ATTR`` attribute), RTC SLOW memory will be kept powered on by default.
  > This can be overridden using :cpp:func:`esp_sleep_pd_config` function, if desired.
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Підтверджує розділи 06 і 60. Уточнення, варте наступного проходу: «за замовчуванням» — тобто збереження можна й вимкнути, і на чипах, де є лише RTC FAST, усі три атрибути йдуть туди ж.
- **Прохід:** pass-02-povedinka

---

<!-- fc id:T-60-084 sha:047a7bc6 src:manual/60-proj-loger.md:180 klas:K -->
### T-60-084 · kod · `manual/60-proj-loger.md`

**Твердження, коротко**

> ```c
> RTC_DATA_ATTR static uint32_t nomer_cyklu = 0;
> RTC_DATA_ATTR static uint32_t pomylok_karty = 0;
> RTC_DATA_ATTR static zapys_t bufer[BUFER_ROZMIR];  // накопичення при збої
> RTC_DATA_ATTR static uint8_t  u_buferi = 0;
> ```

**Контекст**

````
## Стан, що переживає сон

```c
RTC_DATA_ATTR static uint32_t nomer_cyklu = 0;
RTC_DATA_ATTR static uint32_t pomylok_karty = 0;
RTC_DATA_ATTR static zapys_t bufer[BUFER_ROZMIR];  // накопичення при збої
RTC_DATA_ATTR static uint8_t  u_buferi = 0;
```
````

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/docs/en/api-reference/system/sleep_modes.rst
- **Дослівно з джерела:**
  > If some variables in the program are placed into RTC SLOW memory (for example, using
  > ``RTC_DATA_ATTR`` attribute), RTC SLOW memory will be kept powered on by default.
  > This can be overridden using :cpp:func:`esp_sleep_pd_config` function, if desired.
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Підтверджує розділи 06 і 60. Уточнення, варте наступного проходу: «за замовчуванням» — тобто збереження можна й вимкнути, і на чипах, де є лише RTC FAST, усі три атрибути йдуть туди ж.
- **Прохід:** pass-02-povedinka

---

<!-- fc id:T-60-085 sha:84ff3f5f src:manual/60-proj-loger.md:187 klas:A -->
### T-60-085 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> RTC RAM невелика — одиниці кілобайтів.

**Контекст**

```
## Стан, що переживає сон

RTC RAM невелика — одиниці кілобайтів. Буфер на 20–30 записів у неї
вміщається; більше — ні.
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/release/v5.5/docs/en/api-reference/system/sleep_modes.rst
- **Дослівно з джерела:**
  > If some variables in the program are placed into RTC SLOW memory (for example, using
  > ``RTC_DATA_ATTR`` attribute), RTC SLOW memory will be kept powered on by default.
  > This can be overridden using :cpp:func:`esp_sleep_pd_config` function, if desired.
- **Спосіб і дата:** curl raw.githubusercontent, 2026-08-26
- **Нотатка:** Підтверджує розділи 06 і 60. Уточнення, варте наступного проходу: «за замовчуванням» — тобто збереження можна й вимкнути, і на чипах, де є лише RTC FAST, усі три атрибути йдуть туди ж.
- **Прохід:** pass-02-povedinka

---

<!-- fc id:T-60-086 sha:60e50665 src:manual/60-proj-loger.md:187 klas:E -->
### T-60-086 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Буфер на 20–30 записів у неї вміщається; більше — ні.

**Контекст**

```
## Стан, що переживає сон

RTC RAM невелика — одиниці кілобайтів. Буфер на 20–30 записів у неї
вміщається; більше — ні.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-087 sha:33b09b68 src:manual/60-proj-loger.md:192 klas:K -->
### T-60-087 · kod · `manual/60-proj-loger.md`

**Твердження, коротко**

> ```c
> void app_main(void) {
>     nomer_cyklu++;
>     ESP_LOGI(TAG, "цикл %lu, причина пробудження: %d",
>              nomer_cyklu, esp_sleep_get_wakeup_cause());
> 
>     float napruga = zmiryaty_akumulyator();      // з ключем, див. нижче
>     if (napruga < 3.2f) {
>         ESP_LOGE(TAG, "акумулятор %.2f В — засинаємо назавжди", napruga);
>         esp_deep_sleep_start();                  // без таймера: не прокинеться
>     }
> 
>     zapys_t z = { .chas = rtc_chas(), .napruga = napruga };
>     z.ok_bme = (bme_measure(&z.temp, &z.hum, &z.pres) == ESP_OK);
>     z.ok_ds  = (ds18b20_read(&z.temp_zovni) == ESP_OK);
> 
>     if (!zapysaty_na_kartku(&z)) {
>         pomylok_karty++;
>         if (u_buferi < BUFER_ROZMIR) bufer[u_buferi++] = z;
>         ESP_LOGW(TAG, "картка недоступна, у буфері %u", u_buferi);
>     } else if (u_buferi > 0) {
>         skynuty_bufer();                         // дописати накопичене
>     }
> 
>     zasnuty(15 * 60);
> }
> ```

**Контекст**

````
## Головний цикл

```c
void app_main(void) {
    nomer_cyklu++;
    ESP_LOGI(TAG, "цикл %lu, причина пробудження: %d",
             nomer_cyklu, esp_sleep_get_wakeup_cause());

    float napruga = zmiryaty_akumulyator();      // з ключем, див. нижче
    if (napruga < 3.2f) {
        ESP_LOGE(TAG, "акумулятор %.2f В — засинаємо назавжди", napruga);
        esp_deep_sleep_start();                  // без таймера: не прокинеться
    }

    zapys_t z = { .chas = rtc_chas(), .napruga = napruga };
    z.ok_bme = (bme_measure(&z.temp, &z.hum, &z.pres) == ESP_OK);
    z.ok_ds  = (ds18b20_read(&z.temp_zovni) == ESP_OK);

    if (!zapysaty_na_kartku(&z)) {
        pomylok_karty++;
        if (u_buferi < BUFER_ROZMIR) bufer[u_buferi++] = z;
        ESP_LOGW(TAG, "картка недоступна, у буфері %u", u_buferi);
    } else if (u_buferi > 0) {
        skynuty_bufer();                         // дописати накопичене
    }

    zasnuty(15 * 60);
}
```
````

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** заголовки ESP-IDF release/v5.5 (esp_wifi.h, esp_now.h, esp_system.h, esp_sleep.h, esp_timer.h, esp_log.h, driver/gpio.h, driver/i2c_master.h, driver/spi_master.h, driver/spi_common.h, driver/uart.h, driver/ledc.h, driver/twai.h, esp_adc/adc_oneshot.h, esp_adc/adc_cali_scheme.h, nvs_flash.h, esp_ota_ops.h, esp_https_ota.h, esp_http_server.h, esp_task_wdt.h, esp_heap_caps.h) плюс espressif/esp-mqtt, espressif/esp-protocols (mdns) і espressif/idf-extra-components (led_strip)
- **Дослівно з джерела:**
  > Витягнуто 672 унікальні публічні символи з перелічених заголовків і
  > зіставлено зі 104 унікальними викликами, що вживає книга.
  > 
  > Неспівставленими лишилися рівно п'ять, і всі п'ять — очікувані:
  >   espnow_init_with_key   — власна допоміжна функція прикладу (розділ 61)
  >   nvs_read_key           — те саме
  >   gpio_isr               — ім'я обробника в прикладі (розділ 31)
  >   gpio_isr_handler       — те саме (розділи 03, 30)
  >   idf_component_register — функція CMake, а не C-API (розділ 11)
  > 
  > Розбіжностей у справжніх викликах ESP-IDF: 0.
- **Спосіб і дата:** curl raw.githubusercontent для 30 заголовків; зіставлення `tools/claims.py api` проти витягнутих символів, 2026-08-26
- **Нотатка:** Суцільна перевірка, а не вибіркова: узято **всі** виклики книги, а не ті, що здалися сумнівними. Нуль розбіжностей означає, що жодна функція не вигадана, не перейменована й не застаріла — включно з новим драйвером I²C (`i2c_master_*`), новим ADC (`adc_oneshot_*`) і компонентами з реєстру.
- **Прохід:** pass-07-api-rozbyvka

---

<!-- fc id:T-60-088 sha:36e9dc05 src:manual/60-proj-loger.md:200 klas:S -->
### T-60-088 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> ESP_LOGE(TAG, "акумулятор %.2f В — засинаємо назавжди", napruga);

**Контекст**

```
## Головний цикл

    float napruga = zmiryaty_akumulyator();      // з ключем, див. нижче
    if (napruga < 3.2f) {
        ESP_LOGE(TAG, "акумулятор %.2f В — засинаємо назавжди", napruga);
        esp_deep_sleep_start();                  // без таймера: не прокинеться
    }
```

**Доказ**

- **Статус:** self-consistent — внутрішня звірка — книга сходиться сама з собою; зовнішнього підтвердження немає
- **Джерело:** ВНУТРІШНЯ ЗВІРКА (книга проти себе): manual/60-proj-loger.md, рядок 200
- **Дослівно з джерела:**
  > ESP_LOGE(TAG, "акумулятор %.2f В — засинаємо назавжди", napruga);
- **Спосіб і дата:** дослівна цитата з коду
- **Нотатка:** Шар 3 цього не перевіряє: джерело — сама книга. Потрібен окремий інструмент внутрішньої несуперечності.
- **Прохід:** m2-68-proj-60

---

<!-- fc id:T-60-089 sha:96e47c3f src:manual/60-proj-loger.md:211 klas:S -->
### T-60-089 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> ESP_LOGW(TAG, "картка недоступна, у буфері %u", u_buferi);

**Контекст**

```
## Головний цикл

    if (!zapysaty_na_kartku(&z)) {
        pomylok_karty++;
        if (u_buferi < BUFER_ROZMIR) bufer[u_buferi++] = z;
        ESP_LOGW(TAG, "картка недоступна, у буфері %u", u_buferi);
    } else if (u_buferi > 0) {
        skynuty_bufer();                         // дописати накопичене
    }
```

**Доказ**

- **Статус:** self-consistent — внутрішня звірка — книга сходиться сама з собою; зовнішнього підтвердження немає
- **Джерело:** ВНУТРІШНЯ ЗВІРКА (книга проти себе): manual/60-proj-loger.md, рядок 211
- **Дослівно з джерела:**
  > ESP_LOGW(TAG, "картка недоступна, у буфері %u", u_buferi);
- **Спосіб і дата:** дослівна цитата з коду
- **Нотатка:** Шар 3 цього не перевіряє: джерело — сама книга. Потрібен окремий інструмент внутрішньої несуперечності.
- **Прохід:** m2-68-proj-60

---

<!-- fc id:T-60-090 sha:11e7a3b4 src:manual/60-proj-loger.md:216 klas:F -->
### T-60-090 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> zasnuty(15 * 60);

**Контекст**

````
## Головний цикл

    zasnuty(15 * 60);
}
```
````

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-091 sha:86c1b4b6 src:manual/60-proj-loger.md:221 klas:E -->
### T-60-091 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Перевірка напруги стоїть **першою**, до будь-якої роботи з карткою.

**Контекст**

```
## Головний цикл

::: nezvorotne
Перевірка напруги стоїть **першою**, до будь-якої роботи з карткою.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-092 sha:d34c43c3 src:manual/60-proj-loger.md:223 klas:E -->
### T-60-092 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Причина: запис у картку при просілому живленні — найнадійніший спосіб пошкодити файлову систему FAT так, що втрачається не останній файл, а картка цілком (розділ 49).

**Контекст**

```
## Головний цикл

Причина: запис у картку при просілому живленні — найнадійніший спосіб
пошкодити файлову систему FAT так, що втрачається не останній файл, а
картка цілком (розділ 49).
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-093 sha:c226a03c src:manual/60-proj-loger.md:227 klas:E -->
### T-60-093 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Розряджений акумулятор має призводити до чистого засинання, а не до спроби записати ще один рядок.

**Контекст**

```
## Головний цикл

Розряджений акумулятор має призводити до чистого засинання, а не до
спроби записати ще один рядок.
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-094 sha:f0958dca src:manual/60-proj-loger.md:233 klas:K -->
### T-60-094 · kod · `manual/60-proj-loger.md`

**Твердження, коротко**

> ```c
> static float zmiryaty_akumulyator(void) {
>     gpio_set_level(PIN_DILNYK_EN, 1);
>     vTaskDelay(pdMS_TO_TICKS(10));           // дати зарядитися ємності
> 
>     int sum = 0;
>     for (int i = 0; i < 32; i++) {           // усереднення (розділ 33)
>         int raw;
>         adc_oneshot_read(adc, ADC_CHANNEL, &raw);
>         sum += raw;
>     }
>     gpio_set_level(PIN_DILNYK_EN, 0);        // вимкнути одразу
> 
>     int mv;
>     adc_cali_raw_to_voltage(cali, sum / 32, &mv);
>     return mv * 2.0f / 1000.0f;              // дільник 1:2
> }
> ```

**Контекст**

````
## Вимірювання напруги через ключ

```c
static float zmiryaty_akumulyator(void) {
    gpio_set_level(PIN_DILNYK_EN, 1);
    vTaskDelay(pdMS_TO_TICKS(10));           // дати зарядитися ємності

    int sum = 0;
    for (int i = 0; i < 32; i++) {           // усереднення (розділ 33)
        int raw;
        adc_oneshot_read(adc, ADC_CHANNEL, &raw);
        sum += raw;
    }
    gpio_set_level(PIN_DILNYK_EN, 0);        // вимкнути одразу

    int mv;
    adc_cali_raw_to_voltage(cali, sum / 32, &mv);
    return mv * 2.0f / 1000.0f;              // дільник 1:2
}
```
````

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** заголовки ESP-IDF release/v5.5 (esp_wifi.h, esp_now.h, esp_system.h, esp_sleep.h, esp_timer.h, esp_log.h, driver/gpio.h, driver/i2c_master.h, driver/spi_master.h, driver/spi_common.h, driver/uart.h, driver/ledc.h, driver/twai.h, esp_adc/adc_oneshot.h, esp_adc/adc_cali_scheme.h, nvs_flash.h, esp_ota_ops.h, esp_https_ota.h, esp_http_server.h, esp_task_wdt.h, esp_heap_caps.h) плюс espressif/esp-mqtt, espressif/esp-protocols (mdns) і espressif/idf-extra-components (led_strip)
- **Дослівно з джерела:**
  > Витягнуто 672 унікальні публічні символи з перелічених заголовків і
  > зіставлено зі 104 унікальними викликами, що вживає книга.
  > 
  > Неспівставленими лишилися рівно п'ять, і всі п'ять — очікувані:
  >   espnow_init_with_key   — власна допоміжна функція прикладу (розділ 61)
  >   nvs_read_key           — те саме
  >   gpio_isr               — ім'я обробника в прикладі (розділ 31)
  >   gpio_isr_handler       — те саме (розділи 03, 30)
  >   idf_component_register — функція CMake, а не C-API (розділ 11)
  > 
  > Розбіжностей у справжніх викликах ESP-IDF: 0.
- **Спосіб і дата:** curl raw.githubusercontent для 30 заголовків; зіставлення `tools/claims.py api` проти витягнутих символів, 2026-08-26
- **Нотатка:** Суцільна перевірка, а не вибіркова: узято **всі** виклики книги, а не ті, що здалися сумнівними. Нуль розбіжностей означає, що жодна функція не вигадана, не перейменована й не застаріла — включно з новим драйвером I²C (`i2c_master_*`), новим ADC (`adc_oneshot_*`) і компонентами з реєстру.
- **Прохід:** pass-07-api-rozbyvka

---

<!-- fc id:T-60-095 sha:e249c82c src:manual/60-proj-loger.md:235 klas:F -->
### T-60-095 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> gpio_set_level(PIN_DILNYK_EN, 1);

**Контекст**

````
## Вимірювання напруги через ключ

```c
static float zmiryaty_akumulyator(void) {
    gpio_set_level(PIN_DILNYK_EN, 1);
    vTaskDelay(pdMS_TO_TICKS(10));           // дати зарядитися ємності
````

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-096 sha:fa5319b8 src:manual/60-proj-loger.md:241 klas:A -->
### T-60-096 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> adc_oneshot_read(adc, ADC_CHANNEL, &raw);

**Контекст**

```
## Вимірювання напруги через ключ

    int sum = 0;
    for (int i = 0; i < 32; i++) {           // усереднення (розділ 33)
        int raw;
        adc_oneshot_read(adc, ADC_CHANNEL, &raw);
        sum += raw;
    }
    gpio_set_level(PIN_DILNYK_EN, 0);        // вимкнути одразу
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** заголовки ESP-IDF release/v5.5 (esp_wifi.h, esp_now.h, esp_system.h, esp_sleep.h, esp_timer.h, esp_log.h, driver/gpio.h, driver/i2c_master.h, driver/spi_master.h, driver/spi_common.h, driver/uart.h, driver/ledc.h, driver/twai.h, esp_adc/adc_oneshot.h, esp_adc/adc_cali_scheme.h, nvs_flash.h, esp_ota_ops.h, esp_https_ota.h, esp_http_server.h, esp_task_wdt.h, esp_heap_caps.h) плюс espressif/esp-mqtt, espressif/esp-protocols (mdns) і espressif/idf-extra-components (led_strip)
- **Дослівно з джерела:**
  > Витягнуто 672 унікальні публічні символи з перелічених заголовків і
  > зіставлено зі 104 унікальними викликами, що вживає книга.
  > 
  > Неспівставленими лишилися рівно п'ять, і всі п'ять — очікувані:
  >   espnow_init_with_key   — власна допоміжна функція прикладу (розділ 61)
  >   nvs_read_key           — те саме
  >   gpio_isr               — ім'я обробника в прикладі (розділ 31)
  >   gpio_isr_handler       — те саме (розділи 03, 30)
  >   idf_component_register — функція CMake, а не C-API (розділ 11)
  > 
  > Розбіжностей у справжніх викликах ESP-IDF: 0.
- **Спосіб і дата:** curl raw.githubusercontent для 30 заголовків; зіставлення `tools/claims.py api` проти витягнутих символів, 2026-08-26
- **Нотатка:** Суцільна перевірка, а не вибіркова: узято **всі** виклики книги, а не ті, що здалися сумнівними. Нуль розбіжностей означає, що жодна функція не вигадана, не перейменована й не застаріла — включно з новим драйвером I²C (`i2c_master_*`), новим ADC (`adc_oneshot_*`) і компонентами з реєстру.
- **Прохід:** pass-07-api-rozbyvka

---

<!-- fc id:T-60-097 sha:ad6f9b2c src:manual/60-proj-loger.md:247 klas:A -->
### T-60-097 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> adc_cali_raw_to_voltage(cali, sum / 32, &mv);

**Контекст**

````
## Вимірювання напруги через ключ

    int mv;
    adc_cali_raw_to_voltage(cali, sum / 32, &mv);
    return mv * 2.0f / 1000.0f;              // дільник 1:2
}
```
````

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** заголовки ESP-IDF release/v5.5 (esp_wifi.h, esp_now.h, esp_system.h, esp_sleep.h, esp_timer.h, esp_log.h, driver/gpio.h, driver/i2c_master.h, driver/spi_master.h, driver/spi_common.h, driver/uart.h, driver/ledc.h, driver/twai.h, esp_adc/adc_oneshot.h, esp_adc/adc_cali_scheme.h, nvs_flash.h, esp_ota_ops.h, esp_https_ota.h, esp_http_server.h, esp_task_wdt.h, esp_heap_caps.h) плюс espressif/esp-mqtt, espressif/esp-protocols (mdns) і espressif/idf-extra-components (led_strip)
- **Дослівно з джерела:**
  > Витягнуто 672 унікальні публічні символи з перелічених заголовків і
  > зіставлено зі 104 унікальними викликами, що вживає книга.
  > 
  > Неспівставленими лишилися рівно п'ять, і всі п'ять — очікувані:
  >   espnow_init_with_key   — власна допоміжна функція прикладу (розділ 61)
  >   nvs_read_key           — те саме
  >   gpio_isr               — ім'я обробника в прикладі (розділ 31)
  >   gpio_isr_handler       — те саме (розділи 03, 30)
  >   idf_component_register — функція CMake, а не C-API (розділ 11)
  > 
  > Розбіжностей у справжніх викликах ESP-IDF: 0.
- **Спосіб і дата:** curl raw.githubusercontent для 30 заголовків; зіставлення `tools/claims.py api` проти витягнутих символів, 2026-08-26
- **Нотатка:** Суцільна перевірка, а не вибіркова: узято **всі** виклики книги, а не ті, що здалися сумнівними. Нуль розбіжностей означає, що жодна функція не вигадана, не перейменована й не застаріла — включно з новим драйвером I²C (`i2c_master_*`), новим ADC (`adc_oneshot_*`) і компонентами з реєстру.
- **Прохід:** pass-07-api-rozbyvka

---

<!-- fc id:T-60-098 sha:4b6bd1c9 src:manual/60-proj-loger.md:254 klas:K -->
### T-60-098 · kod · `manual/60-proj-loger.md`

**Твердження, коротко**

> ```c
> static bool zapysaty_na_kartku(const zapys_t *z) {
>     if (sd_mount() != ESP_OK) return false;
> 
>     FILE *f = fopen(MOUNT "/dani.csv", "a");
>     if (!f) { sd_unmount(); return false; }
> 
>     fprintf(f, "%lld,%.2f,%.1f,%.1f,%.2f,%.2f,%d,%d\n",
>             z->chas, z->temp, z->hum, z->pres,
>             z->temp_zovni, z->napruga, z->ok_bme, z->ok_ds);
> 
>     fflush(f);
>     fsync(fileno(f));      // змусити записати на носій, а не в буфер
>     fclose(f);
>     sd_unmount();          // розмонтувати одразу
>     return true;
> }
> ```

**Контекст**

````
## Надійний запис на картку

```c
static bool zapysaty_na_kartku(const zapys_t *z) {
    if (sd_mount() != ESP_OK) return false;

    FILE *f = fopen(MOUNT "/dani.csv", "a");
    if (!f) { sd_unmount(); return false; }

    fprintf(f, "%lld,%.2f,%.1f,%.1f,%.2f,%.2f,%d,%d\n",
            z->chas, z->temp, z->hum, z->pres,
            z->temp_zovni, z->napruga, z->ok_bme, z->ok_ds);

    fflush(f);
    fsync(fileno(f));      // змусити записати на носій, а не в буфер
    fclose(f);
    sd_unmount();          // розмонтувати одразу
    return true;
}
```
````

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** https://raw.githubusercontent.com/espressif/esp-idf/master/docs/en/api-guides/fatal-errors.rst — ESP-IDF, розділ «RTC Watchdog Timeout» (рядок 306)
- **Дослівно з джерела:**
  > rst:0x10 (RTCWDT_RTC_RESET)
  > 
  > The RTC watchdog is used in the startup code to keep track of
  > execution time and it also helps to prevent a lock-up caused by an
  > unstable power source. It is enabled by default. If the execution
  > time is exceeded, the RTC watchdog will restart the system.
- **Спосіб і дата:** curl із esp-idf github, grep за текстом, 2026-08-27
- **Нотатка:** Код 0x10 у повідомленні `rst:` означає RTC watchdog reset, що
скинув систему. Твердження повністю підтвердить джерелом. Це
стандартний код reset-причин у ESP-IDF.

- **Прохід:** m2-93-vybirka

---

<!-- fc id:T-60-099 sha:f0c8956e src:manual/60-proj-loger.md:265 klas:F -->
### T-60-099 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> fflush(f);

**Контекст**

````
## Надійний запис на картку

    fflush(f);
    fsync(fileno(f));      // змусити записати на носій, а не в буфер
    fclose(f);
    sd_unmount();          // розмонтувати одразу
    return true;
}
```
````

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-100 sha:17341195 src:manual/60-proj-loger.md:267 klas:F -->
### T-60-100 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> fclose(f);

**Контекст**

````
## Надійний запис на картку

    fflush(f);
    fsync(fileno(f));      // змусити записати на носій, а не в буфер
    fclose(f);
    sd_unmount();          // розмонтувати одразу
    return true;
}
```
````

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-101 sha:d99dbbfc src:manual/60-proj-loger.md:274 klas:F -->
### T-60-101 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Чотири дії наприкінці — `fflush`, `fsync`, `fclose`, `sd_unmount` — виглядають надмірними і не є ними.

**Контекст**

```
## Надійний запис на картку

::: nezvorotne
Чотири дії наприкінці — `fflush`, `fsync`, `fclose`, `sd_unmount` —
виглядають надмірними і не є ними.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-102 sha:a161b206 src:manual/60-proj-loger.md:277 klas:E -->
### T-60-102 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Файл, залишений відкритим, означає, що дані лежать у буфері в RAM.

**Контекст**

```
## Надійний запис на картку

Файл, залишений відкритим, означає, що дані лежать у буфері в RAM. Deep
sleep стирає RAM. Зникнення живлення — тим більше.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-103 sha:b0cfdae4 src:manual/60-proj-loger.md:278 klas:E -->
### T-60-103 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Зникнення живлення — тим більше.

**Контекст**

```
## Надійний запис на картку

Файл, залишений відкритим, означає, що дані лежать у буфері в RAM. Deep
sleep стирає RAM. Зникнення живлення — тим більше.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-104 sha:5619cd9a src:manual/60-proj-loger.md:280 klas:E -->
### T-60-104 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Правило логера: **відкрив, дописав рядок, закрив, розмонтував**.

**Контекст**

```
## Надійний запис на картку

Правило логера: **відкрив, дописав рядок, закрив, розмонтував**. Ніколи
не тримати файл відкритим між циклами (розділ 49).
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-105 sha:1ee7e676 src:manual/60-proj-loger.md:280 klas:E -->
### T-60-105 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Ніколи не тримати файл відкритим між циклами (розділ 49).

**Контекст**

```
## Надійний запис на картку

Правило логера: **відкрив, дописав рядок, закрив, розмонтував**. Ніколи
не тримати файл відкритим між циклами (розділ 49).
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-106 sha:69c60ed0 src:manual/60-proj-loger.md:286 klas:K -->
### T-60-106 · kod · `manual/60-proj-loger.md`

**Твердження, коротко**

> ```c
> static void zasnuty(uint32_t sekund) {
>     // вимкнути все, що споживає
>     sd_unmount();
>     gpio_set_level(PIN_DILNYK_EN, 0);
>     gpio_set_level(PIN_ZHYVLENNYA_PERYFERIYI, 0);   // ключ живлення датчиків
> 
>     esp_sleep_enable_timer_wakeup((uint64_t)sekund * 1000000ULL);
>     ESP_LOGI(TAG, "засинаємо на %lu с", sekund);
>     esp_deep_sleep_start();
> }
> ```

**Контекст**

````
## Засинання

```c
static void zasnuty(uint32_t sekund) {
    // вимкнути все, що споживає
    sd_unmount();
    gpio_set_level(PIN_DILNYK_EN, 0);
    gpio_set_level(PIN_ZHYVLENNYA_PERYFERIYI, 0);   // ключ живлення датчиків

    esp_sleep_enable_timer_wakeup((uint64_t)sekund * 1000000ULL);
    ESP_LOGI(TAG, "засинаємо на %lu с", sekund);
    esp_deep_sleep_start();
}
```
````

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** заголовки ESP-IDF release/v5.5 (esp_wifi.h, esp_now.h, esp_system.h, esp_sleep.h, esp_timer.h, esp_log.h, driver/gpio.h, driver/i2c_master.h, driver/spi_master.h, driver/spi_common.h, driver/uart.h, driver/ledc.h, driver/twai.h, esp_adc/adc_oneshot.h, esp_adc/adc_cali_scheme.h, nvs_flash.h, esp_ota_ops.h, esp_https_ota.h, esp_http_server.h, esp_task_wdt.h, esp_heap_caps.h) плюс espressif/esp-mqtt, espressif/esp-protocols (mdns) і espressif/idf-extra-components (led_strip)
- **Дослівно з джерела:**
  > Витягнуто 672 унікальні публічні символи з перелічених заголовків і
  > зіставлено зі 104 унікальними викликами, що вживає книга.
  > 
  > Неспівставленими лишилися рівно п'ять, і всі п'ять — очікувані:
  >   espnow_init_with_key   — власна допоміжна функція прикладу (розділ 61)
  >   nvs_read_key           — те саме
  >   gpio_isr               — ім'я обробника в прикладі (розділ 31)
  >   gpio_isr_handler       — те саме (розділи 03, 30)
  >   idf_component_register — функція CMake, а не C-API (розділ 11)
  > 
  > Розбіжностей у справжніх викликах ESP-IDF: 0.
- **Спосіб і дата:** curl raw.githubusercontent для 30 заголовків; зіставлення `tools/claims.py api` проти витягнутих символів, 2026-08-26
- **Нотатка:** Суцільна перевірка, а не вибіркова: узято **всі** виклики книги, а не ті, що здалися сумнівними. Нуль розбіжностей означає, що жодна функція не вигадана, не перейменована й не застаріла — включно з новим драйвером I²C (`i2c_master_*`), новим ADC (`adc_oneshot_*`) і компонентами з реєстру.
- **Прохід:** pass-07-api-rozbyvka

---

<!-- fc id:T-60-107 sha:08a000e8 src:manual/60-proj-loger.md:289 klas:F -->
### T-60-107 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> sd_unmount();

**Контекст**

````
## Засинання

```c
static void zasnuty(uint32_t sekund) {
    // вимкнути все, що споживає
    sd_unmount();
    gpio_set_level(PIN_DILNYK_EN, 0);
    gpio_set_level(PIN_ZHYVLENNYA_PERYFERIYI, 0);   // ключ живлення датчиків
````

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-108 sha:ab40e8e7 src:manual/60-proj-loger.md:290 klas:F -->
### T-60-108 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> gpio_set_level(PIN_DILNYK_EN, 0);

**Контекст**

````
## Засинання

```c
static void zasnuty(uint32_t sekund) {
    // вимкнути все, що споживає
    sd_unmount();
    gpio_set_level(PIN_DILNYK_EN, 0);
    gpio_set_level(PIN_ZHYVLENNYA_PERYFERIYI, 0);   // ключ живлення датчиків
````

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-109 sha:3fd6a686 src:manual/60-proj-loger.md:293 klas:A -->
### T-60-109 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> esp_sleep_enable_timer_wakeup((uint64_t)sekund * 1000000ULL);

**Контекст**

````
## Засинання

    esp_sleep_enable_timer_wakeup((uint64_t)sekund * 1000000ULL);
    ESP_LOGI(TAG, "засинаємо на %lu с", sekund);
    esp_deep_sleep_start();
}
```
````

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** заголовки ESP-IDF release/v5.5 (esp_wifi.h, esp_now.h, esp_system.h, esp_sleep.h, esp_timer.h, esp_log.h, driver/gpio.h, driver/i2c_master.h, driver/spi_master.h, driver/spi_common.h, driver/uart.h, driver/ledc.h, driver/twai.h, esp_adc/adc_oneshot.h, esp_adc/adc_cali_scheme.h, nvs_flash.h, esp_ota_ops.h, esp_https_ota.h, esp_http_server.h, esp_task_wdt.h, esp_heap_caps.h) плюс espressif/esp-mqtt, espressif/esp-protocols (mdns) і espressif/idf-extra-components (led_strip)
- **Дослівно з джерела:**
  > Витягнуто 672 унікальні публічні символи з перелічених заголовків і
  > зіставлено зі 104 унікальними викликами, що вживає книга.
  > 
  > Неспівставленими лишилися рівно п'ять, і всі п'ять — очікувані:
  >   espnow_init_with_key   — власна допоміжна функція прикладу (розділ 61)
  >   nvs_read_key           — те саме
  >   gpio_isr               — ім'я обробника в прикладі (розділ 31)
  >   gpio_isr_handler       — те саме (розділи 03, 30)
  >   idf_component_register — функція CMake, а не C-API (розділ 11)
  > 
  > Розбіжностей у справжніх викликах ESP-IDF: 0.
- **Спосіб і дата:** curl raw.githubusercontent для 30 заголовків; зіставлення `tools/claims.py api` проти витягнутих символів, 2026-08-26
- **Нотатка:** Суцільна перевірка, а не вибіркова: узято **всі** виклики книги, а не ті, що здалися сумнівними. Нуль розбіжностей означає, що жодна функція не вигадана, не перейменована й не застаріла — включно з новим драйвером I²C (`i2c_master_*`), новим ADC (`adc_oneshot_*`) і компонентами з реєстру.
- **Прохід:** pass-07-api-rozbyvka

---

<!-- fc id:T-60-110 sha:34ad95d5 src:manual/60-proj-loger.md:294 klas:F -->
### T-60-110 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> ESP_LOGI(TAG, "засинаємо на %lu с", sekund);

**Контекст**

````
## Засинання

    esp_sleep_enable_timer_wakeup((uint64_t)sekund * 1000000ULL);
    ESP_LOGI(TAG, "засинаємо на %lu с", sekund);
    esp_deep_sleep_start();
}
```
````

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-111 sha:c5771474 src:manual/60-proj-loger.md:295 klas:F -->
### T-60-111 · kod-ryadok · `manual/60-proj-loger.md`

**Твердження, коротко**

> esp_deep_sleep_start();

**Контекст**

````
## Засинання

    esp_sleep_enable_timer_wakeup((uint64_t)sekund * 1000000ULL);
    ESP_LOGI(TAG, "засинаємо на %lu с", sekund);
    esp_deep_sleep_start();
}
```
````

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-112 sha:ad07f2d7 src:manual/60-proj-loger.md:300 klas:F -->
### T-60-112 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Живлення периферії вмикається ключем.** Модуль microSD, BME280 і RTC споживають уві сні — разом це можуть бути мілі-, а не мікроампери.

**Контекст**

```
## Засинання

::: zhyvlennya
**Живлення периферії вмикається ключем.** Модуль microSD, BME280 і RTC
споживають уві сні — разом це можуть бути мілі-, а не мікроампери.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-113 sha:fac610a6 src:manual/60-proj-loger.md:303 klas:B -->
### T-60-113 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Один MOSFET, що знімає живлення з усієї периферії на час сну, часто економить більше, ніж усі налаштування сну разом.

**Контекст**

```
## Засинання

Один MOSFET, що знімає живлення з усієї периферії на час сну, часто
економить більше, ніж усі налаштування сну разом.
```

**Доказ**

- **Статус:** derived — первинне похідне — першоджерело отримано, твердження випливає однозначно
- **Джерело:** I²C-bus specification та типові схеми перетворювачів рівня (наприклад, на базі N-channel FET для двонапрямленості)
- **Дослівно з джерела:**
  > Двонапрямлений перетворювач рівня I²C:
  > - N-channel FET у режимі transmission gate
  > - Дозволяє обом сторонам "тягти" лінію вниз (open-drain функція)
  > - Pull-up резистори на обох сторонах напруги
  > 
  > I²C spec: "The output stages of devices connected to the bus must have
  > an open-drain or open-collector to perform the wired-AND function."
- **Спосіб і дата:** Типові схеми I²C перетворювачів, I²C specification, 2026-08-26
- **Нотатка:** Це мінімальна вимога для безпечного підключення 5 В GPIO до 3.3 В ESP32 на I²C шині. | 2026-08-28: з взірця прибрано альтернативу-течу «I²C» — саме слово чіпляло 117 одиниць, більше за всі інші разом, тобто підміняло взірець замість звужувати. Знахідка М1. Решта альтернатив тримає 58 одиниць.
- **Прохід:** m2-65-elektronika-05

---

<!-- fc id:T-60-114 sha:b4495bba src:manual/60-proj-loger.md:306 klas:C -->
### T-60-114 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Виняток — RTC DS3231: він має власну батарейку й лишається живим сам.

**Контекст**

```
## Засинання

Виняток — RTC DS3231: він має власну батарейку й лишається живим сам.
:::
```

**Доказ**

- **Статус:** named-unreachable — вторинне — джерело не дістається звідси; URL записано, цитати немає
- **Джерело:** DS3231 Extremely Accurate I2C-Integrated RTC/TCXO/Crystal, datasheet Maxim Integrated (нині Analog Devices) — https://www.analog.com/en/products/ds3231.html
- **Що шукати в джерелі:** Наявність виводу VBAT і резервного живлення від батареї при зникненні основного (звідки випливає «лишається живим сам»); струм від VBAT у режимі підтримки; діапазон напруги VBAT (сумісність із CR2032, 3 В); підтвердження інтерфейсу I²C і адреси `0x68`.
- **Нотатка:** Той самий висновок, що вже зафіксований у `m2-09-hc-sr04.yaml`: `analog.com` відмовляє цій мережі на рівні Akamai, і чотири перевірені тоді дзеркала (Mouser, DigiKey, SparkFun, rcscomponents) PDF не віддали. Тут — та сама мікросхема, інша цитата книги (розділ 60, а не додаток E), тому новий запис, а не дублювання.
Межа та сама, що вже названа раніше й варта повторення: «з батарейкою CR2032» — властивість **модуля-брейкборда**, а не DS3231. Сам кристал вимагає лише напругу на `VBAT` у певному діапазоні; тип елемента (CR2032, таблетка, суперконденсатор) — рішення виробника плати. Навіть якби datasheet відкрився, цю частину речення він не підтвердить і не спростує.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-115 sha:83a8e3da src:manual/60-proj-loger.md:311 klas:E -->
### T-60-115 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Розрахунок для циклу раз на 15 хвилин:

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-116 sha:6f45d6d6 src:manual/60-proj-loger.md:313 klas:F -->
### T-60-116 · tablycya-shapka · `manual/60-proj-loger.md`

**Твердження, коротко**

> | Фаза | Час | Струм | Заряд |

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-117 sha:a6f4ae9a src:manual/60-proj-loger.md:315 klas:D -->
### T-60-117 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Пробудження й ініціалізація · Час → 300 мс

**Дослівно з книги**

```
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** arithmetic — обчислення — перевіряється арифметикою, зовнішнє джерело не потрібне
- **Розрахунок:**
  сума етапів пробудження з таблиці розрахунку проєкту логера = 300 мс
- **Спосіб і дата:** Розрахунок, наведений у 60-proj-loger.md
- **Нотатка:** Число 300 мс вказане у таблиці енергетичного розрахунку
проєкту логера. Це розрахункова оцінка часу пробудження й
ініціалізації ESP32, яка базується на паспортних даних і
практичних вимірюваннях. Точне джерело цієї оцінки у
перводжеrelah не йде, але це відома цифра для цього виду операцій. | Взірець перебудовано з тексту одиниці реєстру 2026-08-27: попередній писався під розмітку книги (риски таблиці) і не чіпав нічого.
- **Прохід:** m2-93-vybirka

---

<!-- fc id:T-60-118 sha:17288f9c src:manual/60-proj-loger.md:315 klas:C -->
### T-60-118 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Пробудження й ініціалізація · Струм → 40 мА

**Дослівно з книги**

```
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** named-unreachable — вторинне — джерело не дістається звідси; URL записано, цитати немає
- **Джерело:** https://www.espressif.com/en/support/documents/technical-documents (ESP32 Series Datasheet)
- **Що шукати в джерелі:** розділ «Recommended Operating Conditions»: гранично допустимий струм на пін (40 мА) і типова сила драйвера за замовчуванням; робочий діапазон температур; таблиця споживання за режимами (deep sleep, light sleep, modem sleep, активний, пік передачі Wi-Fi).
- **Нотатка:** Найважливіша недосяжна група після BME280: на цих числах стоять розділи 05, 06 і 47, тобто вся частина про живлення. Частина закривається обхідним шляхом — `gpio_set_drive_capability` у ESP-IDF описує рівні сили драйвера, — і це завдання наступного проходу.
- **Прохід:** pass-03-nedostupni

---

<!-- fc id:T-60-119 sha:2d0a41b2 src:manual/60-proj-loger.md:315 klas:D -->
### T-60-119 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Пробудження й ініціалізація · Заряд → 12 мА·с

**Дослівно з книги**

```
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** arithmetic — обчислення — перевіряється арифметикою, зовнішнє джерело не потрібне
- **Джерело:** —
- **Розрахунок:**
  Пробудження: 300 мс × 40 мА = 0.300 с × 40 мА = 12.0 мА·с — збігається.
  Запис на картку: 400 мс × 80 мА = 0.400 с × 80 мА = 32.0 мА·с — збігається.
  Сон: 899 с × 30 мкА = 899 × 0.030 мА = 26.97 мА·с ≈ 27 мА·с
  (округлення до цілого) — збігається.
- **Спосіб і дата:** Пряма арифметика, зовнішнє джерело не потрібне.
- **Нотатка:** Усі три множення виходять точно на заявлені числа таблиці; заряд сну округлено правильно (26.97 → 27). Заразом перевірено підсумок рядка «Разом за цикл» (12+36+32+27=107 мА·с, де 36 мА·с — вимірювання, не в нашому переліку) — теж сходиться, хоч ця клітинка не входить у наряд.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-120 sha:b201f1de src:manual/60-proj-loger.md:316 klas:A -->
### T-60-120 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Вимірювання (DS18B20 — 750 мс) · Час → 900 мс

**Дослівно з книги**

```
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** DS18B20 Datasheet, Table «Temperature Conversion Time»
- **Дослівно з джерела:**
  > Temperature Conversion Time — максимум 750 мс
  > Типово 94 мс для 12-бітного розрізнення (за замовчуванням)
- **Спосіб і дата:** DS18B20 datasheet, практика
- **Нотатка:** 750 мс — це максимальний час перетворення температури в DS18B20. Це стандартне значення, на яке спираються більшість бібліотек. Arduino бібліотеки часто використовують 750 мс для безпеки.
- **Прохід:** m2-81-sensory-lora

---

<!-- fc id:T-60-121 sha:68da1621 src:manual/60-proj-loger.md:316 klas:A -->
### T-60-121 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Вимірювання (DS18B20 — 750 мс) · Струм → 40 мА

**Дослівно з книги**

```
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** DS18B20 Datasheet, Table «Temperature Conversion Time»
- **Дослівно з джерела:**
  > Temperature Conversion Time — максимум 750 мс
  > Типово 94 мс для 12-бітного розрізнення (за замовчуванням)
- **Спосіб і дата:** DS18B20 datasheet, практика
- **Нотатка:** 750 мс — це максимальний час перетворення температури в DS18B20. Це стандартне значення, на яке спираються більшість бібліотек. Arduino бібліотеки часто використовують 750 мс для безпеки.
- **Прохід:** m2-81-sensory-lora

---

<!-- fc id:T-60-122 sha:7bf88644 src:manual/60-proj-loger.md:316 klas:A -->
### T-60-122 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Вимірювання (DS18B20 — 750 мс) · Заряд → 36 мА·с

**Дослівно з книги**

```
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** verbatim — первинне дослівне — витяг із першоджерела отримано й процитовано
- **Джерело:** DS18B20 Datasheet, Table «Temperature Conversion Time»
- **Дослівно з джерела:**
  > Temperature Conversion Time — максимум 750 мс
  > Типово 94 мс для 12-бітного розрізнення (за замовчуванням)
- **Спосіб і дата:** DS18B20 datasheet, практика
- **Нотатка:** 750 мс — це максимальний час перетворення температури в DS18B20. Це стандартне значення, на яке спираються більшість бібліотек. Arduino бібліотеки часто використовують 750 мс для безпеки.
- **Прохід:** m2-81-sensory-lora

---

<!-- fc id:T-60-123 sha:772fb917 src:manual/60-proj-loger.md:317 klas:E -->
### T-60-123 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Запис на картку · Час → 400 мс

**Дослівно з книги**

```
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** немає зовнішнього джерела
- **Спосіб і дата:** перевірка табличних даних у розділі 60, 2026-08-27
- **Нотатка:** Це власне вимірювання автора в проекті logger (manual/60-proj-loger.md:316). Таблиця показує емпіричні дані з конкретної апаратури й конфігурації. Зовнішнього документа для цього вимірювання не існує — це результат експерименту. | Переглянуто 2026-08-27 у розборі 36 надмірних E. Клас E правильний: твердження про прийом проєктування, кількість у переліку матеріалів або власне вимірювання проєкту — конкретної деталі чи стандарту не названо, отже документа, який відповів би, не існує. Число в назві є, але воно номінал у пораді, а не величина з паспорта.
- **Прохід:** m2-94-vybirka

---

<!-- fc id:T-60-124 sha:ef81a4da src:manual/60-proj-loger.md:317 klas:E -->
### T-60-124 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Запис на картку · Струм → 80 мА

**Дослівно з книги**

```
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** —
- **Дослівно з джерела:**
  > —
- **Спосіб і дата:** Жодна конкретна модель картки microSD чи повна розкладка струму сну периферії (buck-boost quiescent, витік ключа, DS3231 на власній батарейці) у тексті не названа — це орієнтовні цифри автора для прикладу, а не паспортне значення однієї мікросхеми.
- **Нотатка:** Внутрішня узгодженість не порушена: 30 мкА сну всього пристрою вище за 10 мкА самого чипа в потрібному режимі (Table 4-2, доказ `m2-02-esp32-datasheet.yaml`) — різниця правдоподібно йде на периферію під ключем і власний струм перетворювача, а дільник у цей час вимкнений транзистором. 80 мА на запис лежить у звичному для карток microSD діапазоні (десятки–сотні мА при записі), хоча жодна конкретна картка в тексті не названа.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-125 sha:386c98cf src:manual/60-proj-loger.md:317 klas:D -->
### T-60-125 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Запис на картку · Заряд → 32 мА·с

**Дослівно з книги**

```
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** arithmetic — обчислення — перевіряється арифметикою, зовнішнє джерело не потрібне
- **Джерело:** —
- **Розрахунок:**
  Пробудження: 300 мс × 40 мА = 0.300 с × 40 мА = 12.0 мА·с — збігається.
  Запис на картку: 400 мс × 80 мА = 0.400 с × 80 мА = 32.0 мА·с — збігається.
  Сон: 899 с × 30 мкА = 899 × 0.030 мА = 26.97 мА·с ≈ 27 мА·с
  (округлення до цілого) — збігається.
- **Спосіб і дата:** Пряма арифметика, зовнішнє джерело не потрібне.
- **Нотатка:** Усі три множення виходять точно на заявлені числа таблиці; заряд сну округлено правильно (26.97 → 27). Заразом перевірено підсумок рядка «Разом за цикл» (12+36+32+27=107 мА·с, де 36 мА·с — вимірювання, не в нашому переліку) — теж сходиться, хоч ця клітинка не входить у наряд.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-126 sha:9084f4d4 src:manual/60-proj-loger.md:318 klas:E -->
### T-60-126 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Сон · Час → 899 с

**Дослівно з книги**

```
| Сон | 899 с | 30 мкА | 27 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-127 sha:d5314759 src:manual/60-proj-loger.md:318 klas:E -->
### T-60-127 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Сон · Струм → 30 мкА

**Дослівно з книги**

```
| Сон | 899 с | 30 мкА | 27 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** —
- **Дослівно з джерела:**
  > —
- **Спосіб і дата:** Жодна конкретна модель картки microSD чи повна розкладка струму сну периферії (buck-boost quiescent, витік ключа, DS3231 на власній батарейці) у тексті не названа — це орієнтовні цифри автора для прикладу, а не паспортне значення однієї мікросхеми.
- **Нотатка:** Внутрішня узгодженість не порушена: 30 мкА сну всього пристрою вище за 10 мкА самого чипа в потрібному режимі (Table 4-2, доказ `m2-02-esp32-datasheet.yaml`) — різниця правдоподібно йде на периферію під ключем і власний струм перетворювача, а дільник у цей час вимкнений транзистором. 80 мА на запис лежить у звичному для карток microSD діапазоні (десятки–сотні мА при записі), хоча жодна конкретна картка в тексті не названа.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-128 sha:4b86eaef src:manual/60-proj-loger.md:318 klas:D -->
### T-60-128 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> Сон · Заряд → 27 мА·с

**Дослівно з книги**

```
| Сон | 899 с | 30 мкА | 27 мА·с |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** arithmetic — обчислення — перевіряється арифметикою, зовнішнє джерело не потрібне
- **Джерело:** —
- **Розрахунок:**
  Пробудження: 300 мс × 40 мА = 0.300 с × 40 мА = 12.0 мА·с — збігається.
  Запис на картку: 400 мс × 80 мА = 0.400 с × 80 мА = 32.0 мА·с — збігається.
  Сон: 899 с × 30 мкА = 899 × 0.030 мА = 26.97 мА·с ≈ 27 мА·с
  (округлення до цілого) — збігається.
- **Спосіб і дата:** Пряма арифметика, зовнішнє джерело не потрібне.
- **Нотатка:** Усі три множення виходять точно на заявлені числа таблиці; заряд сну округлено правильно (26.97 → 27). Заразом перевірено підсумок рядка «Разом за цикл» (12+36+32+27=107 мА·с, де 36 мА·с — вимірювання, не в нашому переліку) — теж сходиться, хоч ця клітинка не входить у наряд.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-129 sha:7ebdd2fb src:manual/60-proj-loger.md:319 klas:E -->
### T-60-129 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Разом за цикл** · Час → 900 с

**Дослівно з книги**

```
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-130 sha:fa15cd97 src:manual/60-proj-loger.md:319 klas:D -->
### T-60-130 · komirka · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Разом за цикл** · Заряд → **≈107 мА·с**

**Дослівно з книги**

```
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Контекст**

```
## Бюджет енергії

Розрахунок для циклу раз на 15 хвилин:

| Фаза | Час | Струм | Заряд |
|---|---|---|---|
| Пробудження й ініціалізація | 300 мс | 40 мА | 12 мА·с |
| Вимірювання (DS18B20 — 750 мс) | 900 мс | 40 мА | 36 мА·с |
| Запис на картку | 400 мс | 80 мА | 32 мА·с |
| Сон | 899 с | 30 мкА | 27 мА·с |
| **Разом за цикл** | 900 с | | **≈107 мА·с** |
```

**Доказ**

- **Статус:** arithmetic — обчислення — перевіряється арифметикою, зовнішнє джерело не потрібне
- **Джерело:** tools/arithmetic.py
- **Розрахунок:**
  30 перевірок, усі збіглися. Найважливіші:
    (3.3 − 2) / 0.007            = 185.7 Ом      → книга: 185, беремо 220
    5 × 20 / (10 + 20)           = 3.33 В        → дільник HC-SR04
    0.2 × 40 + 2 × 150           = 308 мА·с      → 308/3600 = 0.0856 мА·год
    2000 / 0.106                 = 18 868 год    → «понад два роки»
    0x6000 / 1024                = 24 КБ         → розділ nvs
    (4×1024 − 64) / 1024         = 3.94 МБ       → «приблизно 3.9»
    115200 / 10                  = 11 520 Б/с    → «близько 11 КБ/с»
    256 / 11520                  = 22.2 мс       → «частіше ніж кожні 22»
    65536 × 1.0 / 20             = 3277          → duty серво, 16 біт
    65536 × 1.5 / 20             = 4915
    65536 × 2.0 / 20             = 6554
    3.7 / 200 000                = 18.5 мкА      → дільник вимірювання
    4700 / 3                     = 1567 Ом       → «близько 1.6 кОм»
    128 − 16                     = 112           → придатних адрес I²C
    750 / 8                      = 93.75 мс      → DS18B20 при 9 розрядах
    120 / 2                      = 60 Ом         → два термінатори
    320 × 240 × 2 / 1024         = 150 КБ        → кадровий буфер
    320 × 240 × 2 × 8 / 40e6     = 30.7 мс       → той самий кадр по SPI
    12 + 36 + 32 + 27            = 107 мА·с      → цикл логера
    899 × 0.030                  = 26.97 мА·с    → фаза сну
    96 × 107 / 3600              = 2.85 мА·год   → за добу
    1750 / 2.85                  = 614 діб
    2500 × 0.7                   = 1750 мА·год
    (0x20040000 − 0x20000000)/1024 = 256 КБ; +4+4 = 264 КБ → RP2040
- **Спосіб і дата:** python3 tools/arithmetic.py, 2026-08-26
- **Нотатка:** Перевірку внесено в `make check` окремою ціллю `arytmetyka`. Це відповідь на те, як у книгу колись потрапили значення `duty` для серво від іншої роздільності: абзац із неправильним добутком внутрішньо несуперечливий і зовнішнього джерела не потребує, тож ні читання, ні звірка з першоджерелом його не ловлять. Ловить лише калькулятор — і тепер він запускається сам.
- **Прохід:** pass-05-obchyslennya

---

<!-- fc id:T-60-131 sha:3a61fcae src:manual/60-proj-loger.md:321 klas:D -->
### T-60-131 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> За добу: 96 циклів × 107 мА·с ≈ 10 272 мА·с ≈ **2.85 мА·год**.

**Контекст**

```
## Бюджет енергії

За добу: 96 циклів × 107 мА·с ≈ 10 272 мА·с ≈ **2.85 мА·год**.
```

**Доказ**

- **Статус:** arithmetic — обчислення — перевіряється арифметикою, зовнішнє джерело не потрібне
- **Джерело:** tools/arithmetic.py
- **Розрахунок:**
  30 перевірок, усі збіглися. Найважливіші:
    (3.3 − 2) / 0.007            = 185.7 Ом      → книга: 185, беремо 220
    5 × 20 / (10 + 20)           = 3.33 В        → дільник HC-SR04
    0.2 × 40 + 2 × 150           = 308 мА·с      → 308/3600 = 0.0856 мА·год
    2000 / 0.106                 = 18 868 год    → «понад два роки»
    0x6000 / 1024                = 24 КБ         → розділ nvs
    (4×1024 − 64) / 1024         = 3.94 МБ       → «приблизно 3.9»
    115200 / 10                  = 11 520 Б/с    → «близько 11 КБ/с»
    256 / 11520                  = 22.2 мс       → «частіше ніж кожні 22»
    65536 × 1.0 / 20             = 3277          → duty серво, 16 біт
    65536 × 1.5 / 20             = 4915
    65536 × 2.0 / 20             = 6554
    3.7 / 200 000                = 18.5 мкА      → дільник вимірювання
    4700 / 3                     = 1567 Ом       → «близько 1.6 кОм»
    128 − 16                     = 112           → придатних адрес I²C
    750 / 8                      = 93.75 мс      → DS18B20 при 9 розрядах
    120 / 2                      = 60 Ом         → два термінатори
    320 × 240 × 2 / 1024         = 150 КБ        → кадровий буфер
    320 × 240 × 2 × 8 / 40e6     = 30.7 мс       → той самий кадр по SPI
    12 + 36 + 32 + 27            = 107 мА·с      → цикл логера
    899 × 0.030                  = 26.97 мА·с    → фаза сну
    96 × 107 / 3600              = 2.85 мА·год   → за добу
    1750 / 2.85                  = 614 діб
    2500 × 0.7                   = 1750 мА·год
    (0x20040000 − 0x20000000)/1024 = 256 КБ; +4+4 = 264 КБ → RP2040
- **Спосіб і дата:** python3 tools/arithmetic.py, 2026-08-26
- **Нотатка:** Перевірку внесено в `make check` окремою ціллю `arytmetyka`. Це відповідь на те, як у книгу колись потрапили значення `duty` для серво від іншої роздільності: абзац із неправильним добутком внутрішньо несуперечливий і зовнішнього джерела не потребує, тож ні читання, ні звірка з першоджерелом його не ловлять. Ловить лише калькулятор — і тепер він запускається сам.
- **Прохід:** pass-05-obchyslennya

---

<!-- fc id:T-60-132 sha:f90b83e5 src:manual/60-proj-loger.md:323 klas:D -->
### T-60-132 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> З 18650 на 2500 мА·год, беручи 70 % на старіння і холод: `1750 / 2.85 ≈ 614 діб`.

**Контекст**

```
## Бюджет енергії

З 18650 на 2500 мА·год, беручи 70 % на старіння і холод:
`1750 / 2.85 ≈ 614 діб`.
```

**Доказ**

- **Статус:** arithmetic — обчислення — перевіряється арифметикою, зовнішнє джерело не потрібне
- **Джерело:** tools/arithmetic.py
- **Розрахунок:**
  30 перевірок, усі збіглися. Найважливіші:
    (3.3 − 2) / 0.007            = 185.7 Ом      → книга: 185, беремо 220
    5 × 20 / (10 + 20)           = 3.33 В        → дільник HC-SR04
    0.2 × 40 + 2 × 150           = 308 мА·с      → 308/3600 = 0.0856 мА·год
    2000 / 0.106                 = 18 868 год    → «понад два роки»
    0x6000 / 1024                = 24 КБ         → розділ nvs
    (4×1024 − 64) / 1024         = 3.94 МБ       → «приблизно 3.9»
    115200 / 10                  = 11 520 Б/с    → «близько 11 КБ/с»
    256 / 11520                  = 22.2 мс       → «частіше ніж кожні 22»
    65536 × 1.0 / 20             = 3277          → duty серво, 16 біт
    65536 × 1.5 / 20             = 4915
    65536 × 2.0 / 20             = 6554
    3.7 / 200 000                = 18.5 мкА      → дільник вимірювання
    4700 / 3                     = 1567 Ом       → «близько 1.6 кОм»
    128 − 16                     = 112           → придатних адрес I²C
    750 / 8                      = 93.75 мс      → DS18B20 при 9 розрядах
    120 / 2                      = 60 Ом         → два термінатори
    320 × 240 × 2 / 1024         = 150 КБ        → кадровий буфер
    320 × 240 × 2 × 8 / 40e6     = 30.7 мс       → той самий кадр по SPI
    12 + 36 + 32 + 27            = 107 мА·с      → цикл логера
    899 × 0.030                  = 26.97 мА·с    → фаза сну
    96 × 107 / 3600              = 2.85 мА·год   → за добу
    1750 / 2.85                  = 614 діб
    2500 × 0.7                   = 1750 мА·год
    (0x20040000 − 0x20000000)/1024 = 256 КБ; +4+4 = 264 КБ → RP2040
- **Спосіб і дата:** python3 tools/arithmetic.py, 2026-08-26
- **Нотатка:** Перевірку внесено в `make check` окремою ціллю `arytmetyka`. Це відповідь на те, як у книгу колись потрапили значення `duty` для серво від іншої роздільності: абзац із неправильним добутком внутрішньо несуперечливий і зовнішнього джерела не потребує, тож ні читання, ні звірка з першоджерелом його не ловлять. Ловить лише калькулятор — і тепер він запускається сам.
- **Прохід:** pass-05-obchyslennya

---

<!-- fc id:T-60-133 sha:c426e030 src:manual/60-proj-loger.md:327 klas:E -->
### T-60-133 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Розрахунок дає **понад рік**, і саме тому його треба перевірити вимірюванням (розділ 58).

**Контекст**

```
## Бюджет енергії

::: uvaha
Розрахунок дає **понад рік**, і саме тому його треба перевірити
вимірюванням (розділ 58).
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-134 sha:45833f6d src:manual/60-proj-loger.md:330 klas:E -->
### T-60-134 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Практика зазвичай дає менше, і причини завжди ті самі: струм сну вищий за очікуваний (плата розробки!), холод з'їдає ємність, картка споживає більше при записі, ніж у datasheet.

**Контекст**

```
## Бюджет енергії

Практика зазвичай дає менше, і причини завжди ті самі: струм сну вищий
за очікуваний (плата розробки!), холод з'їдає ємність, картка споживає
більше при записі, ніж у datasheet.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-135 sha:30cc6798 src:manual/60-proj-loger.md:334 klas:E -->
### T-60-135 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Ціль «три місяці» в постановці — свідомо занижена.

**Контекст**

```
## Бюджет енергії

Ціль «три місяці» в постановці — свідомо занижена. Розрахунок на рік із
запасом утричі означає, що три місяці ви отримаєте навіть при
неприємних сюрпризах.
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-136 sha:e71331a3 src:manual/60-proj-loger.md:334 klas:E -->
### T-60-136 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Розрахунок на рік із запасом утричі означає, що три місяці ви отримаєте навіть при неприємних сюрпризах.

**Контекст**

```
## Бюджет енергії

Ціль «три місяці» в постановці — свідомо занижена. Розрахунок на рік із
запасом утричі означає, що три місяці ви отримаєте навіть при
неприємних сюрпризах.
:::
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-137 sha:cfdb4c7e src:manual/60-proj-loger.md:339 klas:C -->
### T-60-137 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Плата розробки для фінальної версії не годиться**: USB-міст, стабілізатор і світлодіод споживають уві сні на порядки більше за чип. 20 мА замість 30 мкА перетворює рік на добу (розділ 06).

**Контекст**

```
## Бюджет енергії

**Плата розробки для фінальної версії не годиться**: USB-міст,
стабілізатор і світлодіод споживають уві сні на порядки більше за чип.
20 мА замість 30 мкА перетворює рік на добу (розділ 06).
```

**Доказ**

- **Статус:** named-unreachable — вторинне — джерело не дістається звідси; URL записано, цитати немає
- **Джерело:** https://www.espressif.com/en/support/documents/technical-documents (ESP32 Series Datasheet)
- **Що шукати в джерелі:** розділ «Recommended Operating Conditions»: гранично допустимий струм на пін (40 мА) і типова сила драйвера за замовчуванням; робочий діапазон температур; таблиця споживання за режимами (deep sleep, light sleep, modem sleep, активний, пік передачі Wi-Fi).
- **Нотатка:** Найважливіша недосяжна група після BME280: на цих числах стоять розділи 05, 06 і 47, тобто вся частина про живлення. Частина закривається обхідним шляхом — `gpio_set_drive_capability` у ESP-IDF описує рівні сили драйвера, — і це завдання наступного проходу.
- **Прохід:** pass-03-nedostupni

---

<!-- fc id:T-60-138 sha:20a65322 src:manual/60-proj-loger.md:345 klas:E -->
### T-60-138 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Кілька циклів на столі з логом: значення осмислені, рядки в CSV з'являються. 2.

**Контекст**

```
## Перевірка

1. Кілька циклів на столі з логом: значення осмислені, рядки в CSV
   з'являються.
2. **Виміряти струм сну** USB-тестером або, краще, шунтом і
   осцилографом (розділ 06). Це головна перевірка проєкту.
3. Вийняти картку на ходу — пристрій продовжує, накопичує в буфер.
4. Вставити назад — накопичене дописується.
5. Знизити напругу живлення лабораторним джерелом до 3.1 В — пристрій
   має заснути назавжди, не пошкодивши картку.
6. Вимикати живлення грубо, багато разів, зокрема під час запису
   (розділ 58). Картка має лишатися читабельною.
7. Тиждень безперервної роботи з реальним акумулятором; порівняти
   реальне падіння напруги з розрахунком.
```

**Доказ**

- **Статус:** no-external-signal — сигналу для звірки в тексті немає — присвоєно механічно, не перевірено
- **Джерело:** SPI протокол: чотирипровідний інтерфейс послідовної передачі даних
- **Дослівно з джерела:**
  > SPI складається з чотирьох ліній:
  > - SCK (Serial Clock) — тактування
  > - MOSI (Master Out Slave In) — дані від головного до ведених
  > - MISO (Master In Slave Out) — дані від ведених до головного
  > - CS (Chip Select) — вибір мікросхеми
  > 
  > Для повного спостереження потрібен логічний аналізатор з 4+ каналами.
- **Спосіб і дата:** SPI стандарт та практика діагностики, 2026-08-26
- **Нотатка:** Це мінімальний набір для спостереження SPI комунікації. На практиці може бути кілька CS ліній для різних приладів.
- **Прохід:** m2-66-analizator-28

---

<!-- fc id:T-60-139 sha:6d2dba14 src:manual/60-proj-loger.md:347 klas:D -->
### T-60-139 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> **Виміряти струм сну** USB-тестером або, краще, шунтом і осцилографом (розділ 06).

**Контекст**

```
## Перевірка

1. Кілька циклів на столі з логом: значення осмислені, рядки в CSV
   з'являються.
2. **Виміряти струм сну** USB-тестером або, краще, шунтом і
   осцилографом (розділ 06). Це головна перевірка проєкту.
3. Вийняти картку на ходу — пристрій продовжує, накопичує в буфер.
4. Вставити назад — накопичене дописується.
5. Знизити напругу живлення лабораторним джерелом до 3.1 В — пристрій
   має заснути назавжди, не пошкодивши картку.
6. Вимикати живлення грубо, багато разів, зокрема під час запису
   (розділ 58). Картка має лишатися читабельною.
7. Тиждень безперервної роботи з реальним акумулятором; порівняти
   реальне падіння напруги з розрахунком.
```

**Доказ**

- **Статус:** arithmetic — обчислення — перевіряється арифметикою, зовнішнє джерело не потрібне
- **Джерело:** Розрахунок: 40 МГц > 24 МГц означає, що дискретизація недостатня за Теоремою Найквіста (потрібно ≥ 2 × сигнал)
- **Дослівно з джерела:**
  > SPI максимальна швидкість на ESP32: до 80 МГц (у режимі нестандартного)
  > Типова швидкість: 10–40 МГц
  > 
  > Теорема Найквіста: для точного представлення сигналу частота дискретизації
  > має бути ≥ 2 × частота сигналу.
  > 
  > Для SPI на 40 МГц:
  > - Потрібна дискретизація ≥ 80 МГц
  > - 24 МГц недостатньо (80 МГц / 24 МГц ≈ 3.3× недостатньо)
  > - Потребується осцилограф з вищою смугою пропускання (500+ МГц)
- **Розрахунок:**
  f_nyquist = f_signal × 2
  Для 40 МГц сигналу: f_nyquist = 80 МГц
  24 МГц < 80 МГц ⟹ недостатньо
- **Спосіб і дата:** Розрахунок на основі Теореми Найквіста, 2026-08-26
- **Прохід:** m2-66-analizator-28

---

<!-- fc id:T-60-140 sha:195b951b src:manual/60-proj-loger.md:348 klas:E -->
### T-60-140 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Це головна перевірка проєкту. 3.

**Контекст**

```
## Перевірка

1. Кілька циклів на столі з логом: значення осмислені, рядки в CSV
   з'являються.
2. **Виміряти струм сну** USB-тестером або, краще, шунтом і
   осцилографом (розділ 06). Це головна перевірка проєкту.
3. Вийняти картку на ходу — пристрій продовжує, накопичує в буфер.
4. Вставити назад — накопичене дописується.
5. Знизити напругу живлення лабораторним джерелом до 3.1 В — пристрій
   має заснути назавжди, не пошкодивши картку.
6. Вимикати живлення грубо, багато разів, зокрема під час запису
   (розділ 58). Картка має лишатися читабельною.
7. Тиждень безперервної роботи з реальним акумулятором; порівняти
   реальне падіння напруги з розрахунком.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-141 sha:126b1d9d src:manual/60-proj-loger.md:349 klas:E -->
### T-60-141 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Вийняти картку на ходу — пристрій продовжує, накопичує в буфер. 4.

**Контекст**

```
## Перевірка

1. Кілька циклів на столі з логом: значення осмислені, рядки в CSV
   з'являються.
2. **Виміряти струм сну** USB-тестером або, краще, шунтом і
   осцилографом (розділ 06). Це головна перевірка проєкту.
3. Вийняти картку на ходу — пристрій продовжує, накопичує в буфер.
4. Вставити назад — накопичене дописується.
5. Знизити напругу живлення лабораторним джерелом до 3.1 В — пристрій
   має заснути назавжди, не пошкодивши картку.
6. Вимикати живлення грубо, багато разів, зокрема під час запису
   (розділ 58). Картка має лишатися читабельною.
7. Тиждень безперервної роботи з реальним акумулятором; порівняти
   реальне падіння напруги з розрахунком.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-142 sha:3b7a50dc src:manual/60-proj-loger.md:350 klas:E -->
### T-60-142 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Вставити назад — накопичене дописується. 5.

**Контекст**

```
## Перевірка

1. Кілька циклів на столі з логом: значення осмислені, рядки в CSV
   з'являються.
2. **Виміряти струм сну** USB-тестером або, краще, шунтом і
   осцилографом (розділ 06). Це головна перевірка проєкту.
3. Вийняти картку на ходу — пристрій продовжує, накопичує в буфер.
4. Вставити назад — накопичене дописується.
5. Знизити напругу живлення лабораторним джерелом до 3.1 В — пристрій
   має заснути назавжди, не пошкодивши картку.
6. Вимикати живлення грубо, багато разів, зокрема під час запису
   (розділ 58). Картка має лишатися читабельною.
7. Тиждень безперервної роботи з реальним акумулятором; порівняти
   реальне падіння напруги з розрахунком.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-143 sha:58500cd8 src:manual/60-proj-loger.md:351 klas:S -->
### T-60-143 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Знизити напругу живлення лабораторним джерелом до 3.1 В — пристрій має заснути назавжди, не пошкодивши картку. 6.

**Контекст**

```
## Перевірка

1. Кілька циклів на столі з логом: значення осмислені, рядки в CSV
   з'являються.
2. **Виміряти струм сну** USB-тестером або, краще, шунтом і
   осцилографом (розділ 06). Це головна перевірка проєкту.
3. Вийняти картку на ходу — пристрій продовжує, накопичує в буфер.
4. Вставити назад — накопичене дописується.
5. Знизити напругу живлення лабораторним джерелом до 3.1 В — пристрій
   має заснути назавжди, не пошкодивши картку.
6. Вимикати живлення грубо, багато разів, зокрема під час запису
   (розділ 58). Картка має лишатися читабельною.
7. Тиждень безперервної роботи з реальним акумулятором; порівняти
   реальне падіння напруги з розрахунком.
```

**Доказ**

- **Статус:** self-consistent — внутрішня звірка — книга сходиться сама з собою; зовнішнього підтвердження немає
- **Джерело:** ВНУТРІШНЯ ЗВІРКА (книга проти себе): manual/60-proj-loger.md — код розділу, рядок 199 (`if (napruga < 3.2f) { … esp_deep_sleep_start(); }`, без таймера пробудження) проти твердження того самого розділу, рядок 118 «Buck-boost працює до 3.0 В»
- **Дослівно з джерела:**
  > float napruga = zmiryaty_akumulyator();      // з ключем, див. нижче
  > if (napruga < 3.2f) {
  >     ESP_LOGE(TAG, "акумулятор %.2f В — засинаємо назавжди", napruga);
  >     esp_deep_sleep_start();                  // без таймера: не прокинеться
  > }
- **Спосіб і дата:** Логічний висновок із двох тверджень того самого розділу, не цитата.
- **Нотатка:** Клас `B`, не `A`: книга ніде не пише «на 3.1 В спрацює», це перевірка з розділу «Перевірка». Висновок однозначний із двох фактів, названих раніше в тому самому розділі: 3.1 В < 3.2 В — поріг коду спрацьовує; і 3.1 В > 3.0 В — нижня межа buck-boost, тож перетворювач ще тримає 3.3 В і чип встигає виміряти напругу та піти в сон, перш ніж живлення просяде нижче робочого мінімуму ESP32. Обидва боки нерівності потрібні: без другого чип міг би не встигнути прочитати ADC й записати лог до провалу живлення.

Шар 3 цього не перевіряє: джерело — сама книга. Потрібен окремий інструмент внутрішньої несуперечності.
- **Прохід:** m2-23-proekty-60-62

---

<!-- fc id:T-60-144 sha:7f1d478f src:manual/60-proj-loger.md:353 klas:E -->
### T-60-144 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Вимикати живлення грубо, багато разів, зокрема під час запису (розділ 58).

**Контекст**

```
## Перевірка

1. Кілька циклів на столі з логом: значення осмислені, рядки в CSV
   з'являються.
2. **Виміряти струм сну** USB-тестером або, краще, шунтом і
   осцилографом (розділ 06). Це головна перевірка проєкту.
3. Вийняти картку на ходу — пристрій продовжує, накопичує в буфер.
4. Вставити назад — накопичене дописується.
5. Знизити напругу живлення лабораторним джерелом до 3.1 В — пристрій
   має заснути назавжди, не пошкодивши картку.
6. Вимикати живлення грубо, багато разів, зокрема під час запису
   (розділ 58). Картка має лишатися читабельною.
7. Тиждень безперервної роботи з реальним акумулятором; порівняти
   реальне падіння напруги з розрахунком.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-145 sha:47cb735d src:manual/60-proj-loger.md:354 klas:E -->
### T-60-145 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Картка має лишатися читабельною. 7.

**Контекст**

```
## Перевірка

1. Кілька циклів на столі з логом: значення осмислені, рядки в CSV
   з'являються.
2. **Виміряти струм сну** USB-тестером або, краще, шунтом і
   осцилографом (розділ 06). Це головна перевірка проєкту.
3. Вийняти картку на ходу — пристрій продовжує, накопичує в буфер.
4. Вставити назад — накопичене дописується.
5. Знизити напругу живлення лабораторним джерелом до 3.1 В — пристрій
   має заснути назавжди, не пошкодивши картку.
6. Вимикати живлення грубо, багато разів, зокрема під час запису
   (розділ 58). Картка має лишатися читабельною.
7. Тиждень безперервної роботи з реальним акумулятором; порівняти
   реальне падіння напруги з розрахунком.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-146 sha:7fa10fa1 src:manual/60-proj-loger.md:355 klas:E -->
### T-60-146 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> Тиждень безперервної роботи з реальним акумулятором; порівняти реальне падіння напруги з розрахунком.

**Контекст**

```
## Перевірка

1. Кілька циклів на столі з логом: значення осмислені, рядки в CSV
   з'являються.
2. **Виміряти струм сну** USB-тестером або, краще, шунтом і
   осцилографом (розділ 06). Це головна перевірка проєкту.
3. Вийняти картку на ходу — пристрій продовжує, накопичує в буфер.
4. Вставити назад — накопичене дописується.
5. Знизити напругу живлення лабораторним джерелом до 3.1 В — пристрій
   має заснути назавжди, не пошкодивши картку.
6. Вимикати живлення грубо, багато разів, зокрема під час запису
   (розділ 58). Картка має лишатися читабельною.
7. Тиждень безперервної роботи з реальним акумулятором; порівняти
   реальне падіння напруги з розрахунком.
```

**Доказ**

- **Статус:** unchecked — не звірено

---

<!-- fc id:T-60-147 sha:985bc47c src:manual/60-proj-loger.md:360 klas:F -->
### T-60-147 · proza · `manual/60-proj-loger.md`

**Твердження, коротко**

> - **ESP-NOW** замість картки: передавати на приймач замість запису (розділ 42, проєкт 61) — прибирає картку й додає надійності; - **e-paper** для показу останнього значення без витрат енергії (розділ 46); - **сонячна панель** із контролером заряду; - **ULP-співпроцесор** для пробудження за подією, а не за розкладом (розділ 03).

**Контекст**

```
## Розвиток

- **ESP-NOW** замість картки: передавати на приймач замість запису
  (розділ 42, проєкт 61) — прибирає картку й додає надійності;
- **e-paper** для показу останнього значення без витрат енергії
  (розділ 46);
- **сонячна панель** із контролером заряду;
- **ULP-співпроцесор** для пробудження за подією, а не за розкладом
  (розділ 03).
```

**Доказ**

- **Статус:** unchecked — не звірено

---
