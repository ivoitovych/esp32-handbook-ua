# Суцільний прохід — k11-nikoly.
#
# Посаджено `tools/prochid_posadka.py`. Сюди потрапляє лише
# те, що пережило третій шар: цитата знайшлася в названому
# документі дослівно. Заявок без такої перевірки тут немає.
#
# Взірці виведено механічно — найкоротший префікс, відмітний
# у всьому реєстрі. Це компроміс між двома законами:
# широкий взірець бреше, довгий ламається.




- nazva: 'T-K11-008: **Не вмикати Flash Encryption і Secure Boot «щоб'
  zbih: \*\*Не\s+вмикати\s+Flash\s+Encryption
  klas: A
  dzherelo: https://raw.githubusercontent.com/espressif/esp-idf/master/docs/en/security/flash-encryption.rst
  cytata: 'For :ref:`flash-enc-release-mode`, the second stage bootloader sets all the eFuse
    bits set under development mode as well as ``DIS_DOWNLOAD_MANUAL_ENCRYPT``. It also write-protects
    the ``{IDF_TARGET_CRYPT_CNT}`` eFuse bits.

    '
  sposib: Source document retrieved 2026-08-27 and the quote verified against it by substring
    match. Status `verbatim` means the document was obtained and the quote is exact — it does
    **not** mean a maintainer read the passage and agreed. That judgement is separate work.
  notatka: write-protect в release-режиме означает невозможность изменения этих параметров
    после активации
  title: 'T-K11-008: **Не вмикати Flash Encryption і Secure Boot «щоб'
  match: \*\*Не\s+вмикати\s+Flash\s+Encryption
  status: verbatim
  source: https://raw.githubusercontent.com/espressif/esp-idf/master/docs/en/security/flash-encryption.rst
  quote: 'For :ref:`flash-enc-release-mode`, the second stage bootloader sets all the eFuse
    bits set under development mode as well as ``DIS_DOWNLOAD_MANUAL_ENCRYPT``. It also write-protects
    the ``{IDF_TARGET_CRYPT_CNT}`` eFuse bits.

    '
  method: Source document retrieved 2026-08-27 and the quote verified against it by substring
    match. Status `verbatim` means the document was obtained and the quote is exact — it does
    **not** mean a maintainer read the passage and agreed. That judgement is separate work.
  note: write-protect в release-режиме означает невозможность изменения этих параметров после
    активации
