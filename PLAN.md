# План до друкованого стану

**Це робочий файл, а не опис наміру.** Кожна ітерація: узяти перший
незакритий пункт, зробити його, поставити позначку, закомітити,
надіслати. Далі — наступний пункт без паузи й без питань.

Якщо пункт виявився більшим, ніж на одну ітерацію, — розбити його тут
на підпункти й робити перший. Якщо пункт заблокований — записати чим,
пропустити, узяти наступний.

Стан оновлюється **в цьому файлі**, а не в пам'яті сесії: будь-яке
пробудження починається з читання цього переліку.

---

## Черга

### A. Фактчекінг — частка М1

- [x] A1. Тріаж: строгий сигнал для комірок і прози (E +660)
- [x] A2. Поділ роботи М1/М2 — `tools/podil.py`, `factcheck/PODIL.md`
- [x] A3. Кошик `komandy` (прохід 28)
- [x] A4. Кошик `log` і решта `komandy` (прохід 29)
- [x] A5. Кошик `piny` (прохід 30)
- [x] A6. Кошики `adresy` й `api` (прохід 31)
- [ ] **A7. Залишок частки М1 — 145 одиниць.** Робочий пул із трьох
      агентів (Sonnet, середнє зусилля). Черга шматків нижче.

#### A7. Пул: що робить агент і чого не робить

Поділ праці тут не за темами, а **за тим, де живе небезпека**.

Агент **дістає й звіряє**: качає першоджерело, читає його цілком проти
переліку тверджень, повертає структурований звіт — дослівна цитата на
кожне підтверджене, розбіжність із наслідком, прогалина, або чесне
«потрібен недосяжний документ».

Агент **не пише** ані доказів, ані взірців, ані класів, і взагалі нічого
не змінює в репозиторії. Причина конкретна: за цей проєкт **тричі**
широкий взірець мовчки позначив «звірено» те, чого не звіряв
(`ESP-IDF|esptool` на 173 одиниці, `службов` на задачі FreeRTOS,
велика літера на початку речення). Ця відмова невидима, і ловить її лише
аудит `sketch -v` у контексті, який пам'ятає всі три випадки.

Клас доказу — так само за М1. `A` означає «цитату справді отримано в цій
сесії», і межу між `A` і `B` тримає той, хто відповідає за реєстр.

Наслідок для чергування: агент звільнився → віддати наступний шматок.
Інтеграція (доказ + взірець + аудит + коміт) робиться в основній сесії
пакетами, а не на кожен звіт.

#### A7-б. Пул на п'ять послідовностей (Haiku)

Третій шар зняв вимогу до сили моделі: цитату перевіряє скрипт, тож
збирання віддано найдешевшій. Помічник тепер віддає **YAML**, а не
звіт прозою, і `tools/citaty.py <файл>` звіряє його **до** того, як
щось потрапляє в реєстр.

| Посл. | Що | Бунчів | Одиниць |
|---|---|---|---|
| S1 | команди esptool та idf.py | 3 | 30 |
| S2 | номери GPIO, шість сімейств | 4 | 31 |
| S3 | API, адреси, рядки логу | 3 | 24 |
| S4 | ремонт цитат, перша половина | 3 | 25 |
| S5 | ремонт цитат + бібліотеки додатка E | 4 | 35 |

Один агент — одна послідовність, бунчі підряд від початку до кінця.

**Результат першого прогону.** 143 записи від помічників, третій шар
пропустив **94 дослівні цитати**. Відмови рівно передбачені: власна
проза в полі цитати, каталог замість файлу, вгаданий шлях у дереві.
Жодна не вимагала розуміння предмета, щоб її помітити.

Найдорожче зловлене — **у моєму власному доказі**: `pass-31` наводив
зсуви кодів помилок OTA як `+0x02` і `+0x04`, тоді як у заголовку
`+0x01` і `+0x03`. Я відтворив рядок із пам'яті замість скопіювати, і
клас при цьому стояв `A`. Книга не постраждала (вона називає ці
помилки іменами), але доказ брехав п'ять проходів поспіль, і побачила
це найдешевша модель із набору.

#### A7. Черга шматків

| # | Шматок | Файли | ~одиниць | Стан |
|---|---|---|---|---|
| 1 | TWAI/CAN і логування | `38-can`, `25-log` | 13 | **закрито** |
| 2 | Розділи флешу, серійна, купа | `18-rozdily`, `21-seriyna`, `30-struktura` | 15 | **закрито** |
| 3 | JTAG і периферія з коду | `27-jtag`, `33-peryferiya-kod` | 11 | **закрито** |
| 4 | Розділ GPIO суцільно | `07-gpio` | 19 | **закрито** |
| 5 | Проєкти 60 і 62 | `60-proj-loger`, `62-proj-keruvannya` | 16 | **закрито** |
| 6 | esptool і завантаження | `17-esptool`, `16-boot` | 15 | **закрито** |
| 7 | Тулчейн і паніка | `11-idf`, `dodatky/d-panik` | 12 | **закрито** (прохід 35) |
| 8 | Решта дрібними хвостами | усе інше з `PODIL.md` | ~44 | **закрито** (прохід 36) |
| 9 | Команди esptool і idf.py | `naryad-komandy` | 30 | **звітовано** (прохід 38) |
| 10 | Номери GPIO | `naryad-piny` | 31 | **звітовано** (прохід 38) |
| 11 | API, адреси, лог | `naryad-hvist` | 24 | **звітовано** (прохід 38) |

Пул зупинено: нових шматків не роздано. «Звітовано» ≠ «закрито» —
у книгу внесено лише те, що М1 звірив власноруч; решта чекає в
`factcheck/PEREVIRYTY.md`.
- [ ] A12. **51 розбіжність шару 3** — `factcheck/CYTATY.md`. Цитата не
      стоїть за названою адресою: переказ, переніс рядка, з'їдена
      розмітка RST, змінене значення в джерелі або не той файл.
      Роздано пулу (S4, S5) на полагодження.
- [ ] A11. **Повторна перевірка звітів пулу** — `factcheck/PEREVIRYTY.md`.
      Три звіти на 85 одиниць; у реєстр пішло п'ять доказів, які М1
      звірив сам. Решта — черга, не покриття. Порядок розбору
      записано у файлі; перший пункт `T-60-074`, бо там, схоже,
      «я не знайшов» подано як «джерело недосяжне».
- [ ] A9. **71 мертва альтернатива** — знайдено новим аудитом
      (прохід 37). Переважно взірці, що відстали від правки книги.
      Розбирати поступово, поза чергою кошиків.
- [ ] A10. **П'яте місце помилок: книга суперечить сама собі.**
      Знахідка М2 про BMP280 — розділ 45 казав правильно, додаток E
      протилежне, і жоден інструмент цього не бачив. Реєстр звіряє
      книгу з джерелами, не книгу з книгою. Потрібна механічна звірка
      довідкових таблиць проти тексту розділів. Як — поки не знаю.
- [ ] A8. Другий тріаж: 1924 одиниці слабкого сигналу. Розібрати
      вибіркою по 50, визначити частку справді редакційного, і якщо
      вона висока — розширити строгий тест ще раз.

### B. Те, що справді спиняє друк

- [x] **B1. Правописна перевірка** — `tools/pravopys.py`, spylls +
      словник `uk_UA` з LibreOffice у кеші, власний словник проєкту
      в `docs/slovnyk-proyektu.txt`. 50 482 слововживання, 132
      невідомих слова (0.4 %). Знайдено й виправлено `переход` →
      `перехід`. Решта — на вичитку людині: судити про українську
      має вона, інструмент лише скоротив їй роботу.
- [ ] **B2. Предметний покажчик.** Механічно витягти кандидатів
      (терміни, чипи, команди, піни), відсіяти вручну, зверстати.
      Книга на 413 сторінок без покажчика на папері майже некерована.
- [ ] B3. 413 → 416 сторінок. Кратність 16 — вимога друкарні.
      Свідомий вміст або свідомі порожні, не випадковість.
- [x] B0. Відтворюваність збирання: виготовлювач у `BUILD.txt`,
      версії в `toolchain-baseline.yaml`, попередження в `pdf-smoke`.
- [ ] B5. Хук `pre-commit` проти нових двійкових поза `release/`
      (третій шар захисту Р-КЕШ, за пропозицією М2).
- [ ] B4. Перевірка PDF на друкарські вимоги: вбудовані шрифти (є),
      колірний простір, поля під палітурку (є), відсутність
      прозорості.

### C. Поза межами того, що робить М1

- [ ] C1. **Вичитка українською, 413 сторінок.** Людина. Критичний
      шлях. Інструмент не заміняє.
- [ ] C2. Обкладинка: перед, спинка, корінець. Ширина корінця
      залежить від паперу — після розмови з друкарнею.
- [ ] C3. ISBN / УДК / ББК — за рішенням власника: приватний тираж їх
      не потребує, публікація потребує.
- [ ] C4. Друкарня: папір, палітурка, пробний примірник.

### D. Постійне, у кожній ітерації

- [x] D1. Прочитати гілку М2: `git fetch`, нові повідомлення в
      `zvyazok/`, нові докази.
- [x] D2. Відповісти на кожну знахідку М2 окремим `rishennya`.
      Ітерація 1: шість знахідок закрито, відкритих нуль.
- [ ] D3. `make check` чисто перед кожним комітом.
- [ ] D4. Надіслати на `origin`.

---

## Правила ітерації

1. **Одна ітерація — один закритий пункт і один коміт.** Не більше:
   великий коміт ховає помилку. Не менше: порожня ітерація марна.
2. **Перед пунктом — `git fetch` і листування.** Знахідка М2, що
   лежить непрочитаною, дорожча за будь-який пункт черги.
3. **Після пункту — позначка тут.** Файл комітиться разом із роботою.
4. **Не питати дозволу на наступний пункт.** Черга і є дозвіл.
5. **Заблоковане не тримає чергу.** Записати причину, взяти наступне.

## Критерій зупинки

Черга порожня, крім `C`, — тобто лишилося тільки те, що робить людина.
Тоді останній коміт і звіт власникові про стан кожного пункту `C`.

Фактчекінг **не** входить у критерій: він не має природного нуля й
друк не спиняє. Він іде доти, доки є досяжні джерела.
