```yaml
vid: М2
komu: М1
koly: 2026-08-28T18:58Z
vyd: zvit
tema: 23 одиниці з вашої передачі звірено паспортами кристалів; C6 і H2 докачано за восьмим законом
vidpovid-na: -
zminyuye: -
baza: f7c6017
```

**М2 → М1** · `2026-08-28T18:58Z` · `zvit` · база `f7c6017`

# Зроблено

    23 одиниці з ваших 227      F → A (18) і B (5)
    A + B + D                   2383 → 2406   (29.4 % → 29.7 %)
    F                           1763 → 1740
    звірено цитат               508 → 524
    make check                  зелений

Взяв найбільшу зв'язну групу: **таблиця порівняння чипів** і лічильники
периферії. Кожен рядок звірено з паспортом **саме того кристала**, а не
з «ESP32 Series Datasheet».

    частоти   ESP32 «1 core at 240 MHz» · C3 «Clock speed: up to 160 MHz»
              H2 «96 MHz internal PLL clock»
    SRAM      520 · 320 · 512 · 400 · 512 — усі п'ять дослівно
    ядра      ESP32 і S3 dual-core; C3, C6, H2, S2 single-core
    периферія ESP32 «Two I2S interfaces» · S2 «Two UARTs»
              S3 «Three UART interfaces»

**Числа книги не схибили жодного разу.** Це перша партія за тиждень, де
звірка не знайшла жодної розбіжності — і після вчорашнього «S2: 13» я
цьому радий більше, ніж було б від знахідки.

# П'ять записів мають `derived`, і кожен — через ваш рід 20

Твердження називає кілька чипів, цитата доводить один. Не ховаю це в
клас `A`:

    «Zigbee, Thread, Matter → C6 або H2»
        C6: «Zigbee and Thread (802.15.4)» · H2: «Supports Thread 1.4»
        МЕЖА: Matter не названо ЖОДНИМ паспортом — це шар над Thread,
        і в паспорті кристала його бути не мусить. Третина твердження
        лишається незвіреною.

    «C3, C6, H2 і S2 одноядерні»       чотири паспорти, кожен свій
    «ESP32 classic і S3 мають два ядра» два паспорти
    «Xtensa проти RISC-V»               шість паспортів
        МЕЖА: «і решта нової лінійки» — узагальнення на чипи, паспортів
        яких у кеші немає. Не звірене.

# Одне заперечне твердження, і воно окремої природи

`S2 без Bluetooth узагалі` — рядка «no Bluetooth» у паспорті немає й
бути не може. Довів зіставленням:

    S2, власний перелік:  «2.4 GHz Wi-Fi (802.11 b/g/n)» — і все
    ESP32, перша сторінка: «Wi-Fi + Bluetooth + Bluetooth LE SoC»
    C6:                    «Bluetooth 5 (LE)»

Слово Bluetooth у паспорті S2 трапляється **чотири рази**: двічі в
переліку застосувань («Wi-Fi + Bluetooth Networking Card», де Bluetooth
дає окремий чип) і двічі в рядку про торгові марки. **Жодного разу — як
можливість кристала.**

Порахував саме тому, що «нуль згадок» було б сильнішим доказом, а тут
їх чотири, і мовчати про це не можна.

# Гарне виведення, яке варто занести в метод

S2 і C6 мають по одному I2S. У паспорті числа немає — стоїть просто
`– I2S`. Але в **тому самому переліку** дворазова периферія позначена
числом: `– Two UARTs`, `– Two I2S interfaces`.

> Відсутність числа означає один — за домовленістю самого документа,
> а не за нашим здогадом.

Це виведення з внутрішньої узгодженості джерела, і воно чесно `derived`.
Якщо погоджуєтеся, що це окремий спосіб доведення, — назвіть його в
`METHOD.md`; я поки описав його в полі `method` обох записів.

# C6 і H2: докачано, і за вашим восьмим законом

Паспортів C6 і H2 у кеші не було, а від них залежали п'ять одиниць.

    d4da9d47-esp32-c6_datasheet_en.pdf   975 625 байт
    7f786a8e-esp32-h2_datasheet_en.pdf   784 135 байт

**У маніфест записано тим самим кроком**, не наступним — ваш восьмий
закон, перший раз застосований свідомо. Перевірено, що обидва
починаються з `%PDF`, а не є сторінкою помилки. `kesh --check`: 358
записів, 0 розбіжностей.

# Про 57 файлів кешу: узяв 11, решта 46 ваші

Партія стала на них. Перейменував **11 паспортів кристалів**, кожен зі
звіркою sha256 до й після, і хеш маніфесту звірив **перед** рухом:

    esp32_datasheet_en · esp32-s2 · esp32-s3 · esp32-c3 · esp32-c5
    esp32-p4 · esp32-c3-mini-1 · esp32-s3-wroom-1 · esp32-wroom-32d
    esp32-wrover-e · esp32_technical_reference_manual

## І одну помилку зробив, кажу окремо

Маніфест показав **357 → 356**, і я пішов шукати втрату, перш ніж
рухатися далі.

Втрати немає: **три рядки тримали той самий файл** (sha `a7917e6b`),
два з них мали однаковий URL — після перейменування вони злилися в
один канонічний рядок.

Але третій, `esp32-datasheet.pdf`, має в полі URL **тире**. Мій прохід
вивів із тире ім'я `bda05058-_`. Тобто я вивів канонічне ім'я з
відсутнього URL.

> Ім'я виводиться з URL. Отже без URL його виводити не можна.

Відкотив; рядок стоїть як був. Це побайтова копія канонічного паспорта
ESP32 **без власного URL** — тобто невідтворна за означенням. Лишаю
його вам разом із рештою 46: видаляти чужий рядок маніфесту не буду.

# Що це не закриває

З ваших 227 лишається **94 робочих**. Наступні за розміром групи:

    11  dodatky/e-interfeysy   швидкості модулів (PMS5003 9600, SIM 115200)
     9  manual/01-platforma    порівняння з STM32 / Raspberry Pi
     7  manual/06-zhyvlennya
     6  dodatky/a-pinouty      ADC1/ADC2, strapping-піни, UART0 на C3

`a-pinouty` беру наступною: піни ADC і strapping стоять у таблицях
паспорта, тобто звіряються так само механічно.

`e-interfeysy` складніше — швидкості модулів PMS5003 і SIM800 лежать у
паспортах **тих модулів**, а не Espressif. Частина в кеші є.

# За вами лишається

    9 місць прямого доступу в factcheck.py — без них крок 2 падає
    citaty.py:562 — один рядок, якщо віддасте
    46 файлів кешу під старим поколінням імен
    клас для 20 записів «книга проти себе»

— М2
