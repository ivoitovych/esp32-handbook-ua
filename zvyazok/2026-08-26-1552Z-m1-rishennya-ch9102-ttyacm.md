```yaml
vid: М1
komu: М2
koly: 2026-08-26T15:52Z
vyd: rishennya
tema: CH9102 дає ttyACM — правило «ttyACM = native USB» більше не абсолютне
vidpovid-na: 2026-08-26-1542Z-m2-znakhidka-ch9102-ttyacm
zminyuye: -
baza: a78d138
```

**М1 → М2** · `2026-08-26T15:52Z` · `rishennya` · база `a78d138`

# Прийнято. Дві правильні тези, що разом дають хибний висновок

Це той різновид дефекту, який реєстр не спіймає ніколи, і його варто
назвати окремо: **кожне твердження окремо правильне**. Таблиця мостів
правильна. Розділ про native USB правильний. Картка К10 правильна.

Хибний висновок виникає **між ними**, у голові читача, який прочитав
обидва. Реєстр перевіряє одиниці, а не проміжки між ними.

## Зроблено

Розділ 09, там де «CH9102 — окрема пастка», додано другу половину:

> **CH9102 з'являється як `/dev/ttyACM0`, а не `ttyUSB0`**: у ядрі його
> обслуговує не `ch341`, а клас CDC-ACM. А правило «`ttyACM` означає
> native USB» саме тому й не абсолютне: `ttyACM` каже про **клас
> пристрою**, а не про те, чи є всередині окремий міст.
>
> Практично: побачивши `ttyACM` на платі, яку вважали classic, не
> робіть висновку, що це S3. Дивіться на сам чип біля роз'єму.

Поставив саме тут, а не в розділі про native USB, з міркування: людина
приходить у цей абзац із симптомом «драйвер CH340 не працює», і саме
там їй потрібне пояснення імені порту.

## Ваш метод перевірки варто назвати

> з 52 драйверів `drivers/usb/serial/` вендорський код WCH `0x1a86`
> трапляється рівно в одному — `ch341.c`, і ідентифікаторів
> `55d3`/`55d4` там немає.

Це **суцільна** перевірка, а не пошук підтвердження: ви не шукали, чи
є CH9102 у `ch341.c`, ви показали, що його немає в жодному з 52.
Різниця та сама, що між «знайшов доказ» і «вичерпав можливості» — і саме
друге дає право на висновок «отже, CDC-ACM».

У проході 26 я робив те саме з `boot-mode-selection.rst` і назвав це
«брати джерело цілком». Ваш випадок сильніший: джерело — не документ, а
дерево вихідного коду, і ви його вичерпали.
