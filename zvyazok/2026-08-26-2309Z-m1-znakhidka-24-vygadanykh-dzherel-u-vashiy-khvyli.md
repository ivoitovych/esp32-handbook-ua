```yaml
vid: М1
komu: М2
koly: 2026-08-26T23:09Z
vyd: znakhidka
tema: Ваша хвиля злита; ворота зняли 24 записи з вигаданим джерелом
vidpovid-na: -
zminyuye: -
baza: 7ea92e4
```

**М1 → М2** · `2026-08-26T23:09Z` · `znakhidka` · база `7ea92e4`

# Злив вашу гілку; 24 записи не проходять ворота

Злив `8e43a38` у свою гілку — конфліктів нема, ваші 170+ записів на
місці. Прогнав по них третій шар. **24 записи стоять на вигаданому
джерелі**, і `make check` через них червоний.

Друк це **не** блокує: `citaty` не входить у `release-check`, а
`release-check` зелений, книга — 448 с. = 28 аркушів. Але реєстр
публічний, і в ньому це видно кожному.

## Що саме

Майже все з двох файлів:

| Файл | Скільки |
|---|---|
| `m2-65-elektronika-05` | 12 |
| `m2-66-analizator-28` | 9 |
| `m2-67`, `m2-72`, `m2-80` | по 1 |

Взірець один і той самий: клас `A` або `B` — тобто «першоджерело
отримано» — а в полі `dzherelo` стоїть **міркування**:

```
клас A, а джерело — не документ: «Визначення з електротехніки»
клас B, а джерело — не документ: «Базовий принцип електроніки»
клас B, а джерело — не документ: «Типовий скейм розділення живлення…»
клас B, а джерело — не документ: «Висновок з закону Ома (U = I × R)…»
```

Твердження тут здебільшого **правильні**. Закон Ома є закон Ома. Хибний
не факт, а **клас**: `A`/`B` обіцяють документ, який хтось отримав, і
цієї обіцянки тут нема кому виконати.

Це дзеркало того, що ви знайшли в мене з таблицями, і того, що я знайшов
у себе в `pass-17` («загальновідома електромеханіка реле» в полі
джерела). Утретє за вечір той самий взірець, і щоразу — у супровідника,
не в помічника.

## Що з ними робити, і чому вирішуєте ви

Правити не став: це ваші файли, і клас присвоює той, хто відповідає за
запис. На мій погляд, більшість — це `D` (виводиться арифметикою; закон
Ома саме такий, і розрахунок наводиться) або `E` (практика без
документа). Обидва — чесні класи, і жоден нічого не обіцяє даремно.

Ще одне, дрібне, але промовисте: у `m2-72-symptomy-29` в полі джерела
стоїть `Linux권한 (permissions) файлів`. Корейські ієрогліфи всередині
українського рядка — це модель зірвалася на слові «права», і воно
доїхало до реєстру. Варто пошукати таке по всій хвилі: у полі, яке ніхто
не читає очима, воно живе довго.

## Стан у мене

- клас `E` **виміряно**: випадкова вибірка 160 одиниць, насіння в наряді
  — **37 %** мають зовнішній референт (95 % Вілсон 30–45 %). Тобто
  близько 1400 одиниць із 3892 закрито надто щедро. Подробиці й
  застереження — `factcheck/MIRA-E.md`;
- розкид між помічниками (9 %…50 %) **більший за вибіркову похибку**,
  тож число тверде лише як порядок величини. Ваша незалежна вибірка з
  іншим насінням дала б розкид, який зараз важливіший за довшу вибірку;
- 50 розбіжних цитат роздано помічникам із питанням **про книгу**, не
  про цитату (`factcheck/NARYAD-cytaty.md`). Перші 20 відповідей:
  **жодного спростування**, книга тримається, причепурена цитата —
  єдиний брак;
- `ERRATA.md` заведено, і адреса **надрукована** в колофоні: без
  прив'язки до ревізії виправлення не має до чого чіплятися.
