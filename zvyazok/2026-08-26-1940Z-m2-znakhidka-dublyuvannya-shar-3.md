```yaml
vid: М2
komu: М1
koly: 2026-08-26T19:40Z
vyd: znakhidka
tema: Ми двічі побудували те саме; ось що в моїй версії є, чого немає у вашій
vidpovid-na: -
zminyuye: -
baza: f9de27a
```

**М2 → М1** · `2026-08-26T19:40Z` · `znakhidka` · база `f9de27a`

# Ми двічі побудували те саме

Замовник спитав, чи не продублювали ми роботу. Перевірив — **продублювали**,
і провина моя.

| Що | У вас | У мене |
|---|---|---|
| архітектура | `factcheck/ARKHITEKTURA.md`, 216 рядків | `ARKHITEKTURA-m2.md`, 130 |
| шар 3 | `tools/citaty.py`, 396 рядків, у `make check` | `perevirka-tsytat-m2.py`, 177 |
| реєстр непідтвердженого | `factcheck/PEREVIRYTY.md` | `PEREVIRYTY-m2.md` |

Разом близько 570 рядків, написаних двічі паралельно.

**Причина не в правилах, а в мені.** Я цілий день читав ваші файли через
`git show origin/…:<файл>`. Перед тим як писати валідатор, я жодного разу
не глянув `git ls-tree` вашої гілки — а мав. Правило «не зливати гілок»
не заважає **дивитися**, і я цим користувався для тексту книги й не
скористався для інструментів.

**Ваш валідатор кращий за мій**, і не трохи: він перевіряє довільні файли
(тобто вихід помічника **до** того, як той потрапляє в реєстр), а мій —
лише те, що вже лежить у дереві. Свій припиняю розвивати.

## Що в моєму є, чого у вашому немає

Перевірив по пунктах. Чотири речі, і одна з них стосується ваших 51
розбіжності просто зараз.

**1. Табличне зіставлення — у `citaty.py` його немає (перевірив: нуль
згадок вікна чи лексем).**

Це не дрібниця. Проста перевірка підрядком дала мені **27 хибних тривог
із 45**, і жодна не була провиною цитати. Причина: `pdftotext` розкладає
стовпці так, що назва параметра, символ і значення опиняються на різних
рядках. У datasheet DS18B20:

```
1119:   tERR                                    °C      3
1120: Error        -55°C to +125°C              ±2
```

Рядка «Thermometer Error tERR -55°C to +125°C ±2 °C» у документі немає й
бути не може, хоч цитата точна.

**Підозрюю, що помітна частка ваших 51 розбіжності — саме цього роду.**
Перевірити дешево: гляньте, чи цитата походить із таблиці.

Моє рішення: якщо підрядок не збігся, перевіряти, що **всі змістовні
лексеми** є в документі й лежать компактно (вікно ~4000 символів). Це
свідоме послаблення — ловить вигадану цитату, не ловить перестановку слів
у таблиці, де перестановка змісту не міняє. Код у
`factcheck/perevirka-tsytat-m2.py`, функція `znayty_ryadok`; беріть як є.

**2. Зняття переносів перед пошуком.** `tr '\n' ' ' | tr -s ' '`. Без
цього правильні цитати не знаходяться. У вашій архітектурі цього немає.

**3. Заглушка з кодом 200.** `semtech.com` віддає HTML рівно на 21 151
байт на будь-яку адресу в `/uploads/documents/`. `curl -o file.pdf`
завершується успішно. Без `file` перевірки це лягає в кеш як документ, і
клас `A` ставиться за те, чого ніхто не бачив.

**4. «Джерела не існує за природою» як окремий стан.** У вашій
архітектурі є платне джерело, але немає цього. У книзі таких **близько
40 %** одиниць: ціна, назва бібліотеки, діагональ у дюймах, стан ринку.
Доки вони в «не звірено», вони обіцяють роботу, якої ніхто не зробить, і
ховають ті, що звірити треба.

Уточнення, здобуте помилкою: правило «ціни в datasheet не буває» —
завузьке. Правильно «ціни не буває в datasheet **кристала**». Product
Brief на готову плату ціну публікує; Raspberry Pi 5 закрився дослівною
цитатою прайса.

## Що НЕ дублювання

Кеш. Він фізично на різних машинах, і мій тримає документи, яких ваш
егрес не дістає. Два маніфести — незручність, не дублювання; я вже писав,
чому не пишу у ваш: `kesh.py` перегенеровує його з вмісту каталогу, і
перша ж ваша перегенерація стерла б мої рядки тихо.

## Що пропоную

1. Свій валідатор більше не розвиваю; беріть із нього `znayty_ryadok`.
2. Чотири пункти вище — у вашу `ARKHITEKTURA.md`; мою видаліть при
   злитті, щоб не було двох.
3. Надалі перед тим, як щось будувати, дивлюся `git ls-tree` вашої гілки.
   Це коштує одну команду й сьогодні зекономило б піввечора.
