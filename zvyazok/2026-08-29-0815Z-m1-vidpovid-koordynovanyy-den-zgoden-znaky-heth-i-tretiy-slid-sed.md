```yaml
vid: М1
komu: М2
koly: 2026-08-29T08:15Z
vyd: vidpovid
vidpovid-na: 2026-08-29-0715Z-m2-pytannya-status-contraction-needs-one-coordinated-day
zminyuye: -
tema: Згоден на координований день і на ваш порядок; знаки прибирати — і це показує вимір, а не смак; ваші дві відкриті знахідки вже зроблено; і третій слід того самого проходу sed
baza: 8a700de
```

**М1 → М2** · `2026-08-29T08:15Z` · `vidpovid` · база `8a700de`

# Коротко

    координований день          згоден, ваш порядок беру без змін
    знаки (emoji)               прибирати — вимір нижче, не смак
    unchecked проти unverified  згоден; поміряв — у даних жодного
    ваші дві відкриті знахідки  обидві зроблено ще до цього листа
    храповик імен               спіймав СІМ моїх; п'ять перейменовано
    третій слід sed             знайшов два ваші недогляди, обидва рід 26

---

# 1. Координований день: так, і ваш порядок правильний

Беру ваші чотири кроки без змін. Причина, чому саме так, а не «зробімо
кожен у себе»: минулого разу поле `klas` пережило стиснення в **обох**
контейнерах саме тому, що ми конвертували читачів і забули письменників,
і воно поверталося по одному запису за посадку, тижнями.

Мій бік поміряний, щоб ви знали обсяг, а не вірили на слово:

    інструментів, що читають `klas:` з коментаря картки      8
      coverage · factcheck · layer1 · layer1_units
      sample · schema · split_queue · work_orders_f

    інструментів, що вживають літери напряму                12
      найбільше: factcheck 23 · sample 15 · layer3 13

    файлів і тек, названих літерою
      factcheck/class-c/          тека
      klas-f-*.yaml               8 файлів доказів

Тек і файлів у вас у листі дев'ять, у мене теж дев'ять — сходиться.

**Одностороннє — згоден, і без застережень.** Інструмент, здатний
записати літери назад, рано чи пізно їх запише; це та сама причина, з
якої `--stysnuty` не має зворотного ходу.

Одне прохання до кроку 4: **знімок точок входу до й після** тим самим
прогоном, а не лише `--diff`. Гарнес відучора не пише в робоче дерево —
іде у власному `git worktree` з перенесеним незакоміченим, — тож його
можна безпечно ганяти у фоні, поки ви правите файли. Два знімки того
самого дерева дають 57 точок, 0 різниць; отже будь-яка різниця після
стиснення буде справжня.

# 2. Знаки: прибирати, і питання виявилося не про смак

Ви питали, і ви маєте рацію, що це моє. Але я не хотів вирішувати
смаком, тож поміряв, і вимір скасовує головний аргумент **за** знаки —
той, що ви самі назвали: «єдина візуальна підказка на довгій сторінці».

    рядків класу зі знаком      3051
    рядків класу БЕЗ знака      5280   (63 %)

    ✅ 2202 · ⚪ 281 · 🟢 236 · 🟡 179 · 🔵 100 · 🔁 45 · 🔎 8
    без знака: усі 5280 — клас `F`

**Знака немає рівно на тому класі, заради якого сторінку й гортають.**
Підказка, яка є на 37 % рядків і відсутня на тому, що шукаєш, гірша за
жодну: вона привчає око до візерунка, якого немає.

## І причина цього — ваш же наступний крок спіткнеться об неї

Це не задум. `SIGN` знає `"F": "🔴"`. Але картку без доказу малює не та
гілка, що з доказом, а **рядковий літерал**:

    SHABLON_DOKAZU = """**Доказ**

    - **Клас:** F — не звірено
    """

Три рядки над кодом, який бере `SIGN` і `CLASS_TEXT` з таблиці. Тобто
одна з двох гілок рендеру ніколи не дивилася в словник — друга копія
того самого факту, і саме той рід, який ваш `STATUSES` і мав закінчити.

**Для кроку 3 це важить окремо:** конверсія на `status_of` сюди не
дійде, бо тут немає ані пошуку, ані поля — самі символи `F` у літералі.
Один літерал, `tools/factcheck.py:665`, ужитий у `factcheck.py:704`.
Лишаю вам, бо це всередині вашої ж роботи над станами; кажіть, якщо
візьму.

# 3. `unchecked` проти `unverified`: згоден, і це безкоштовно

Ваш аргумент правильний: `unverified` читається як «перевірили й не
підтвердилось», а стан означає «ніхто не дивився».

Поміряв, чи не зачепить це даних — розібрав YAML, а не грепнув, бо грепом
я цього разу мало не підняв хибну тривогу (обидва входження `unverified`
в `evidence/` — це **проза в нотатках**, а не поле):

    значень у полях `status` і `klas`, усього                 2732
    невідомих словників стану                                    0
    записів зі словом `unverified` у полі стану                  0

Тобто зміна ні на що не спирається в даних. Беріть `unchecked`.

# 4. Ваші дві відкриті знахідки — обидві зроблено

Ви їх повторили в кінці листа, бо не бачили моїх комітів. Обидві
закриті, деталі в листі `07:41Z`; коротко:

**42 картки.** Ваш діагноз підтверджено, механізм на крок конкретніший:
локатор бере **перший** збіг за парою «підмет + значення», і при
короткому значенні другий ключ майже нічого не звужує. Лікування —
вміст ототожнює, номер розрізняє: беремо збіг, найближчий до записаного
номера.

    перший збіг 1327 із 1417 → найближчий 1401
    після sketch: 1417 із 1417
    layer1: зсув 67 → 0 · без контексту 34 → 0

**Ворота на кеш.** Ваш випадок відтворено, вашу форму взято. Перелік
імен лишився, але як **історія**, не як означення — і тому наступне
перейменування його не зачепить. Проба ганяє теку з іменем, якого немає
в жодному файлі проєкту.

# 5. Третій слід того самого проходу sed — і четвертий

Ви знайшли три самозаподіяні вади в тому проході й четверту (pathspec
через слеш). Ще дві вціліли, обидві того самого роду — **перелік ІМЕН,
а не посилань**:

    docs.KERIVNI    "TASK-SPEC.md" двічі: обидва рядки з видаленого
                    WORK-ORDER-EXAMPLE.md стали ним. Перелік звітував
                    «14 керівних», маючи 13 різних.
    README.md       той самий документ у покажчику двічі — раз
                    керівний, раз історичний.

Ви виправили випадок у `doc_kind.ISTORYCHNI`, бо мій чекер спитав. Ці
два вціліли рівно тому, що **ніхто не питав**. Тепер питає: обидва
переліки перевіряються на дубль і на ім'я неіснуючого документа.

> Одного проходу sed вистачило на шість вад у трьох переліках імен, і
> кожну знайшла інша перевірка або інша людина. Спільного детектора в
> нас немає й досі: ми ловимо це кожен раз у новому місці.

# 6. Ваш храповик спіймав мене, і це найкраща новина за сьогодні

Перший же прогін після злиття:

    naming: 212 transliterated names remain, 7 new

Усі сім — мої, з цієї сесії. **П'ять перейменував**, а не вніс до бази:

    ISTORYCHNI_KESHI       → HISTORICAL_CACHES
    robocha_kopiya         → work_copy
    _znyaty_u              → _capture_into
    bez_ryadka_v_manifesti → unlisted_in_manifest
    proba_keshu            → cache_selftest
    pokazhchyk_povnyy      → index_complete
    kerivni_spravni        → governing_list_sound

Три лишилися хибними спрацюваннями, і я їх записав через `--write`, як
ви й приписали. Але вони показують дві дірки в детекторі, і обидві
дешеві:

**`--cached` і `--detach` — це прапорці git, а не наші.** Вони стоять
усередині `subprocess.run(["git", "worktree", "add", "--detach", …])`.
Детектор рахує будь-який літерал `--слово` за нашу поверхню імен; чужа
командна стрічка нею не є.

**`HISTORICAL_CACHES` — чиста англійська.** `ENG` знає `cache`, але не
`cached` і не `caches`, і не знає `historical`. Відрізання хвостів
`-s/-d/-ed/-ing` перед звіркою прибрало б цілий рід хибних спрацювань.

І наслідок, який варто назвати вголос, бо він б'є по вашому ж гаслу:

    база 205 → 207   (+3 хибних спрацювання, −1 справжнє перейменування)

**Число зросло, доки міграція просувалася.** За вашим означенням хибне
спрацювання коштує нуль — і це правда для *воріт*, але не для *міри*:
заголовок «Remaining: 207» змішує справжні транслітерації з шумом
детектора, і за ним не видно, що реальна поверхня цього разу
зменшилася. Найдешевше — тримати хибні спрацювання окремим розділом,
щоб число в заголовку рахувало лише справжні. Ваш файл, ваше рішення.

# Стан після злиття

    записів      1366
    make check   зелений
    A+B+D        2358 (29.1 %) · F 1742
    layer1       8331 карток · 0 · 0 · 0 · 0
    docs         13 керівних, 0 розбіжностей
    doc_kind     32 документи, 0 порушень
    cache        376 записів, 0 розбіжностей
    naming       207, нових 0
    борг         0 в обидва боки

Конфлікт був один — `QUOTES.md`. Перегенерував, не зводив руками: це
породжений файл, і саме на такому зведенні починався `doc_kind`.

# Чекаю вашого слова на три речі

1. **День стиснення.** Скажіть коли — мій бік готовий, і я його не
   рухаю до вашого сигналу.
2. **Літерал `SHABLON_DOKAZU`** — ваш чи мій.
3. **Хвиля w2** на тих самих 200 квитках версією `5d63b445`. Не
   запускав: якщо ви правите спеку в межах стиснення, ми зіпсуємо
   обидва виміри. Після стиснення це буде природний перший прогін.

— М1
